It's a load balancer in Google Cloud Platform.

What are those load balancers.

Let us just look at all those.

Well before we hit here I will go back to the UI and let me cancel it.

Click here.

I can even directly click here and show you the type of load balance of that we have.

So that is a CTP as Lord Allen said this restricted load balancer DCP load balancer and Europe load

balancer.

If we look at all this there are different categorization that happens in GDP or is due to be a load

balancer.

These load balancer could be internal facing Internet facing or internal single or multiple region.

Okay.

And you can treat this issue to be load balancer as are your global load balance and it can take your

internet traffic is treated by Internet traffic and it can distribute it into backend likewise is to

TB load balancer you have DCP load balancer for your DCP load balancer SSL proxy RTC CCP proxy it is

again Internet facing or internal it can be single or multi regional so you can how global load balancer

in case of DCP load balancer disappear SSL proxy load balancer or you can have internal load balancer

as well.

And then there is a Uribe traffic Internet facing internal.

It's a single region load balancer this is you can think of regional load balancer that you have available

if I click on Start configuration that is where it will ask you whether you want global external facing

are it is internal to your victims okay.

And there are different use cases as I said earlier.

Going back again to the slides.

So there are global external load balancers SGP or a steady base load balancing SSL proxy and DCP proxy.

These are the category of global load balancer.

There is no load balancers regional external load balance that is not network load balancer and regional

internal load balance that is internal load balance airplane simple internal load balancer and there

are multiple ways you can use DCP load and configure network load balancers order internal load balance

that is traditionally which is given by Google and you can use this decision three to identify which

load balancer suitable for your specific use cases are you need multiple load balancers and such that's

it guys we are going to see is deployed sensor in the next lecture.

Thank you.

これは、Google Cloud Platformのロードバランサーです。

それらのロードバランサーとは何ですか。

それらすべてを見てみましょう。

ここでヒットする前に、UIに戻ってキャンセルします。

ここをクリック。

ここをクリックして直接、負荷分散のタイプを表示することもできます。

ロードアレンがこの制限付きロードバランサーDCPロードバランサーとヨーロッパロードを言ったように、それはCTPです

バランサー。

これをすべて見ると、GDPで発生するか、負荷が原因であるさまざまな分類があります

バランサー。

これらのロードバランサーは、インターネットに直接接続する内部または単一または複数の内部領域になります。

はい。

そして、この問題をグローバルなロードバランスと同様にロードバランサーとして扱うことができます。

インターネットトラフィックはインターネットトラフィックによって処理され、バックエンドに配信できます。

TBロードバランサーDCPロードバランサー用のDCPロードバランサーがあるSSLプロキシRTC CCPプロキシ

再びインターネットに面するか内部にあるかは、単一または複数の地域である可能性があるため、グローバルなロードバランサー

DCPロードバランサーがSSLプロキシロードバランサーを失った場合、または内部ロードバランサーを使用できます

同様に。

そして、内部に面したウリベ交通インターネットがあります。

これは単一の地域のロードバランサーです。これは、利用可能な地域のロードバランサーについて考えることができます。

[設定を開始]をクリックすると、グローバルな外部向けかどうかを尋ねられます

それは被害者の内部にありますか？

先ほど言ったように、さまざまなユースケースがあります。

スライドに戻ります。

そのため、グローバル外部ロードバランサーSGPまたは安定したベースロードバランシングSSLプロキシとDCPプロキシがあります。

これらは、グローバルロードバランサーのカテゴリです。

ネットワークロードバランサーおよび地域ではないロードバランサー地域の外部負荷分散はありません。

内部負荷分散機である内部負荷分散機シンプルな内部負荷分散装置

DCPロードを使用して、ネットワークロードバランサーの内部ロードバランスを構成する複数の方法があります

これは伝統的にGoogleが提供するものであり、この決定3を使用してどの

特定のユースケースに適したロードバランサーは、複数のロードバランサーが必要な場合などです。

私たちが見ようとしている人たちは、次の講義でセンサーを配備します。

ありがとうございました。
