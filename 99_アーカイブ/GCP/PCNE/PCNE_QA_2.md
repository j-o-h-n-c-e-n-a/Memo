### 問
トラフィックを検査するためにサードパーティの次世代ファイアウォールを使用しています。送信トラフィックをファイアウォールにルーティングするカスタム ルート 0.0.0.0/0 を作成しました。パブリック IP アドレスを持たない VPC インスタンスが、ファイアウォールを介してトラフィックを送信せずに BigQuery および Cloud Pub/Sub API にアクセスできるようにしたいと考えています。どの 2 つのアクションを取る必要がありますか? (2つお選びください。)
* A. サブネット レベルで限定公開の Google アクセスをオンにします。
* B. VPC レベルで限定公開の Google アクセスを有効にします。正しい選択
* C. VPC レベルでプライベート サービス アクセスをオンにします。正しい選択
* D. デフォルトのインターネット ゲートウェイ経由で Google API およびサービスの外部 IP アドレスにトラフィックを送信するためのカスタム静的ルートのセットを作成します。
* E. デフォルトのインターネット ゲートウェイ経由で Google API およびサービスの内部 IP アドレスにトラフィックを送信するためのカスタム静的ルートのセットを作成します。
<details><div><pre>
</pre></div></details>

### 問
あなたは組織のネットワーク アーキテクチャを設計しています。あなたの組織には、Web、アプリ、データベースの 3 つの開発者チームがあります。すべての開発者チームは、重要なタスクを実行するために Compute Engine インスタンスにアクセスする必要があります。あなたは、開発者にネットワーク アクセスを提供する必要がある小規模なネットワークおよびセキュリティ チームの一員です。サブネット、ルート、ファイアウォールなどのネットワーク リソースに対する集中制御を維持する必要があります。運用上のオーバーヘッドを最小限に抑えたい。このトポロジをどのように設計すればよいでしょうか?
* A. 共有 VPC を使用してホスト プロジェクトを構成します。 Web、アプリ、データベースのサービス プロジェクトを作成します。:正しい選択
* B. Web 用に 1 つの VPC、アプリ用に 1 つの VPC、データベース用に 1 つの VPC を構成します。各 VPC 間に HA VPN を構成します。
* C. 3 つの共有 VPC ホスト プロジェクトを構成し、それぞれにサービス プロジェクトを設定します。1 つは Web 用、1 つはアプリ用、もう 1 つはデータベース用です。
* D. Web 用に 1 つの VPC、アプリ用に 1 つの VPC、データベース用に 1 つの VPC を構成します。 VPC ネットワーク ピアリングを使用して、すべての VPC をフルメッシュで接続します。
<details><div><pre>
単一の共有VPCを構成し、各サービスプロジェクトをサブネットによって割り当てする方法が解決策となります。
共有VPCを個別に分割する方法などは今回の解決策には適しません。
</pre></div></details>

### 問
あなたの組織には、リージョン us-west2 の仮想マシンからのすべての出力トラフィック ペイロードを監視することを要求する新しいセキュリティ ポリシーがあります。新しいポリシーを満たすために、同じリージョンに侵入検知システム (IDS) 仮想アプライアンスをデプロイしました。次に、us-west2 からのすべての出力トラフィック ペイロードを監視するために IDS を環境に統合する必要があります。あなたは何をするべきか？
* A. ファイアウォール ログを有効にし、フィルタリングされたすべての出力ファイアウォール ログを IDS に転送します。
* B. VPC フロー ログを有効にする。 Cloud Logging でシンクを作成し、フィルタリングされた下り VPC フローログを IDS に送信します。：正しい選択
* C. パケット ミラーリング用の内部 TCP/UDP ロード バランサを作成し、出力トラフィック用のパケット ミラーリング ポリシー フィルタを追加します。
* D. パケット ミラーリング用の内部 HTTP(S) ロード バランサを作成し、出力トラフィック用のパケット ミラーリング ポリシー フィルタを追加します。
<details><div><pre>
VPCネットワーク内のすべてのトラフィックを監視するには、VPCフローログが有効です。
</pre></div></details>

### 問
あなたは、組織の Google Cloud 環境で Cloud Router の新しいインスタンスを構成し、新しい Dended Interconnect を介してデータセンターに接続できるようにしています。営業、マーケティング、IT のそれぞれが、組織のホスト プロジェクトに接続されているサービス プロジェクトを持っています。 Cloud Router インスタンスはどこに作成する必要がありますか?
* A. すべてのプロジェクトの VPC ネットワーク
* B. IT プロジェクトの VPC ネットワーク
* C. ホスト プロジェクトの VPC ネットワーク：正しい選択
* D. セールス、マーケティング、IT プロジェクトの VPC ネットワーク
<details><div><pre>
Cloud Router インスタンスは、一般的に、VPC ネットワークが属するプロジェクトに作成 します。
</pre></div></details>

### 問
あなたの会社には 10 の個別の Virtual Private Cloud (VPC) ネットワークがあり、Google Cloud の単一リージョン内のプロジェクトごとに 1 つの VPC があります。セキュリティ チームは、各 VPC ネットワークが同じリージョン内の Partner Interconnect 接続を介してオンプレミスのメインの場所にプライベート接続できるようにすることを要求しています。コストと運用を最適化するには、同じ接続をすべてのプロジェクトで共有する必要があります。異なるプロジェクト、オンプレミスの場所、インターネットの間のすべてのトラフィックが、同じサードパーティ製アプライアンスを使用して検査できることを確認する必要があります。あなたは何をするべきか？
* A. プロジェクトごとに複数のインターフェイスと特定の Partner Interconnect VLAN アタッチメントを使用してサードパーティ製アプライアンスを構成します。サードパーティのアプライアンスと VPC ネットワーク上に関連するルートを作成します。
* B. サードパーティ アプライアンスを複数のインターフェイスで構成し、各インターフェイスが別個の VPC ネットワークに接続されるようにします。オンプレミスとインターネット接続用に別個の VPC ネットワークを作成します。サードパーティのアプライアンスと VPC ネットワーク上に関連するルートを作成します。
* C. 既存のすべてのプロジェクトのサブネットワークを単一の VPC に統合します。オンプレミスとインターネット接続用に個別の VPC ネットワークを作成します。サードパーティ アプライアンスを複数のインターフェイスで構成し、各インターフェイスが別個の VPC ネットワークに接続されます。サードパーティのアプライアンスと VPC ネットワーク上に関連するルートを作成します。
* D. サードパーティ製アプライアンスを複数のインターフェイスで構成します。すべてのプロジェクト用にハブ VPC ネットワークを作成し、オンプレミスとインターネット接続用に個別の VPC ネットワークを作成します。サードパーティのアプライアンスと VPC ネットワーク上に関連するルートを作成します。 VPC ネットワーク ピアリングを使用して、すべてのプロジェクトの VPC ネットワークをハブ VPC に接続します。ハブ VPC からカスタム ルートをエクスポートし、すべてのプロジェクトの VPC ネットワークにインポートします。：正しい選択
<details><div><pre>
* A. 
    * アプライアンスのインターフェースが増え、管理が複雑になる可能性があります。
    * コストも増加する可能性があります。
* B. 
    * アプライアンスのインターフェースが増え、管理が複雑になる可能性があります。
    * ネットワーク構成が複雑になり、トラブルシューティングが難しくなる可能性があります。
* C. 
    * すべてのプロジェクトを単一のVPCに統合することは、セキュリティや管理の観点から推奨されません。
* D. 最も適切な選択肢です。
    * ハブアンドスポークモデルを採用することで、管理が簡素化され、セキュリティも確保できます。
    * VPCピアリングにより、各プロジェクトのVPCを柔軟に接続できます。
    * カスタムルートのエクスポート/インポートにより、ルーティング設定を効率的に管理できます。
</pre></div></details>

### 問
標準の 3 層 (Web、アプリケーション、データベース) アプリケーションの負荷分散を構成しています。 Web サーバー用に外部 HTTP(S) ロード バランサを構成しました。サーバーのアプリケーション層の負荷分散を構成する必要があります。あなたは何をするべきか？
* A. アプリケーション層の既存のロード バランサーに転送ルールを構成します。
* B. アプリケーション サーバー上で等コスト マルチパス ルーティングを構成します。
* C. アプリケーション層用に新しい内部 HTTP(S) ロード バランサを構成します。：正しい選択
* D. トラフィックをアプリケーション層にルーティングするために、既存のロード バランサー上で URL マップを構成します。
<details><div><pre>
* A. 誤り: この選択肢は、アプリケーション層に専用のロードバランサーが存在することを前提としているため、問題文の状況には当てはまりません。
* B. 不適切: 等コスト マルチパス ルーティングは、複数の経路が存在する場合にトラフィックを分散させるためのルーティングプロトコルです。アプリケーション層の負荷分散には、より高度な機能を持つロードバランサーが適しています。
* C. 適切: アプリケーション層の負荷分散には、専用のロードバランサーを構成するのが一般的です。これにより、Web層との分離が可能になり、より柔軟な構成が行えます。
* D. 部分的に有効: 既存のロードバランサーを利用することで、コストを抑えることができますが、Web層とアプリケーション層の負荷分散が混在し、管理が複雑になる可能性があります。
</pre></div></details>

### 問
あなたの組織は、Virtual Private Cloud (VPC) 内の重要な Compute Engine インスタンスでハブアンドスポーク アーキテクチャを使用しています。あなたは、Google Cloud の Cloud DNS の設計を担当します。オンプレミスのデータセンターから Cloud DNS プライベート ゾーンを解決し、ハブアンドスポーク VPC 設計からオンプレミスの名前解決を有効にできる必要があります。あなたは何をするべきか？
* A. 1. ハブ VPC でプライベート DNS ゾーンを構成し、オンプレミス サーバーへの DNS 転送を構成します。
* B. 1. ハブ VPC で DNS ポリシーを設定し、スポーク VPC からの受信クエリの転送を許可します。
* C. 1. スポーク VPC で DNS ポリシーを構成し、オンプレミス DNS を代替 DNS サーバーとして構成します。
* D. 1. ハブ VPC で DNS ポリシーを構成し、オンプレミス DNS を代替 DNS サーバーとして構成します。
<details><div><pre>
上記選択肢の中で、最も適切な解決策はありません。
最も適切な解決策は、Cloud DNS ResolverとカスタムDNSサーバーを組み合わせる方法です。
* A. 部分的に有効: ハブVPCでプライベートDNSゾーンを構成することは正しいアプローチですが、オンプレミスサーバーへのDNS転送は、セキュリティリスクが高く、推奨されません。
* B. 不適切: スポークVPCからの受信クエリの転送を許可しても、オンプレミスからのDNS解決には繋がりません。
* C. 不適切: スポークVPCでDNSポリシーを構成しても、オンプレミスからのDNS解決には直接繋がりません。
* D. 部分的に有効: ハブVPCでDNSポリシーを構成することは正しいですが、オンプレミスDNSを代替DNSサーバーとするのは、DNS解決の遅延や、セキュリティリスクにつながる可能性があります。
</pre></div></details>

### 問
あなたは、組織でハイブリッド接続を担当するネットワーク管理者です。開発者チームは、共有 VPC の us-west1 リージョンで Cloud SQL を使用したいと考えています。 us-west1 で Dended Interconnect 接続と Cloud Router を構成しました。共有 VPC とオンプレミス データセンター間の接続は期待どおりに機能しています。予約された IP アドレス範囲とデフォルト設定を使用して、Cloud SQL に必要なプライベート サービス アクセス接続を作成しました。ただし、開発者はオンプレミスから Cloud SQL インスタンスにアクセスできません。あなたは問題を解決したいと考えています。あなたは何をするべきか？
* A. Cloud SQL に使用される VPC ネットワーク ピアリング接続を変更し、ルートのインポートとエクスポートを有効にします。
* B. VPC ルーティング モードをグローバルに変更します。
* C. us-west2 に追加の Cloud Router を作成します。
* D. VPC ルーティング モードをグローバルに変更します。
<details><div><pre>
上記選択肢の中で、最も適切な解決策はありません。強いて言えば、Aとなります。
* A. 部分的に有効: VPCネットワークピアリングは、異なるVPC間の接続に利用されますが、今回の問題では、オンプレミスからCloud SQLへの接続が主な課題であり、直接的な解決策とはなりません。
* B. 部分的に有効: グローバルルーティングモードに変更することで、ルーティングが簡素化される可能性がありますが、セキュリティリスクが高まるため、推奨されません。
* C. 不適切: us-west2に新たにCloud Routerを作成しても、us-west1のCloud SQLへのアクセスには直接関係ありません。
* D. 重複: 選択肢Bと同一です。
</pre></div></details>

### 問
あなたは、2 つの大都市圏にわたる地理的冗長性を備えた Partner Interconnect ハイブリッド クラウド接続ソリューションを設計しています。 Google が推奨する方法に従って、次の地域と大都市圏のペアを設定したいと考えています。 • (地域 1/大都市圏 1) • (地域 2/大都市圏 2) どうすればよいですか?
* A. Metro1-zone1-x に接続された 2 つの VLAN アタッチメントを持つ Cloud Router をリージョン 1 に作成します。
* B. Metro1-zone1-x に接続された 1 つの VLAN アタッチメントを持つ Cloud Router をリージョン 1 に作成します。
* C. Metro1-zone2-x に接続された 1 つの VLAN アタッチメントを持つ Cloud Router をリージョン 1 に作成します。
* D. リージョン 1 に Cloud Router を作成し、1 つの VLAN アタッチメントが Metro1-zone1-x に接続され、1 つの VLAN アタッチメントが Metro1-zone2-x に接続されます。
<details><div><pre>
正解：D。2つの大都市圏にわたる地理的冗長性を実現するためには、異なるゾーンにVLANアタッチメントを持つCloud Routerを構成することが最も効果的です。この構成により、高可用性、災害復旧性、および柔軟性を確保することができます。
* A. 同じゾーン内の2つのVLANアタッチメントでは、冗長性向上には繋がらず、単一障害点のリスクが残ります。
* B. 冗長性が確保されていません。
* C. 冗長性が確保されていません。
* D. 異なるゾーンにVLANアタッチメントを接続することで、ある程度の冗長性が確保できます。
</pre></div></details>

### 問
同じ Virtual Private Cloud (VPC) 内の複数のクライアントによってのみ呼び出す必要がある Compute Engine 仮想マシン インスタンスでホストされる HTTP API を開発しています。クライアントがサービスの IP アドレスを取得できるようにしたいとします。あなたは何をするべきか？
* A. 静的外部 IP アドレスを予約し、それを HTTP(S) ロード バランシング サービスの転送ルールに割り当てます。クライアントは、この IP アドレスを使用してサービスに接続する必要があります。
* B. URL https://[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal/ でインスタンス名に接続し、クライアントが Compute Engine 内部 DNS を使用していることを確認します。
* C. 静的外部 IP アドレスを予約し、それを HTTP(S) ロード バランシング サービスの転送ルールに割り当てます。次に、Cloud DNS で A レコードを定義します。クライアントは、A レコードの名前を使用してサービスに接続する必要があります。
* D. URL https://[API_NAME]/[API_VERSION]/ でインスタンス名に接続することで、クライアントが Compute Engine 内部 DNS を使用していることを確認します。
<details><div><pre>
* A. 部分的に有効: 外部IPアドレスを割り当てることで、インターネットからのアクセスが可能になります。しかし、VPC内のクライアントのみへのアクセスに限定したい場合、セキュリティ上のリスクがあります。
* B. 適切：このURL形式は、Compute Engineの内部DNSを使用して、VPC内のインスタンスにアクセスするための正しい形式です。
* C. 部分的に有効: 外部IPアドレスとCloud DNSを使用することで、アクセス性を高めることができますが、VPC内のみへのアクセスに限定したい場合は、セキュリティ上のリスクがあります。
* D. 不完全: このURL形式は、一般的なHTTPリクエストの形式ではありません。
</pre></div></details>

### 問
あなたは最近、Google Cloud プロジェクトでネットワーク使用量が毎日繰り返し急増していることに気づきました。必要なコストと管理オーバーヘッドを最小限に抑えながら、トラフィック使用率の急増の原因となっている仮想マシン (VM) インスタンスとトラフィックの種類を特定する必要があります。あなたは何をするべきか？
* A. VPC フロー ログを有効にし、分析のために出力を BigQuery に送信します。
* B. 許可されたすべてのトラフィックに対してファイアウォール ルール ロギングを有効にし、分析のために出力を BigQuery に送信します。
* C. すべてのトラフィックを VM に送信するようにパケット ミラーリングを構成します。 VM 上で Wireshark を使用して、VPC 内の各 VM のトラフィック使用率を特定します。
* D. サードパーティのネットワーク アプライアンスを展開し、デフォルト ゲートウェイとして構成します。サードパーティのネットワーク アプライアンスを使用して、ネットワーク トラフィックが多いユーザーを特定します。
<details><div><pre>
* A. 非常に有効な選択肢です。VPCフローログは、ネットワーク内のすべてのトラフィックを詳細に記録し、BigQueryで分析することで、どのVMがどの宛先にどのくらいのトラフィックを送受信しているか、詳細な情報を得ることができます。
* B. Aと同様に有効な選択肢ですが、VPCフローログほど詳細な情報は得られません。
* C. 技術的には可能ですが、現実的な解決策ではありません
* D. 大規模な環境や高度な機能が必要な場合に検討できますが、導入コストや管理コストが高くなります。
</pre></div></details>

### 問
Google Cloud 組織には、Dev と Prod という 2 つのフォルダがあります。最小限のコストですべての仮想マシン (VM) に次のファイアウォール ルールを適用する、スケーラブルで一貫した方法が必要です。 • Dev フォルダー内のプロジェクトの VM に対してポート 8080 を常に開いておく必要があります。 • ポート 8080 へのトラフィックは、Prod フォルダー内のプロジェクト内のすべての VM に対して拒否される必要があります。あなたは何をするべきか？
* A. ファイアウォール ポリシーを作成し、ポート 8080 を開くルールを使用して Dev フォルダに関連付けます。ファイアウォール ポリシーを作成し、ポート 8080 へのトラフィックを拒否するルールを使用して Prod フォルダに関連付けます。
* B. 開発プロジェクト用の共有 VPC と本番プロジェクト用の共有 VPC を作成します。開発用の共有 VPC でポート 8080 を開くための VPC ファイアウォール ルールを作成します。 Prod 用の共有 VPC でポート 8080 へのトラフィックを拒否するファイアウォール ルールを作成します。これらの共有 VPC に VM をデプロイします。
* C. 開発プロジェクトのすべての VPC で、ポート 8080 を開く VPC ファイアウォール ルールを作成します。本番プロジェクトのすべての VPC で、ポート 8080 へのトラフィックを拒否する VPC ファイアウォール ルールを作成します。
* D. Anthos Config Connector を使用して、開発 VM でポート 8080 を開き、本番 VM でポート 8080 へのトラフィックを拒否するセキュリティ ポリシーを適用します。
<details><div><pre>
* A. 有効: フォルダ単位でポリシーを設定できるため、管理が容易で、一貫性が保たれる。
* B. 欠点: 共有VPCの管理が複雑になる可能性がある。また、すべてのVMを共有VPCに移行する必要がある。
* C. 欠点: 各VPCで個別にルールを作成・管理する必要があるため、管理コストが高く、一貫性が保たれにくい。
* D. 部分的に有効: Kubernetes環境で利用できるツールであり、Kubernetesマニフェストでセキュリティポリシーを定義できる。
</pre></div></details>

### 問
gcloud コマンドライン ツールを使用して、事前定義されたロールを処理することでプロジェクトに新しいカスタム ロールを作成しています。次のエラー メッセージが表示されます。 INVALID_ARGUMENT: 権限 resourcemanager.projects.list が無効です。
* A. resourcemanager.projects.get 権限を追加して、再試行してください。
* B. 新しい名前と同じ権限を持つ別のロールで再試行します。
* C. resourcemanager.projects.list 権限を削除して、再試行します。
* D. resourcemanager.projects.setIamPolicy 権限を追加して、再試行します。
<details><div><pre>
* A. 有効な可能性あり: カスタムロールを作成する際に、プロジェクトに関する情報を取得する権限は必要です。この権限を追加することで、エラーが解消される可能性があります。
* B. 無効: 既存のロールの名前を変更しても、問題の根本的な原因は解決しません。
* C. 無効: この権限を削除すると、カスタムロールにプロジェクトに関する情報を取得する権限が一切なくなってしまうため、エラーが解消されるどころか、さらに問題が発生する可能性があります。
* D. 部分的に有効: カスタムロールにIAMポリシーを設定する権限が必要な場合もありますが、必ずしもこの権限が不足しているとは限りません。
</pre></div></details>

### 問
ネットワーク変更期間の後、会社のアプリケーションの 1 つが動作を停止します。アプリケーションは、アプリケーションからのトラフィックを一切受信しないオンプレミスのデータベース サーバーを使用します。データベース サーバーの IP アドレスは 10.2.1.25 です。変更リクエストを調べます。唯一の変更点は、3 つの追加 VPC サブネットが作成されたことです。作成された新しい VPC サブネットは 10.1.0.0/16、10.2.0.0/16、および 10.3.1.0/24/ です。オンプレミス ルーターは 10.0.0.0/8 をアドバタイズしています。この問題の最も考えられる原因は何ですか?
* A. あまり具体的ではない VPC サブネット ルートが優先されます。
* B. より具体的な VPC サブネット ルートが優先されます。
* C. オンプレミス ルーターがデータベース サーバーへのルートをアドバタイズしていません。
* D. 変更中に、オンプレミス データベース サーバーへのトラフィックをブロックするクラウド ファイアウォール ルールが作成されました。
<details><div><pre>
* A. 一般的に、より具体的なルートが優先されます。この選択肢は、今回の問題の直接的な原因とは考えにくいでしょう。
* B. これが最も可能性が高い原因です。 新しく作成されたVPCサブネットのルートが、より具体的なため、オンプレミスデータベースサーバーへのルートよりも優先されてしまい、トラフィックがそちらに転送されている可能性があります。
* C. もしオンプレミスルーターがルートをアドバタイズしていなければ、そもそもパケットがGCPネットワークに到達しません。しかし、問題はアプリケーションからのトラフィックが受信されないという点なので、この選択肢は可能性が低いと考えられます。
* D. ファイアウォールルールが原因である可能性も考えられますが、問題の状況から、より具体的なルートが優先されていることが原因である可能性の方が高いです。
</pre></div></details>

### 問
プロジェクト my-project には、Virtual Private Cloud (VPC) 内に 2 つのサブネットがあります。IP 範囲 10.128.0.0/20 の subnet-a と IP 範囲 172.16.0.0/24 の subnet-b です。データベース サーバーをサブネット a に展開する必要があります。また、アプリケーション サーバーと Web サーバーを subnet-b に展開します。アプリケーション サーバーからデータベース サーバーへのデータベース トラフィックのみを許可するファイアウォール ルールを構成したいと考えています。あなたは何をするべきか？
* A. ネットワーク タグ app-server とサービス アカウント sa-db@my-project.iam.gserviceaccount.com を作成します。タグをアプリケーション サーバーに追加し、サービス アカウントをデータベース サーバーに関連付けます。次のコマンドを実行します: gcloud compute firewall-rules create app-db-firewall-rule \
* B. サービス アカウント sa-app@my-project.iam.gserviceaccount.com および sa-db@my-project.iam.gserviceaccount.com を作成します。サービス アカウント sa-app をアプリケーション サーバーに関連付け、サービス アカウント sa-db をデータベース サーバーに関連付けます。次のコマンドを実行します: gcloud compute firewall-rules create app-db-firewall-ru
* C. サービス アカウント sa-app@my-project.iam.gserviceaccount.com および sa-db@my-project.iam.gserviceaccount.com を作成します。サービス アカウント sa-app をアプリケーション サーバーに関連付け、サービス アカウント sa-db をデータベース サーバーに関連付けます。次のコマンドを実行します: gcloud compute firewall-rules create app-db-firewall-ru
* D. ネットワーク タグ app-server および db-server を作成します。 app-server タグをアプリケーション サーバーに追加し、db-server タグをデータベース サーバーに追加します。次のコマンドを実行します: gcloud compute firewall-rules create app-db-firewall-rule \
<details><div><pre>
* A, B, C: これらの選択肢は、サービスアカウントを使用しようとしていますが、サービスアカウントは、VMインスタンスへの認証に主に使用され、ファイアウォールルールのターゲットとして直接使用することはできません。
* D. 最適
    * シンプルで分かりやすい: ネットワークタグは、インスタンスのグループ化に非常に便利です。
    * 柔軟性: 異なるタグを組み合わせることで、より複雑なアクセス制御を実現できます。
    * セキュリティ: 特定のタグを持つインスタンス間のトラフィックのみを許可することで、セキュリティを強化できます。
</pre></div></details>

### 問
単一の Dended Interconnect が正常にプロビジョニングされました。物理接続は、us-west2 に最も近いコロケーション施設にあります。ワークロードの 75% は us-east4 にあり、ワークロードの残りの 25% は us-central1 にあります。すべてのワークロードは同じネットワーク トラフィック プロファイルを持ちます。 VLAN アタッチメントを展開するときは、データ転送コストを最小限に抑える必要があります。あなたは何をするべきか？
* A. 既存の専用インターコネクトを維持します。 VLAN アタッチメントを us-west2 の Cloud Router にデプロイし、VPC グローバル ルーティングを使用して us-east4 と us-central1 のワークロードにアクセスします。
* B. 既存の専用インターコネクトを維持します。 VLAN アタッチメントを us-east4 の Cloud Router にデプロイし、別の VLAN アタッチメントを us-central1 の Cloud Router にデプロイします。
* C. us-east4 に最も近いコロケーション施設用に新しい専用インターコネクトを注文し、VPC グローバル ルーティングを使用して us-central1 のワークロードにアクセスします。
* D. us-central1 に最も近いコロケーション施設用に新しい専用インターコネクトを注文し、VPC グローバル ルーティングを使用して us-east4 のワークロードにアクセスします。
<details><div><pre>
* A. 不適切：us-west2からus-east4とus-central1へのトラフィックは、地理的に遠いため、レイテンシーが高くなり、コストも増加する可能性があります。
* B. 　適切：
    * 既存のインターコネクトの有効活用: 既に設置されているインターコネクトを有効活用することで、初期コストを削減できます。
    * 各リージョンへの最適化: 各リージョンにVLANアタッチメントを配置することで、レイテンシーを低減し、パフォーマンスを向上させることができます。
    * VPCグローバルルーティング: VPCグローバルルーティングを利用することで、複数のリージョンにまたがるネットワークを効率的に管理できます。
* C. 不適切：us-east4に新しいインターコネクトを設置するのも、コストがかかり、管理も複雑になります。
* D. 不適切：us-central1に新しいインターコネクトを設置するのも、Aと同様の問題があります
</pre></div></details>

### 問
ポート 80、8080、および 443 を使用して、IPv4 と IPv6 の両方の仮想 IP アドレスの背後で外部に公開される新しい HTTP アプリケーションを構成しています。us-west1 と us-east1 の 2 つのリージョンにバックエンドがあります。高可用性と自動スケーリングを確保しながら、可能な限り低い遅延でコンテンツを提供し、HTTP ホスト名とリクエスト パスを使用してネイティブ コンテンツ ベースのルールを作成したいと考えています。ロード バランサーに接続するクライアントの IP アドレスは、バックエンドから認識できる必要があります。どの構成を使用する必要がありますか?
* A. ネットワーク負荷分散を使用する
* B. PROXY プロトコルを有効にして TCP プロキシ ロード バランシングを使用する
* C. URL マップとカスタム ヘッダーを使用した外部 HTTP(S) 負荷分散の使用
* D. URL マップと X-Forwarded-For ヘッダーを使用した外部 HTTP(S) 負荷分散の使用
<details><div><pre>
* A. 不適切：L3/L4の負荷分散であり、HTTPのヘッダーベースのルーティングや、クライアントIPアドレスの保持には不向きです。
* B. 不適切： TCPレベルの負荷分散であり、HTTPのヘッダーベースのルーティングは可能です。しかし、クライアントIPアドレスが保持されない可能性があります。
* C. 不適切：URLマップによるルーティングは可能ですが、X-Forwarded-Forヘッダーを使用していないため、クライアントIPアドレスが保持されません。
* D. 　適切：
    * URLマップ: HTTPホスト名とリクエストパスに基づいたルーティングが可能。
    * X-Forwarded-Forヘッダー: クライアントIPアドレスを保持し、バックエンドに転送。
    * 外部HTTP(S)負荷分散: 高可用性、自動スケーリング、SSL終端など、豊富な機能を提供。
</pre></div></details>

### 問
次のファイアウォール ルールセットが Virtual Private Cloud (VPC) 内のすべてのインスタンスに適用されています: ファイアウォール ルールを更新して、次のルールをルールセットに追加する必要があります: 新しいユーザー アカウントを使用しています。ファイアウォール ルールを更新する前に、適切な ID およびアクセス管理 (IAM) ユーザー ロールをこの新しいユーザー アカウントに割り当てる必要があります。新しいユーザー アカウントは、アップデートを適用し、ファイアウォール ログを表示できる必要があります。あなたは何をするべきか？
* A. compute.securityAdmin およびlogging.viewer ルールを新しいユーザー アカウントに割り当てます。優先度 50 の新しいファイアウォール ルールを適用します。
* B. 新しいユーザー アカウントに compute.securityAdmin ロールとlogging.bucketWriter ロールを割り当てます。優先度 150 の新しいファイアウォール ルールを適用します。
* C. 新しいユーザー アカウントに compute.orgSecurityPolicyAdmin ロールとlogging.viewer ロールを割り当てます。優先度 50 の新しいファイアウォール ルールを適用します。
* D. compute.orgSecurityPolicyAdmin およびlogging.bucketWriter ロールを新しいユーザー アカウントに割り当てます。優先度 150 の新しいファイアウォール ルールを適用します。
<details><div><pre>
* A. 不適切：
    * compute.securityAdmin ロールは、セキュリティポリシーの作成や更新、およびファイアウォールルールの表示を許可しますが、ログの書き込みは許可しません。
    * logging.viewer ロールは、ログの閲覧のみを許可します。
* B. 不適切：
    * compute.securityAdmin ロールはAと同様です。
    * logging.bucketWriter ロールは、ログの書き込みを許可しますが、セキュリティポリシーの管理には不十分です。
* C. 不適切：
    * compute.orgSecurityPolicyAdmin ロールは、組織全体のセキュリティポリシーを管理できる強力なロールです。今回の目的である、特定のプロジェクト内のファイアウォールルールの更新にはオーバースペックです。
* D. 　適切：
    * compute.orgSecurityPolicyAdmin: セキュリティポリシーの作成、更新、削除を許可します。今回の目的であるファイアウォールルールの更新に最適です。
    * logging.bucketWriter: ファイアウォールログの書き込みを許可します。
</pre></div></details>

### 問
Virtual Private Cloud (VPC) とオンプレミス ネットワークの間に HA VPN 接続を構成しています。 VPN ゲートウェイの名前は VPN_GATEWAY_1 です。プロジェクトで作成された VPN トンネルがオンプレミス VPN パブリック IP アドレス 203.0.113.1/32 にのみ接続するように制限する必要があります。あなたは何をするべきか？
* A. 203.0.113.1/32 を受け入れるファイアウォール ルールを設定し、ターゲット タグを VPN_GATEWAY_1 に設定します。
* B. 203.0.113.1/32 アドレスのみで構成されるallowList を使用するように、リソース マネージャーの制約constraints/compute.restrictVpnPeerIPsを構成します。
* C. Google Cloud Armor セキュリティ ポリシーを構成し、203.0.113.1/32 を許可するポリシー ルールを作成します。
* D. 203.0.113.1/32 を除くすべてのトラフィックを拒否するようにピア VPN ゲートウェイでアクセス コントロール リストを構成し、それをプライマリ外部インターフェイスに接続します。
<details><div><pre>
* A. 不適切：ファイアウォールルールは、VMインスタンスへの入出力トラフィックを制御するもので、VPNトンネルのピアリング相手を制限する目的には合いません。
* B. 　適切：
    * compute.restrictVpnPeerIPs: VPNピアリングのIPアドレスを制限するのに最適な制約です。
    * allowListに指定したIPアドレスのみがVPNピアとして接続できるようになります。
* C. 不適切：Cloud Armorは、Webアプリケーションファイアウォールであり、VPN接続の制御には適していません。
* D. 不適切：ピアVPNゲートウェイのアクセス制御リストは、特定のトラフィックを許可または拒否するもので、VPNピアリングのIPアドレスを制限する目的には合いません。
</pre></div></details>

### 問
あなたは会社の Google Cloud への移行を完了し、3 つの Virtual Private Cloud (VPC) ネットワークを備えたアーキテクチャを構成しました。1 つは営業用、1 つは財務用、もう 1 つはエンジニアリング用です。各 VPC には 100 を超える Compute Engine インスタンスが含まれており、Sales VPC と Finance VPC のインスタンスを使用する開発者は相互にプライベート接続を必要とします。パフォーマンスやセキュリティを損なうことなく、営業と財務間の通信を許可する必要があります。あなたは何をするべきか？
* A. Finance VPC と Sales VPC の間に HA VPN ゲートウェイを構成します。
* B. 外部 IP アドレスを使用して相互に通信する必要があるインスタンスを構成します。
* C. Finance VPC と Sales VPC の間に VPC ネットワーク ピアリング接続を作成します。
* D. Sales および Finance VPC で Cloud NAT と Cloud Router を構成します。
<details><div><pre>
* A. 不適切：オンプレミス環境との接続に適していますが、VPC内での通信にはオーバーヘッドが大きくなり、パフォーマンスが低下する可能性があります。
* B. 不適切：インターネットを経由するため、セキュリティリスクが高く、パフォーマンスも低下します。
* C. 　適切：
    * プライベートネットワーク: インターネットを経由せずに、プライベートネットワーク内で通信するため、セキュリティが高く、パフォーマンスも優れています。
    * スケーラビリティ: 多くのインスタンス間の通信を効率的に処理できます。
    * 柔軟性: ファイアウォールルールで通信を細かく制御できます。
* D. 不適切：インターネット経由の通信に適しており、今回の要件には合いません。
</pre></div></details>

### 問
あなたは、グローバル HTTP(S) ロードバランサによって公開される Compute Engine インスタンスを使用して、新しいグローバル アプリケーションを設計しています。分散型サービス拒否攻撃やアプリケーション層 (レイヤー 7) 攻撃からアプリケーションを保護する必要があります。あなたは何をするべきか？
* A. VPC Service Controls を構成し、安全な境界を作成します。きめ細かい境界制御を定義し、そのセキュリティ体制を Google Cloud サービスとプロジェクト全体に適用します。
* B. プロジェクトで Google Cloud Armor セキュリティ ポリシーを構成し、それをバックエンド サービスにアタッチしてアプリケーションを保護します。
* C. Compute Engine インスタンスを分散型サービス拒否攻撃から保護するために VPC ファイアウォール ルールを構成します。
* D. グローバル HTTP(S) ロード バランサのパブリック IP アドレスの階層型ファイアウォール ルールを組織レベルで構成します。
<details><div><pre>
* A. 不適切：マルチクラウド環境におけるデータの保護に適していますが、今回のケースでは、単一のプロジェクト内のアプリケーションのセキュリティ対策としてオーバースペックです。
* B. 適切：
    * レイヤー7攻撃への対応: WAF機能により、SQLインジェクション、クロスサイトスクリプティングなどのWebアプリケーションに対する攻撃を検知し、防御できます。
    * DDoS攻撃への対応: DDoS攻撃に対して、トラフィックを監視し、異常なトラフィックをブロックすることで、アプリケーションへの影響を軽減します。
    * カスタムルール: 独自のセキュリティルールを作成し、アプリケーションに合わせた保護を実現できます。
    * 統合: グローバル HTTP(S) ロードバランサとシームレスに統合でき、簡単に設定できます。
* C. 不適切：VPC内のインスタンスへの入出力トラフィックを制御するもので、DDoS攻撃やレイヤー7攻撃に対する包括的な保護を提供できません。
* D. 不適切：ネットワークレベルのセキュリティ対策ですが、アプリケーション層の攻撃に対しては十分な保護を提供できません。
</pre></div></details>

### 問
IPv4 と IPv6 の両方のアドレスを使用して外部ロード バランサーの背後に公開され、ポート 443 での TCP パススルーをサポートする新しいアプリケーションを構成しています。us-west1 と us-east1 の 2 つのリージョンにバックエンドがあります。高可用性と自動スケーリングを確保しながら、可能な限り低い遅延でコンテンツを提供したいと考えています。どの構成を使用する必要がありますか?
* A. 両方のリージョンのバックエンドでグローバル SSL プロキシ ロード バランシングを使用します。
* B. 両方のリージョンのバックエンドでグローバル TCP プロキシ ロード バランシングを使用します。
* C. 両方のリージョンのバックエンドでグローバル外部 HTTP(S) 負荷分散を使用します。
* D. 両方のリージョンでネットワーク負荷分散を使用し、DNS ベースの負荷分散を使用してトラフィックを最も近いリージョンに送信します。
<details><div><pre>
* A. 不適切：SSL終端が必要な場合に適していますが、TCPパススルーには対応していません。
* B. 不適切：TCPレベルのロードバランシングですが、HTTP/HTTPSに特化した機能が不足しており、今回の要件には合いません。
* C. 　適切：
    * TCPパススルー: ポート443でのTCPパススルーをサポートしています。
    * IPv4/IPv6: IPv4とIPv6の両方をサポートしています。
    * グローバル: 複数のリージョンにまたがる負荷分散が可能で、高可用性を実現できます。
    * HTTP/HTTPS: HTTP/HTTPSに特化しており、セキュリティ機能も充実しています。
* D. 不適切：L3/L4の負荷分散であり、HTTP/HTTPSの機能が不足しており、今回の要件には合いません。
</pre></div></details>

### 問
あなたは会社の組織管理者です。エンジニアの 1 人は、複数のフォルダーにわたる複数のホスト プロジェクトのセットアップと、サービス プロジェクトとのサブネットの共有を担当します。最小限の手順でタスクを完了するには、エンジニアの ID およびアクセス管理 (IAM) 構成を有効にする必要があります。あなたは何をするべきか？
* A. フォルダー レベルでコンピューティング共有 VPC 管理者の IAM ロールを持つエンジニアをセットアップします。
* B. エンジニアに組織レベルでコンピューティング共有 VPC 管理者の IAM ロールを設定します。
* C. エンジニアにコンピューティング共有 VPC 管理者の IAM ロールとプロジェクト IAM 管理者のロールをフォルダー レベルで設定します。
* D. エンジニアにコンピューティング共有 VPC 管理者 IAM ロールとプロジェクト IAM 管理者ロールを組織レベルで設定します。
<details><div><pre>
* A. 不適切：フォルダーレベルでロールを設定すると、そのフォルダー以下のプロジェクトにしか権限が及ばず、すべてのプロジェクトに対して一括で管理することができません。
* B. 　適切：
    * シンプルさ: 組織レベルで1つのロールを設定するだけで、すべてのプロジェクトに対してコンピューティング共有VPCを管理できるため、最もシンプルです。
    * 効率性: エンジニアは、どのプロジェクトに対しても同じ権限で作業できるため、作業効率が向上します。
    * 一貫性: 組織全体のポリシーを統一的に適用できます。
* C. 不適切： フォルダーレベルでコンピューティング共有VPC管理者、プロジェクトレベルでプロジェクトIAM管理者と、2つのロールを設定する必要があり、管理が煩雑になります。
* D. 不適切：組織レベルでコンピューティング共有VPC管理者、プロジェクトレベルでプロジェクトIAM管理者と、2つのロールを設定する必要があり、A,Cと同様に管理が煩雑になります。
</pre></div></details>

### 問
組織には、複数の Virtual Private Cloud (VPC) を含む 1 つのプロジェクトがあります。企業のパブリック ネットワーク内のリソースからのみ API アクセスを許可することで、Cloud Storage バケットと BigQuery データセットへの API アクセスを保護する必要があります。あなたは何をするべきか？
* A. VPC と企業のパブリック ネットワークの IP 範囲を許可するアクセス コンテキスト ポリシーを作成し、そのポリシーを Cloud Storage と BigQuery にアタッチします。
* B. 企業のパブリック ネットワークの IP 範囲を許可するアクセス コンテキスト ポリシーを使用して、プロジェクトの VPC Service Controls 境界を作成します。
* C. ファイアウォール ルールを作成して、未承認のネットワークから Cloud Storage と BigQuery への API アクセスをブロックします。
* D. 企業のパブリック ネットワークの IP 範囲を許可するアクセス コンテキスト ポリシーを使用して、VPC ごとに VPC Service Controls 境界を作成します。
<details><div><pre>
* A. 不適切：
    * VPC 内のすべてのリソースからのアクセスを許可してしまう可能性があり、セキュリティが不十分です。
    * Cloud Storage と BigQuery へのアクセスを個別に制御したい場合、ポリシー管理が煩雑になる可能性があります。
* B. 　適切：
    * プロジェクト全体を保護: プロジェクトレベルで境界を作成することで、プロジェクト内のすべてのサービスを一貫して保護できます。
    * 詳細なアクセス制御: アクセスコンテキストポリシーを使用することで、IPアドレスだけでなく、その他の属性（例えば、サービスアカウント）に基づいたきめ細かいアクセス制御が可能です。
    * 管理の効率化: プロジェクト全体で一貫したポリシーを適用できるため、ポリシー管理が簡素化されます。
* C. 不適切：ファイアウォールルールは、IPアドレスベースのアクセス制御に適していますが、より細かいアクセス制御や、複数のプロジェクトやサービスにわたる一貫性のあるポリシー管理には不向きです。
* D. 不適切：VPC ごとの管理は柔軟性がありますが、複数のVPCがある場合、管理が煩雑になる可能性があります。
</pre></div></details>

### 問
あなたの組織は、仮想マシン (VM) 間のフローを制御するためにファイアウォール ルールを適用する方法を制御する新しいセキュリティ ポリシーを実装しています。 Google が推奨する方法を使用して、VM A と VM B の間のトラフィックを厳密に制御するファイアウォール ルールを設定する必要があります。VPC 内の VM A から VM B へのみ通信が流れ、他の通信パスが流れないことを確認する必要があります。許可された。 VPC には他のファイアウォール ルールは存在しません。この通信パスのみを許可するには、どのファイアウォール ルールを設定する必要がありますか?
<details><div><pre>
* D. ファイアウォール ルールの方向: 入力
</pre></div></details>

### 問
あなたは組織のためにハイブリッド クラウド環境を設計しています。 Google Cloud 環境は、Cloud HA VPN と Cloud Router を使用してオンプレミス ネットワークと相互接続されます。 Cloud Router はデフォルト設定で構成されています。オンプレミス DNS サーバーは 192.168.20.88 にあり、ファイアウォールで保護されています。Compute Engine リソースは 10.204.0.0/24 にあります。 Compute Engine リソースは、Google Cloud ホスト名を解決しながら、ドメイン corp.altostrat.com を使用してオンプレミスのプライベート ホスト名を解決する必要があります。 Google が推奨する慣行に従いたいと考えています。あなたは何をするべきか？
<details><div><pre>
* C. 1. Cloud DNS に、192.168.20.88 を指す、corp-altostrat-com という「corp .altostrat.com」のプライベート転送ゾーンを作成します。
</pre></div></details>

### 問
あなたには、組織のエンタープライズ ネットワークが Google Workspace にアクセスして使用するための新しい接続ソリューションを設計する責任があります。 us-west1 に Compute Engine インスタンスを含む既存の共有 VPC があります。現在、サービス プロバイダのインターネット アクセスを介して Google Workspace にアクセスします。ネットワークと Google の間に直接接続を設定したいと考えています。あなたは何をするべきか？
* A. 同じ大都市圏で D dedicated Interconnect 接続を注文します。 VLAN アタッチメント、us-west1 の Cloud Router、および Cloud Router とルーター間のボーダー ゲートウェイ プロトコル（BGP）セッションを作成します。
* B. 同じ大都市圏でダイレクト ピアリング接続を注文します。 Google とルーターの間にボーダー ゲートウェイ プロトコル (BGP) セッションを構成します。
* C. us-west1 で HA VPN を構成します。 Cloud Router とオンプレミス データセンターの間にボーダー ゲートウェイ プロトコル（BGP）セッションを構成します。
* D. 同じ大都市圏でキャリア ピアリング接続を注文します。 Google とルーターの間にボーダー ゲートウェイ プロトコル (BGP) セッションを構成します。
<details><div><pre>
* A. 適切：
    * メリット: 高帯域幅、低遅延、高信頼性。専用線のため、他のユーザーの影響を受けにくい。
    * デメリット: 比較的高コスト、物理的な接続作業が必要。
    * 評価:
        * 高い信頼性とパフォーマンスを求める場合に最適。
        * 大量のデータ転送やリアルタイムなアプリケーションに適している。
* B. 不適切：
    * メリット: Dedicated Interconnectよりも低コスト。
    * デメリット: 帯域幅が制限される場合がある。
    * 評価:
        * コストを抑えつつ、比較的高い帯域幅を必要とする場合に適している。
* C. 不適切：
    * メリット: 低コスト、柔軟な設定が可能。
    * デメリット: インターネット経由のため、パブリックインターネットの影響を受ける。
    * 評価:
        * 既存のネットワーク環境との統合が容易。
        * 低コストでVPN接続を実現したい場合に適している。
* D. 不適切：
    * メリット: Dedicated Interconnectよりも低コスト、複数のネットワークプロバイダーを利用可能。
    * デメリット: 設定が複雑になる場合がある。
    * 評価:
        * 多様なネットワークプロバイダーを利用したい場合に適している。
</pre></div></details>

### 問
あなたは、ポート 800 で内部公開されるバックエンドを持つ新しいアプリケーションを設計しています。このアプリケーションは、ポート 700 の TCP 経由で IPv4 と IPv6 の両方を使用して外部に公開されます。このアプリケーションの高可用性を確保したいと考えています。あなたは何をするべきか？
* A. 2 つのインスタンスを持つ 1 つのインスタンス グループを含むバックエンド サービスを使用するネットワーク ロード バランサーを作成します。
* B. 2 つのインスタンスを持つターゲット プール バックエンドを使用するネットワーク ロード バランサーを作成します。
* C. 1 つのインスタンスを含むゾーン ネットワーク エンドポイント グループを使用する TCP プロキシを作成します。
* D. 2 つのインスタンスを持つインスタンス グループを含むバックエンド サービスを使用する TCP プロキシを作成します。
<details><div><pre>
* A. 不適切：
    * メリット:
        * ネットワーク負荷分散の機能を活用できる。
        * インスタンスグループを使用することで、スケールイン/アウトが容易。
    * デメリット:
        * 単一のバックエンドサービスのため、より高度な負荷分散やフェイルオーバーシナリオに対応するには、追加設定が必要になる場合がある。
* B. 　適切：
    * メリット:
        * ターゲットプールを使用することで、複数のインスタンスグループを管理できる。
        * より柔軟な負荷分散が可能。
    * デメリット:
        * 設定が若干複雑になる可能性がある。
* C. 不適切：
    * デメリット:
        * 高可用性が確保できない。
        * 単一のインスタンスに負荷が集中する可能性がある。
* D. 不適切：
    * メリット:
        * TCPプロキシは、HTTP/HTTPS以外のプロトコルを扱う場合に有効。
        * インスタンスグループを使用することで、スケールイン/アウトが容易。
    * デメリット:
        * ネットワーク負荷分散の機能は限定的。
</pre></div></details>

### 問
会社のセキュリティ チームは、セキュリティの脅威から保護するために、Web サーバーに到達できる受信トラフィックの種類を制限したいと考えています。 TCP のみの HTTP および HTTPS Web トラフィックを処理するには、Virtual Private Cloud (VPC) 内の Web サーバーでファイアウォール ルールを構成する必要があります。あなたは何をするべきか？
* A. TCP ポート 80 のすべての IP アドレスを許可するために、ターゲット タグ「web-server」を使用して一致入力ファイアウォール ルールを作成します。
* B. ターゲット タグ「web-server」を使用して、一致する出力ファイアウォール ルールを作成し、TCP ポート 80 のすべての IP アドレスを許可します。
* C. ターゲット タグ「web-server」を使用して、一致する入力ファイアウォール ルールを作成し、TCP ポート 80 および 443 のすべての IP アドレスを許可します。
* D. ターゲット タグ「web-server」を使用して、一致する出力ファイアウォール ルールを作成し、TCP ポート 80 および 443 の Web サーバー IP アドレスを許可します。
<details><div><pre>
* A. 不適切：HTTPS (ポート443) のトラフィックを許可していないため、不十分です。
* B. 不適切：出力ルールは、サーバーから外部への発信トラフィックを制御するため、今回の目的には合いません。
* C. 　適切：
    * 入力ルールは、サーバーへの着信トラフィックを制御するため、今回の目的に合っています。
    * ターゲットタグ「web-server」でWebサーバーを特定し、ポート80と443のTCPトラフィックのみを許可することで、HTTPとHTTPSのアクセスを許可しつつ、他の不要なトラフィックをブロックできます。
* D. 不適切：出力ルールであるため、今回の目的に合いません。
</pre></div></details>

### 問
最近、デフォルトのルーティング構成を使用して、Virtual Private Cloud (VPC) 内のリージョン us-west1 と us-east1 に Compute Engine インスタンスをデプロイしました。会社のセキュリティ ポリシーでは、仮想マシン (VM) にパブリック IP アドレスを接続してはならないと規定しています。外部アクセスを防止しながら、インスタンスがインターネットから更新を取得できるようにする必要があります。あなたは何をするべきか？
* A. us-west1 と us-east1 の両方に Cloud NAT ゲートウェイと Cloud Router を作成します。
* B. VPC 内に単一のグローバル Cloud NAT ゲートウェイとグローバル Cloud Router を作成します。
* C. インスタンスのネットワーク インターフェイスの外部 IP アドレスを [なし] から [一時的] に変更します。
* D. 宛先 0.0.0.0/0 への送信を許可するファイアウォール ルールを作成します。
<details><div><pre>
* A. 　適切： Cloud NATは、パブリックIPアドレスを共有して、複数のインスタンスからインターネットにアクセスできるようにするサービスです。リージョンごとにCloud NATとCloud Routerを作成することで、それぞれのリージョンのインスタンスがインターネットにアクセスできます。
* B. 不適切：有効な選択肢ですが、Aよりも柔軟性が低い可能性があります。 グローバルなリソースは、複数のリージョンにまたがって使用できますが、リージョンごとのトラフィックパターンや要件が異なる場合、パフォーマンスが低下する可能性があります。
* C. 不適切：不適切な選択肢です。 一時的なパブリックIPアドレスを割り当てることになり、セキュリティポリシーに違反します。
* D. 不適切：不適切な選択肢です。 この設定は、すべてのインターネットへのトラフィックを許可してしまうため、セキュリティリスクが高まります。
</pre></div></details>

### 問
あなたは、会社のクラウドベース環境用のハブアンドスポーク ネットワーク アーキテクチャを設計しています。すべてのスポークがハブとピアリングされていることを確認する必要があります。スポークは、インターネット アクセスにハブの仮想アプライアンスを使用する必要があります。仮想アプライアンスは、IP アドレス 10.0.0.5 の内部ロード バランサを使用する 2 つのインスタンスで高可用性モードで構成されています。あなたは何をするべきか？
* C. 1. 仮想アプライアンスのネクストホップ インスタンスを指す 2 つのデフォルト ルートをハブ VPC に作成します。
* B. 1. IP アドレス 10.0.0.5 を指すデフォルト ルートをハブ VPC に作成します。：正しい選択

### 問
あなたは、3 層アプリケーション アーキテクチャをオンプレミスから Google Cloud に移行しています。移行の最初のステップとして、外部 HTTP(S) ロード バランサを使用して新しい Virtual Private Cloud (VPC) を作成します。このロード バランサーは、プレゼンテーション層を実行するオンプレミスのコンピューティング リソースにトラフィックを転送します。悪意のあるトラフィックが VPC に入り、エッジでリソースを消費するのを阻止する必要があるため、IP アドレスをフィルタリングし、クロスサイト スクリプティング (XSS) 攻撃を阻止するようにこのポリシーを構成する必要があります。あなたは何をするべきか？
* A. Google Cloud Armor ポリシーを作成し、アンマネージド インスタンス グループ バックエンドを使用するバックエンド サービスに適用します。
* B. 階層型ファイアウォール ルールセットを作成し、それを VPC の親組織リソース ノードに適用します。
* C. Google Cloud Armor ポリシーを作成し、インターネット ネットワーク エンドポイント グループ (NEG) バックエンドを使用するバックエンド サービスに適用します。
* D. VPC ファイアウォール ルールセットを作成し、それを非マネージド インスタンス グループ内のすべてのインスタンスに適用します。
<details><div><pre>
* A. 有効な選択肢です。 Cloud Armorは、WAF機能を提供し、XSS攻撃などのWebアプリケーション攻撃から保護します。また、IPアドレスベースのフィルタリングも可能です。ただし、アンマネージドインスタンスグループは、Google Cloudのマネージドサービスではないため、運用管理が複雑になる可能性があります。
* B. 部分的に有効な選択肢ですが、範囲が広すぎます。 親組織リソースノードに適用すると、VPC内のすべてのリソースに影響を与えてしまう可能性があります。
* C. 最も有効な選択肢です。 NEGは、マネージドサービスであり、スケーラブルかつ高可用性のバックエンドを提供します。Cloud Armorと組み合わせることで、WAF機能とIPアドレスベースのフィルタリングを効果的に活用できます。
* D. 部分的に有効な選択肢です。 VPCファイアウォールは、ネットワークレベルのセキュリティを提供しますが、WAF機能は提供しません。IPアドレスベースのフィルタリングは可能です。
</pre></div></details>

### 問
次のプライベート Google Kubernetes Engine (GKE) クラスターのデプロイメントがあります。 内部 IP アドレス 192.168.40 2/24 で、外部 IP アドレスが割り当てられていない仮想マシン (VM) が、サブネットワーク kubernetes-management 内の同じ VPC にデプロイされています。 kubectl を使用してクラスター マスターと通信する必要があります。あなたは何をするべきか？
* A. ネットワーク 192.168.40.0/24 を masterAuthorizedNetworksConfig に追加します。エンドポイント 192.168.38.2 と通信するように kubectl を構成します。
* B. ネットワーク 192.168.38.0/28 を masterAuthorizedNetworksConfig に追加します。エンドポイント 192.168.38.2 と通信するように kubectl を構成する
* C. ネットワーク 192.168.36.0/24 を masterAuthorizedNetworksConfig に追加します。エンドポイント 192.168.38.2 と通信するように kubectl を構成する
* D. 外部 IP アドレスを VM に追加し、この IP アドレスを masterAuthorizedNetworksConfig に追加します。エンドポイント 35.224.37.17 と通信するように kubectl を構成します。
<details><div><pre>
* A. 不適切: 追加するネットワークと通信先のIPアドレスが一致していません。
* B. 適切な可能性があります。
    * 通信先のIPアドレスと、masterAuthorizedNetworksConfigに追加するネットワークが一致しています。
    * ただし、/28というサブネットマスクは、非常に狭い範囲であり、誤って他のノードをブロックしてしまう可能性があります。
    * クラスター内のノードのIPアドレス範囲を考慮し、適切なサブネットマスクを選択する必要があります。
* C. 不適切: 追加するネットワークと通信先のIPアドレスが一致していません。
* D. 不適切: 問題文で「外部IPアドレスが割り当てられていない」と明記されています。
</pre></div></details>

### 問
デフォルトの Virtual Private Cloud (VPC) 内の仮想マシン (VM) の 1 つがサービス拒否攻撃を受けている疑いがあります。トラフィックの送信元を理解するには、VM の受信トラフィックを分析する必要があります。あなたは何をするべきか？
* A. VPC のデータ アクセス監査ログを有効にします。ログを分析し、subnetworks.get フィールドから送信元 IP アドレスを取得します。
* B. サブネットの VPC フロー ログを有効にします。ログを分析し、接続フィールドから送信元 IP アドレスを取得します。
* C. VPA の VPC フロー ログを有効にします。ログを分析し、src_location フィールドから送信元 IP アドレスを取得します。
* D. サブネットのデータ アクセス監査ログを有効にします。ログを分析し、networks.get フィールドから送信元 IP アドレスを取得します。
<details><div><pre>
* A. 不適切：データアクセス監査ログは、GCPリソースへのアクセスに関する情報を記録します。しかし、今回の目的であるネットワークトラフィックの分析には、あまり適していません。
* B. 　適切：VPCフローログは、ネットワークトラフィックに関する詳細な情報を提供します。接続フィールドから送信元IPアドレスを取得できるため、今回の目的に合致しています。
* C. 不適切：VPA（Virtual Private Cloud Edge）は、VPCとオンプレミスネットワークを接続するためのサービスであり、今回のケースには直接関係ありません。
* D. 不適切：データアクセス監査ログは、ネットワークトラフィックではなく、リソースへのアクセスに関する情報を記録します。
</pre></div></details>

### 問
既存の Virtual Private Cloud (VPC) のプライベート サブネットで複数のマイクロサービスが実行されています。 Cloud Run と Cloud Functions を使用してマイクロサービスにアクセスする追加のサーバーレス サービスを作成する必要があります。サーバーレス サービスとプライベート マイクロサービス間のネットワーク トラフィック量は少ないです。ただし、各サーバーレス サービスは、任意のマイクロサービスと通信できる必要があります。コストを最小限に抑えるソリューションを実装したいと考えています。あなたは何をするべきか？
* A. サーバーレス サービスをサーバーレス VPC にデプロイします。サーバーレス VPC を既存の VPC にピアリングします。サーバーレス サービスと既存のマイクロサービス間のトラフィックを許可するようにファイアウォール ルールを構成します。
* B. サーバーレス サービスごとにサーバーレス VPC アクセス コネクタを作成します。サーバーレス サービスと既存のマイクロサービス間のトラフィックを許可するようにコネクタを構成します。
* C. サーバーレス サービスを既存の VPConfigure ファイアウォール ルールに展開して、サーバーレス サービスと既存のマイクロサービス間のトラフィックを許可します。
* D. サーバーレス VPC アクセス コネクタを作成します。マイクロサービスとの通信にコネクタを使用するようにサーバーレス サービスを構成します。
<details><div><pre>
* A. コストが高い可能性があります。 サーバーレスVPCのデプロイやピアリングには、追加のコストが発生する可能性があります。また、ファイアウォールルールの管理も必要になります。
* B. 柔軟性が高いですが、管理が複雑になる可能性があります。 サーバーレスサービスが増えるほど、コネクタの管理が複雑になります。
* C. 不適切です。 VPConfigureは、VPCの構成設定であり、ファイアウォールルールを直接展開することはできません。
* D. 最も適切な選択肢です。
    * コスト効率: サーバーレスVPCアクセスコネクタは、オンデマンドでスケーリングするため、トラフィックが少ない場合でもコストを抑えることができます。
    * シンプルさ: 1つのコネクタで複数のサーバーレスサービスからマイクロサービスにアクセスできるため、管理が簡単です。
    * 柔軟性: 必要に応じて、コネクタの設定を変更することで、アクセス制御を細かく設定できます。
</pre></div></details>

### 問
dedicated Interconnect 経由でオンプレミス サービスに接続するサービスを Google Cloud 上に構成しました。ユーザーから最近の接続の問題が報告されています。ファイアウォール ルールまたはルーティングの決定によりトラフィックがドロップされているかどうかを判断する必要があります。あなたは何をするべきか？
* A. Network Intelligence Center の接続テストを使用して、VPC とオンプレミス ネットワーク間の接続をテストします。
* B. Network Intelligence Center のネットワーク トポロジを使用してトラフィック フローを確認し、接続の問題が発生した期間のトラフィックを再生します。
* C. VPC フロー ログを構成する。ソースと宛先でフィルタリングしてログを確認します。
* D. Google Cloud で実行されているサービスと同じ VPC 上に Compute Engine インスタンスを構成し、オンプレミス サービスを対象としたtraceroute を実行します。
<details><div><pre>
* A. 部分的に有効: 接続テストは、接続の有無を確認するのに役立ちますが、トラフィックがドロップしている原因を特定するためには、より詳細な情報が必要です。
* B. 最も有効: Network Intelligence Centerは、ネットワークの可視性を高めるための強力なツールです。ネットワークトポロジでトラフィックフローを確認し、問題発生期間のトラフィックを再生することで、ボトルネックや異常なトラフィックパターンを特定できます。
* C. 有効: VPCフローログは、ネットワークトラフィックの詳細な情報を記録します。ソースと宛先でフィルタリングすることで、問題が発生しているトラフィックを特定し、原因を分析できます。
* D. 部分的に有効: tracerouteは、パケットが経路を通過する際のホップ数を表示し、ネットワークの到達可能性を確認するツールです。しかし、ファイアウォールルールやルーティングの問題を特定するためには、より詳細な情報が必要です。
</pre></div></details>

### 問
あなたは、オンプレミス接続を含む、Google Cloud での大規模なアプリケーションのデプロイを計画しています。このアプリケーションでは、アドレス変換を行わずに、すべてのリージョンとオンプレミスの場所のワークロード間で直接接続する必要がありますが、すべての RFC 1918 範囲はすでにオンプレミスの場所で使用されています。あなたは何をするべきか？
* A. VPC ネットワーク ピアリングを使用して、トランジット ネットワークで複数の VPC ネットワークを使用します。
* B. 複数の分離された VPC ネットワークで重複する RFC 1918 範囲を使用します。
* C. 複数の分離された VPC ネットワークとクラウド NAT で重複する RFC 1918 範囲を使用します。
* D. 単一のグローバル VPC で非 RFC 1918 範囲を使用します。
<details><div><pre>
* A. 不適切：
    * メリット:
        * 複数のVPCを接続し、柔軟なネットワーク構成が可能
    * デメリット:
        * トランジットネットワークを経由するため、直接接続ではない
        * 複雑なネットワーク構成になりがち
        * ピアリング設定の管理オーバーヘッドが増加
* B. 不適切：
    * メリット:
        * シンプルな構成
    * デメリット:
        * RFC 1918範囲が重複するため、ルーティングが複雑になる
        * ネットワークトラブルシューティングが困難になる可能性がある
* C. 不適切：
    * メリット:
        * RFC 1918範囲の重複を回避できる
    * デメリット:
        * クラウドNATを経由するため、直接接続ではない
        * パフォーマンス低下やスケーラビリティの制限がある可能性がある
* D. 　適切：
    * メリット:
        * シンプルな構成
        * RFC 1918範囲の重複を回避
        * 直接接続が可能
    * デメリット:
        * 非RFC 1918範囲のIPアドレスは、インターネットルーティング可能であるため、セキュリティリスクが高まる
        * IPアドレスの枯渇リスクがある
</pre></div></details>

### 問
10 Gbps の VLAN アタッチメントを備えた 20 Gbps の D dedicated Interconnect 接続をプロビジョニングしました。あなたは最近、オンプレミス データ センターからの相互接続接続上の受信トラフィックが着実に増加していることに気づきました。エンドユーザーができるだけ早く完全な 20 Gbps スループットを達成できるようにする必要があります。これを実現するには、どの 2 つの方法を使用できますか? (2つお選びください。)
* A. 別のリージョンで 10 Gbps の追加の VLAN アタッチメントを構成します。同じマルチ出口識別子 (MED) を使用してルートをアドバタイズするようにオンプレミス ルーターを構成します。
* B. 同じリージョン内に 10 Gbps の追加の VLAN アタッチメントを構成します。同じマルチ出口識別子 (MED) を使用してルートをアドバタイズするようにオンプレミス ルーターを構成します。
* C. Google Cloud Console から、VLAN アタッチメントの帯域幅を 20 Gbps に変更します。
* D. Google Cloud コンソールから、20 Gbps の新しい Dended Interconnect 接続をリクエストし、10 Gbps の VLAN アタッチメントを構成します。
* E. 20 Gbps の専用相互接続接続を使用するように、オンプレミス ルーターでリンク アグリゲーション コントロール プロトコル (LACP) を構成します。
<details><div><pre>
* A. 不適切：異なるリージョンでは、レイテンシが増加し、パフォーマンスに影響が出る可能性がある。
* B. 　適切：
    * メリット:
        * 2つのVLANアタッチメントで合計20Gbpsの帯域幅を確保できる。
        * MEDを統一することで、ルーティングがシンプルになり、トラフィックの負荷分散が容易になる。
    * デメリット:
        * VLANアタッチメントの追加作業が必要。
        * オンプレミスルーターの設定変更が必要。
* C. 不適切：VLANアタッチメントの帯域幅は、接続時の設定で決定され、後から変更することはできない。
* D. 不適切：コストが増加し、不要なリソースの利用となる。
* E. 　適切：
    * メリット:
        * 2つの10Gbpsリンクを束ねて、1つの20Gbpsリンクとして扱うことができる。
        * 既存のVLANアタッチメントをそのまま利用できる。
    * デメリット:
        * オンプレミスルーターでLACPの設定が必要。
        * LACPの動作が不安定になる可能性がある。
</pre></div></details>

### 問
あなたの会社では、各部門の親フォルダーとサブフォルダーを含むリソース階層を定義しています。各部門は、割り当てられたフォルダ内でそれぞれのプロジェクトと VPC を定義し、Google Cloud ファイアウォール ルールを作成するための適切な権限を持っています。 VPC 間でトラフィックが流れることを許可してはなりません。他の VPC を含むあらゆる送信元からのすべてのトラフィックをブロックし、VPC 内のファイアウォール ルールのみをそれぞれの部門に委任する必要があります。あなたは何をするべきか？
* A. 各VPCにVPCファイアウォールルールを作成し、優先度0であらゆる送信元からのトラフィックをブロックします。
* B. 各VPCにVPCファイアウォールルールを作成し、優先度1000であらゆる送信元からのトラフィックをブロックします。
* C. 部門のフォルダーごとに2つの階層型ファイアウォールポリシーを作成し、それぞれに2つのルールを作成します。1つはそれぞれのVPCに割り当てられたプライベートCIDRからのトラフィックと一致し、許可するアクションを設定する優先度の高いルール、もう1つはトラフィックをブロックする、優先度の低いルールです。他のソース。
* D. 部門のフォルダーごとに2つのルールを含む2つの階層型ファイアウォールポリシーを作成します。1つはそれぞれのVPCに割り当てられたプライベートCIDRからのトラフィックと一致し、アクションをgoto_nextに設定する優先度の高いルール、もう1つはトラフィックをブロックする優先度の低いルールです。他のソース。
<details><div><pre>
* A. 不適切：
デメリット: 優先度0は最も高い優先度であり、以降のルールが適用されません。つまり、全てのトラフィックがブロックされ、部門内の通信も遮断されてしまいます。
* B. 不適切：
デメリット: 優先度1000は低い優先度であり、より高い優先度のルールが存在する場合、そのルールが適用されます。つまり、部門内で許可するルールを後から追加した場合、ブロックルールよりも先に評価され、意図しない通信が許可されてしまう可能性があります。
* C. 不適切：
    * メリット:
        * 各部門のVPC内でのみ通信を許可し、外部からのアクセスをブロックできる。
        * 階層型ポリシーにより、柔軟なルール設定が可能。
    * デメリット:
        * ルール数が多くなり、管理が複雑になる可能性がある。
* D.  適切：
    * メリット:
        * Cと同様、各部門のVPC内でのみ通信を許可し、外部からのアクセスをブロックできる。
        * goto_nextアクションを使用することで、次のルールに処理が移譲され、柔軟なルール設定が可能。
    * デメリット:
        * Cと同様に、ルール数が多くなり、管理が複雑になる可能性がある。
</pre></div></details>

### 問
あなたの会社では、Google Cloud に単一の Virtual Private Cloud (VPC) ネットワークがデプロイされており、オンプレミス接続がすでに確立されています。 Google Kubernetes Engine (GKE) を使用して新しいアプリケーションをデプロイしています。このアプリケーションには、同じ VPC ネットワークおよびオンプレミスの場所からのみアクセスできる必要があります。 GKE コントロール プレーンが、プライベート接続のみを介してオンプレミス サブネットの事前定義されたリストに公開されていることを確認する必要があります。あなたは何をするべきか？
* A. コントロール プレーンのプライベート エンドポイントを使用して GKE 限定公開クラスタを作成します。 Cloud Router で VPC ネットワーキング ピアリングのエクスポート/インポート ルートとカスタム ルート アドバタイズメントを構成します。承認されたネットワークを構成して、必要なオンプレミスのサブネットを指定します。
* B. コントロール プレーンのパブリック エンドポイントを使用して GKE 限定公開クラスタを作成します。 Cloud Router で VPC ネットワーキング ピアリングのエクスポート/インポート ルートとカスタム ルート アドバタイズメントを構成します。
* C. コントロール プレーンのプライベート エンドポイントを使用して GKE 限定公開クラスタを作成します。承認されたネットワークを構成して、必要なオンプレミスのサブネットを指定します。
* D. GKE 公開クラスタを作成します。承認されたネットワークを構成して、必要なオンプレミス のサブネットを指定します。
<details><div><pre>
* A. 最適解:
    * プライベートエンドポイント: コントロールプレーンへのアクセスをプライベートネットワーク内に限定
    * 限定公開クラスタ: クラスタへのアクセスを許可するIPアドレス範囲を制限
    * Cloud Router: VPCピアリングとカスタムルートアドバタイズメントにより、オンプレミスとGKEクラスタ間のルーティングを確立
    * 承認されたネットワーク: アクセスを許可するオンプレミスサブネットを指定
* B. 不適切: パブリックエンドポイントを使用するため、外部からのアクセスを完全に遮断できない。
* C. 部分的に適切: プライベートエンドポイントと限定公開クラスタは適切だが、Cloud Routerによるルーティング設定が不足している。
* D. 不適切: 公開クラスタは、インターネットからのアクセスを許可するため、セキュリティリスクが高い。
</pre></div></details>

### 問
Partner Interconnect 接続をプロビジョニングして、オンプレミス データセンターから Google Cloud への接続を拡張しました。 Cloud Router を構成し、VPC 内のリソースに接続するための VLAN アタッチメントを作成する必要があります。関連する Cloud Router で使用する自律システム番号（ASN）を構成し、VLAN アタッチメントを作成する必要があります。あなたは何をするべきか？
* A. 4 バイトのプライベート ASN 4200000000-4294967294 を使用します。
* B. 2 バイトのプライベート ASN 64512-65535 を使用します。
* C. 公開されている Google ASN 15169 を使用する。
* D. 公開されている Google ASN 16550 を使用します。
<details><div><pre>
* A. 不適切: この範囲はプライベートASNとして広く使用されていますが、Google CloudのPartner Interconnectでは一般的に使用されません。
* B. 不適切: 2バイトのASNは、利用できる範囲が狭く、大規模なネットワーク環境には不向きです。また、Google CloudのPartner Interconnectでは一般的に使用されません。
* C. 不適切: 15169はGoogleの古いASNであり、現在は一般的に使用されていません。
* D. 適切: Google CloudのPartner Interconnectでは、一般的にASN 16550が使用されます。
</pre></div></details>

### 問
ボーダー ゲートウェイ プロトコル (BGP) を介した動的ルーティングを使用して Cloud VPN を構成しました。 VPN トンネル経由で到達可能なネットワークをアドバタイズするためのカスタム ルートを追加しました。ただし、オンプレミス クライアントは依然として VPN トンネル経由でネットワークに到達できません。 Cloud Logging のログを調べて、適切なルーターが VPN トンネル経由でアドバタイズされていることを確認する必要があります。ログを調べるには Cloud Logging でどのフィルタを使用する必要がありますか?
* A. resource.type= “gce_router”
* B. resource.type= “gce_network_region”
* C. resource.type= “vpn_tunnel”
* D. resource.type= “vpn_gateway”
<details><div><pre>
* A. 不適切：Cloud Routerに関するログですが、VPNトンネルの特定のルーティング情報は含みません。
* B. 不適切：ネットワークリージョンのログであり、VPNトンネルのルーティング情報とは関係ありません。
* C. 　適切：
    * vpn_tunnel: このリソースタイプは、Cloud VPNトンネルに関するイベントを記録します。
    * カスタムルートのアドバタイズ: カスタムルートが正しくアドバタイズされると、そのイベントがvpn_tunnelリソースのログに記録されます。
* D. 不適切：VPNゲートウェイに関するログですが、特定のトンネルやルートに関する情報は含みません。
</pre></div></details>

### 問
あなたの会社は、us-east1 リージョンの Virtual Private Cloud (VPC) のプライベート サブネットに 2000 台の仮想マシン (VM) をプロビジョニングしました。ユーザーがインターネット経由でソフトウェア更新プログラムとパッケージをダウンロードできるように、パブリック リポジトリへの TCP 接続が少なくとも 128 になるように各 VM を構成する必要があります。 VM がインターネットへのアウトバウンド NAT を実行できるように、クラウド NAT ゲートウェイを実装する必要があります。すべての VM が同時にパブリック リポジトリに接続し、ソフトウェアの更新プログラムとパッケージをダウンロードできることを確認する必要があります。これを実現するには、どの 2 つの方法を使用できますか? (2つお選びください。)
* A. NAT ゲートウェイを手動割り当てモードで構成し、2 つの NAT IP アドレスを割り当て、VM あたりの最小ポート数を 256 に更新します。
* B. VM ごとに構成されたデフォルトの最小ポート数を 64 に設定して 2 番目のクラウド NAT ゲートウェイを作成します。
* C. デフォルトのクラウド NAT ゲートウェイの NAT プロキシを使用して、単一の NAT IP アドレスを使用して動的にスケーリングします。
* D. デフォルトのクラウド NAT ゲートウェイを使用して、必要な NAT IP アドレスの数に自動的にスケールし、VM あたりの最小ポート数を 128 に更新します。
* E. NAT ゲートウェイを手動割り当てモードで構成し、4 つの NAT IP アドレスを割り当て、VM あたりの最小ポート数を 128 に更新します。
<details><div><pre>
* A. 不適切：
    * デメリット: 2つのNAT IPアドレスでは、2000台のVMが同時に128の接続を確立するには不足する可能性があります。
* B. 不適切：
    * デメリット: VMごとに最小ポート数を設定する必要があり、管理が複雑になります。また、2つのNATゲートウェイを管理するオーバーヘッドが増加します。
* C. 　適切：
    * メリット: NATプロキシを使用することで、単一のNAT IPアドレスで多くの接続を処理できます。
    * デメリット: 非常に多くの接続が発生した場合、パフォーマンスが低下する可能性があります。
* D. 　適切：
    * メリット:
        * 自動スケーリングにより、接続数の変動に柔軟に対応できる。
        * VMあたりの最小ポート数を128に設定することで、要件を満たせる。
    * デメリット:
        * 自動スケーリングにはコストがかかる場合がある。
* E. 不適切：
    * メリット:
        * 手動でNAT IPアドレスを割り当てることで、制御性を高めることができる。
        * VMあたりの最小ポート数を128に設定することで、要件を満たせる。
    * デメリット:
        * NAT IPアドレスの数を事前に予測する必要がある。
        * 将来的に接続数が増加した場合、NAT IPアドレスを追加する必要がある。
</pre></div></details>

### 問
いくつかのコンテナ化されたマイクロサービスを使用して Web アプリケーションを構築しました。これらのマイクロサービスを Cloud Run で実行したいと考えています。また、顧客にとってサービスが低遅延で高可用性であることを確認する必要もあります。あなたは何をするべきか？
* A. Cloud Run サービスを複数のアベイラビリティ ゾーンにデプロイします。グローバル TCP ロード バランサを作成します。 Cloud Run エンドポイントをバックエンド サービスに追加します。
* B. Cloud Run サービスを複数のリージョンにデプロイします。サービスを指すサーバーレス ネットワーク エンドポイント グループ (NEG) を作成します。グローバル HTTPS ロード バランサを作成し、ロード バランサのバックエンド サービスとしてサーバーレス NEG を接続します。
* C. Cloud Run サービスを複数のアベイラビリティ ゾーンにデプロイします。サービスを指すクラウド エンドポイントを作成します。グローバル HTTPS ロードバランサを作成し、クラウド エンドポイントをそのバックエンドに接続します
* D. Cloud Run サービスを複数のリージョンにデプロイします。 Cloud DNS でラウンドロビン A レコードを構成します。
<details><div><pre>
* A. 不適切：
    * デメリット:
        * グローバルロードバランサの非効率性: TCPロードバランサはHTTP/HTTPSの負荷分散に最適化されていません。
        * 地理的冗長性不足: 複数のアベイラビリティゾーンへのデプロイは、リージョン内の障害に対する耐性はあるものの、リージョン全体の障害には対応できません。
* B. 　適切：
    * メリット:
        * 地理的冗長性: 複数のリージョンにデプロイすることで、リージョン全体の障害に対する耐性を高めます。
        * HTTP/HTTPS最適化: グローバルHTTPSロードバランサは、HTTP/HTTPSトラフィックに最適化されており、高いパフォーマンスを提供します。
        * サーバーレスNEG: Cloud Runサービスをシームレスに統合できます。
    * デメリット:
        * 設定の複雑さ: 複数のコンポーネントを組み合わせる必要があるため、設定が複雑になる可能性があります。
* C. 不適切：
    * デメリット:
        * 地理的冗長性不足: Aと同様、リージョン全体の障害には対応できません。
        * クラウドエンドポイントの制限: クラウドエンドポイントは、特定のユースケースに制限される場合があります。
* D. 不適切：
    * デメリット:
        * 低遅延性の保証が難しい: DNSのラウンドロビンは、最寄りのリージョンに必ずしもリクエストを転送するとは限りません。
        * 高可用性: DNSの障害やネットワークの問題が発生した場合、サービスにアクセスできなくなる可能性があります。
</pre></div></details>

### 問
あなたの会社には、us-west1 と us-east1 という 2 つの異なるリージョンに 2 つの Dended Interconnect 接続を備えた Virtual Private Cloud (VPC) があります。各 Dended Interconnect 接続は、VLAN アタッチメントによってそれぞれのリージョンの Cloud Router に接続されます。高可用性フェイルオーバー パスを構成する必要があります。デフォルトでは、オンプレミス環境からのすべての受信トラフィックは、us-west1 接続を使用して VPC に流れる必要があります。 us-west1 が使用できない場合は、トラフィックを us-east1 に再ルーティングする必要があります。このフェイルオーバー パスを有効にするには、Multi-Exit Discriminator (MED) 値をどのように構成すればよいですか?
* A. 地域ルーティングを使用します。 us-east1 Cloud Router の基本優先度 100 に設定し、us-west1 Cloud Router の基本優先度 1 に設定します。
* B. グローバル ルーティングを使用する。 us-east1 Cloud Router の基本優先度 100 に設定し、us-west1 Cloud Router の基本優先度 1 に設定します。
* C. 地域ルーティングを使用する。 us-east1 Cloud Router の基本優先度 1000 に設定し、us-west1 Cloud Router の基本優先度 1 に設定します。
* D. グローバル ルーティングを使用する。 us-east1 Cloud Router の基本優先度 1000 に設定し、us-west1 Cloud Router の基本優先度 1 に設定します。
<details><div><pre>
* A. 不適切：
    * デメリット: 地域ルーティングは、単一のリージョン内のルーティングに特化しており、複数のリージョンにまたがるフェイルオーバーには不適です。
* B. 不適切：
    * デメリット: MED値の設定が逆になっています。優先度が低い方が優先的に選択されるため、us-east1が常に優先されてしまいます。
* C. 不適切：
    * デメリット: Aと同様、地域ルーティングは複数のリージョンにまたがるフェイルオーバーには不適です。
* D. 適切：
    * メリット:
        * グローバルルーティング: 複数のリージョンにまたがるルーティングが可能
        * MED値の設定: us-west1を優先し、障害発生時にus-east1にフェイルオーバー
    * デメリット: なし
</pre></div></details>

### 問
あなたはハイブリッド クラウド環境を設計しています。 Google Cloud 環境は、中央のトランジット ハブ VPC 内の HA VPN と Cloud Router を使用してオンプレミス ネットワークと相互接続されます。 Cloud Router はデフォルト設定で構成されています。オンプレミスの DNS サーバーは 192.168.20.88 にあります。複数のスポーク VPC 内の Compute Engine リソースが、ドメイン corp.altostrat.com を使用してオンプレミスのプライベート ホスト名を解決できると同時に、Google Cloud のホスト名も解決できることを確認する必要があります。 Google が推奨する慣行に従いたいと考えています。あなたは何をするべきか？
* A. 1. Cloud DNS に、192.168.20.88 を指す、corp-altostrat-com という「corp.altostrat.com」のプライベート転送ゾーンを作成します。ゾーンをハブ VPC に関連付けます。
<details><div><pre>
</pre></div></details>
