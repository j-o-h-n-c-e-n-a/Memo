
## 3
### Q. 1
    あなたは、あるイベント中の通話数を推定するための回帰モデルを構築しています。
    あなたは、特徴値がポアソン回帰モデルを構築するための条件を達成しているかどうかを判断する必要があります。
    特徴量セットにはどの2つの条件が含まれていなければならないか？各正解は、ソリューションの一部を提示します。
    注：各正解の選択は1点の価値があります。
1. the label data must be a negative value
2. the label data must be whole numbers
3. the label data must be non-discrete
4. the label data must be a positive value
5. the label data must be positice or negative
<details><div>
    答え：2,4
    ポアソン回帰は、数値（通常はカウント）を予測するために使用される回帰モデルでの使用を意図しています。したがって、予測しようとする値が以下の条件に適合する場合のみ、このモジュールを使用して回帰モデルを作成する必要があります。
    応答変数がポアソン分布である。
    カウントは負であってはならない。負のラベルで使用しようとすると、このメソッドは完全に失敗します。
    ポアソン分布は離散分布です; したがって、整数でない数でこの方法を使用することは意味がありません。
</div></details>

### Q. 2
    あなたは、あるデータセットに対してフィーチャーエンジニアリングを行っています。
    CityName という名前のフィーチャーを追加して、列の値に London というテキストを入力する必要があります。
    あなたは、データセットに新しいフィーチャーを追加する必要があります。
    どのAzure Machine Learning Studioモジュールを使用する必要がありますか？
1. extract n-gram features from text
2. edit metadata
3. preprocess text
4. apply sql transformation
<details><div>
    答え：2
    典型的なメタデータの変更には、カラムをフィーチャーとしてマークすることが含まれるかもしれません。
</div></details>

### Q. 3
    あなたは、ラベル付けされた写真のセットを使用するマルチクラス画像分類の深層学習モデルを持っています。あなたは、モデルを訓練するときにハイパーパラメータ値を選択するために、次のコードを作成します。

    以下の各ステートメントについて、そのステートメントが真である場合はYesを選択します。そうでない場合は、「いいえ」を選択します。
    注：各正解の選択は1点の価値があります。
1. [yes] ハイパーパラメータの組み合わせは、前の実験がどのように行われたかに基づいて選択される。
2. [no]  ハイパーパラメータの組み合わせは、前の実験がどのように行われたかに基づいて選択される。
3. [yes] モデル学習時に学習率0.9が使用される可能性があります。
4. [no]  モデル学習時に学習率0.9が使用される可能性があります。
5. [yes] このハイパーパラメータの早期終了ポリシーを定義することができます。
6. [no]  このハイパーパラメータの早期終了ポリシーを定義することができます。
<details><div>
    答え：1,3,5
    ボックス1: はい
        ハイパーパラメータとは、モデルを学習するために選択する調整可能なパラメータで、学習プロセス自体を支配するものです。Azure Machine Learningでは、効率的な方法でハイパーパラメータの探索を自動化することができ、時間とリソースを大幅に節約することができます。ハイパーパラメータの値の範囲と、トレーニング実行の最大回数を指定します。その後、システムは自動的に異なるパラメータ構成で複数の同時実行を開始し、選択したメトリックで測定して最高のパフォーマンスをもたらす構成を見つけます。パフォーマンスの悪いトレーニングランは自動的に早期終了され、計算機資源の無駄を省きます。これらのリソースは、代わりに他のハイパーパラメータ構成の探索に使用されます。
    ボックス 2: はい
        uniform(low, high) - low と high の間に一様に分布する値を返す。
    ボックス3: No
        ベイズサンプリングは、現在、早期終了ポリシーをサポートしていません。
</div></details>

### Q. 4
    Azure Machine Learningワークスペースで、自動化された機械学習実験を実行します。実行に関する情報は、以下の表に記載されています。
    あなたは、Azure Machine Learning SDKを使用して、実験実行の最良の反復を取得するスクリプトを書く必要があります。
    あなたはどのPythonコードセグメントを使用する必要がありますか？
    オプション:A

    オプション:B
1. A
2. B
<details><div>
    答え：1
    automl_classifierのget_outputメソッドは、最後に起動したときのベストランとフィットしたモデルを返します。get_output のオーバーロードにより、任意のログされたメトリックまたは特定の反復のための最良の実行と適合されたモデルを取得することができます。
</div></details>

### Q. 5
    分類モデルをトレーニングするためのデータを含むカンマ区切り値（CSV）ファイルがあります。
    Azure Machine Learning studio の Automated Machine Learning インターフェイスを使用して、分類モデルを学習します。タスクの種類を分類に設定します。
    自動機械学習プロセスが、線形モデルのみを評価することを確認する必要があります。
    あなたは何をする必要がありますか？
1. 線形以外のすべてのアルゴリズムをブロックされたアルゴリズムに追加する。
2. 終了基準オプションをメトリックスコア閾値に設定する。
3. 自動特徴づけを行うオプションをオフにする。
4. ディープラーニングを有効にするオプションをオフにする
5. タスクの種類を回帰に設定
<details><div>
    答え：1
</div></details>

### Q. 6
    あなたは、銀行で働くデータサイエンティストで、顧客がローンを返済する可能性があるかどうかを予測する機械学習モデルを訓練し登録するために、Azure MLを使用しました。
    あなたは、モデルがどのように選択を行っているかを理解したい。また、モデルが、申請者の居住地に基づく融資の拒否などの政府の規制に違反しないことを確認する必要がある。
    顧客データの各特徴が予測にどの程度影響を及ぼしているかを判断する必要があります。
    どうすればいいのでしょうか？
1. モデルとそのトレーニングデータセットのデータドリフトモニタリングを有効にする。
2. 既知のラベル値を持つテストデータに対してモデルをスコアリングし、その結果を使用して混同行列を計算する。
3. hyperdrive ライブラリを使用して、複数のハイパーパラメータ値でモデルをテストする。
4. interpretability パッケージを使用して、モデルの説明文を生成する。
5. モデル登録に、学習データセットの特徴量の名前を示すタグを追加する。
<details><div>
    答え：4
    モデルの説明を計算して可視化する場合、自動化されたMLモデルに対する既存のモデルの説明に限定されるわけではありません。また、異なるテストデータでモデルの説明を得ることもできます。このセクションのステップでは、テストデータに基づいてエンジンの特徴量の重要度を計算し、視覚化する方法を紹介します。
不正解です。
    A：機械学習の文脈では、データドリフトとは、モデルの性能劣化につながるモデル入力データの変化のことです。このため、データドリフトを監視することで、モデル性能の問題を検出することができます。
    B: 混同行列は、分類モデルの性能を記述するために使用されます。各行にはデータセット内の真のクラス（実際のクラス）のインスタンスが表示され、各列にはモデルによって予測されたクラスのインスタンスが表示されます。
    C: ハイパーパラメーターとは、モデル学習のために選択した調整可能なパラメーターで、学習プロセスをガイドするものです。HyperDriveパッケージは、これらのパラメーターの選択を自動化するのに役立ちます。
</div></details>

### Q. 7
    PyTorch深層学習フレームワークを使用するマルチクラス画像分類深層学習モデルを作成します。
    分類モデルのハイパーパラメータを最適化するために、Azure Machine Learning Hyperdrive を構成する必要があります。
    最高の精度スコアを持つモデルをもたらすハイパーパラメータ値を決定するために、主要なメトリックを定義する必要があります。
    どの3つのアクションを実行する必要がありますか？各正解はソリューションの一部を提示します。
    注：各正解の選択は1点の価値があります。
1. bird_classifier_train.pyスクリプトの実行に使用するestimatorのprimary_metric_goalを最大化するように設定する。
2. bird_classifer_train.py スクリプトに、モデルの評価損を計算するコードを追加し、キーロスと共に float calue としてログに記録する。
3. bird_classifier_train.pyスクリプトの実行に使用する推定値のprimary_metric_goalをminimizeに設定する。
4. bird_classifier_train.pyスクリプトの実行に使用されたestimatorのprimary_metrix_nameをaccuracyに設定する。
5. bird_classifier_train.pyスクリプトを実行するために使用されるestimatorがlossの場合、primary_metrix_nameをセットする。
6. bird_classifer_train.py スクリプトに、モデルの評価精度を計算するコードを追加し、accuracy をキーに float calue としてログに記録します。
<details><div>
    答え：1,4,6
    ADです。
        primary_metric_name="accuracy"。
    プライマリーメトリックゴール(Primary_Metric_Goal.MAXIMIZE)
    accuracy」を最大化するようにランを最適化します。トレーニングスクリプトにこの値を必ず記録してください。
注意してください。
    primary_metric_name: 最適化するプライマリーメトリクスの名前。プライマリーメトリクスの名前は、トレーニングスクリプトで記録されたメトリクスの名前と正確に一致する必要があります。
    primary_metric_goal: PrimaryMetricGoal.MAXIMIZE または PrimaryMetricGoal.MINIMIZE のいずれかであり、ランの評価時にプライマリーメトリックを最大化または最小化するか決定する。
    F: 学習スクリプトはval_accuracyを計算し、"accuracy "としてログに記録し、プライマリメトリクスとして使用する。
</div></details>

### Q. 8
    Azure Machine Learningのワークスペースから実行された実験を取得するコードを記述します。
    実行では、Azure Machine Learning のモデル解釈サポートを使用して、モデルの説明を生成してアップロードしました。
    あなたの組織のビジネスマネージャーは、モデル内の特徴の重要性を見たいと思っています。
    あなたは、次のように見える出力で、モデルの特徴とその相対的な重要性を印刷する必要があります。
    どのようにコードを完成させるべきですか？回答するには、回答欄で適切な選択肢を選んでください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：2
    ボックス 1: from_run_id
        from_run_id(workspace, experiment_name, run_id)
            run ID を指定し、ファクトリーメソッドでクライアントを作成する。
            ExplanationClient のインスタンスを返す。

        パラメータ
            ワークスペース Workspace ワークスペースを表すオブジェクト。
        experiment_name str 実験名．
            run_id str ランを表す GUID。

    ボックス 2: list_model_explanations
        list_model_explanations は、利用可能な全てのモデル説明のメタデータの辞書を返します。
        戻り値
            id、データ型、説明方法、モデル型、アップロード時間などの説明用メタデータを、アップロード時間順に並べた辞書
</div></details>

### Q. 9
    Azure Machine LearningのHyperdrive機能を使って、モデルをトレーニングします。
    次のコードを実行して Hyperdrive 実験を構成します。

    次の各ステートメントについて、そのステートメントが真である場合は [はい] を選択します。そうでない場合は、[いいえ] を選択します。
    注：正しい選択にはそれぞれ 1 ポイントの価値があります。
1. [yes]このようにサンプリングを定義することで、パラメータのすべての可能な組み合わせがテストされます。
2. [no] このようにサンプリングを定義することで、パラメータのすべての可能な組み合わせがテストされます。
3. [yes]learning_rateパラメータのランダムな値は、平均20、標準偏差3の正規分布から選択される。
4. [no] learning_rateパラメータのランダムな値は、平均20、標準偏差3の正規分布から選択される。
5. [yes]keep_probabilityパラメータは常に0.05または0.1のどちらかにする。
6. [no] keep_probabilityパラメータは常に0.05または0.1のどちらかにする。
7. [yes]number_of_hidden_layersパラメータのランダムな値は、平均が3、標準偏差が5の正規分布から選択される。
8. [no]number_of_hidden_layersパラメータのランダムな値は、平均が3、標準偏差が5の正規分布から選択される。
<details><div>
    答え：1,3,6,8
    ボックス 1: はい
        ランダム・サンプリングでは、ハイパーパラメータ値は定義された探索空間からランダムに選択されます。ランダム・サンプリングでは、探索空間に離散的および連続的なハイパーパラメータを含めることができます。
    ボックス2: はい
        learning_rate は、平均値 10、標準偏差 3 の正規分布を持つ。
    ボックス3: No
        keep_probability は一様分布で、最小値は 0.05、最大値は 0.1 である。
    ボックス4: No
        number_of_hidden_layers は [3, 4, 5] のいずれかを取る。
</div></details>

### Q. 10
    あなたは、自動機械学習を用いて回帰モデルを学習することを計画している。データには、欠損値を持つ特徴量と、明確な値がほとんどないカテゴリ特徴量がある。
    あなたは、学習タスクの一部として、自動的に欠損値をインプットし、カテゴリ特徴をエンコードするために自動化された機械学習を設定する必要があります。
    AutoMLConfigクラスでどのパラメータと値のペアを使用する必要がありますか？
1. featurization=auto
2. enable_voting_ensemble=true
3. task=classification
4. exclude_nan_labes=true
5. enable_tf=true
<details><div>
    答え：1
    Featurization str または FeaturizationConfig
    値を指定する。auto' / 'off' / FeaturizationConfig
    特徴づけのステップを自動的に行うかどうか、あるいはカスタマイズされた特徴づけを使用するかどうかの指示子。
    列の種類は自動的に検出される。検出されたカラムタイプに基づき、以下のように前処理/特徴づけが行われる。
        カテゴリー：ターゲットエンコーディング、1つのホットエンコーディング、高カーディナリティカテゴリーの削除、欠損値のインピュートを行います。
        Numeric: 欠損値のインプット、クラスタ距離、証拠の重み付け。
        DateTime: 日、秒、分、時間など、いくつかの特徴。
    テキスト Bag of words, 事前学習済みWord embedding, テキストターゲットエンコーディング。
</div></details>

### 事例紹介. 11
#### 概要
    あなたは、プロスポーツイベントのデータサイエンスを提供する会社のデータサイエンティストです。モデルは、グローバルおよびローカルな市場データを使用して、以下のビジネス目標を達成します。
        スポーツイベントにおけるモバイルデバイスユーザーの感情を、観衆の反応から得た音声に基づいて理解する。
        広告に対するユーザーの反応傾向を把握する。
        モバイル端末に配信される広告のスタイルをカスタマイズする。
        ペナルティイベントの検知に動画を活用
#### 現在の環境
    ペナルティ検知に使用されるメディアは、コンシューマーデバイスから提供されます。メディアには、スポーツイベント中に撮影され、ソーシャルメディア上で共有される画像や動画が含まれることがあります。画像や動画のサイズや形式はさまざまです。
    モデル構築のために利用できるデータは、7年分のスポーツイベントメディアから構成されています。スポーツイベントのメディアには、録画されたビデオやラジオの解説、スポーツイベント中に撮影された関連するソーシャルメディアフィードのログが含まれます。
    群集心理には、イベント参加者から提出されたモノラルとステレオの両方の形式の音声記録が含まれます。
#### ペナルティの検出と感情
    データサイエンティストは、ペナルティイベントの検出のために複数の機械学習モデルを使用して、インテリジェントなソリューションを構築する必要があります。
    データサイエンティストは、機械学習パイプラインの自動特徴エンジニアリングとモデル構築を使って、ローカル環境でノートブックを構築する必要があります。
    ノートブックは、動的ワーカー割り当てのSparkインスタンスを使用して、再トレーニングのためにデプロイされなければなりません。
    ノートブックは、新しいSparkインスタンス上で同じコードで実行し、データのソースのみを再コード化する必要があります。
    グローバルなペナルティ検出モデルは、学習時に動的な実行時グラフ計算を使用して学習する必要があります。
    ローカルペナルティ検出モデルは、BrainScriptを用いて記述すること。
    ローカルな群衆感情モデルの実験では、ローカルなペナルティ検出データを組み合わせる必要があります。
    群集感情モデルは、歓声やキャッチフレーズなどの既知の音を識別しなければならない。個々の群衆感情モデルは、類似した音を検出する。
    ローカルモデルで共有する素性は全て連続変数である。
    共有される特徴量は倍精度を使用しなければならない。後続のレイヤーは、集計された実行平均と標準偏差のメトリクスが利用可能でなければならない。
#### 広告
    最初の数週間の運用で、以下のことが観察されました。
    広告のレスポンス率が低下した。
    広告のスタイルによって、レスポンスの低下が異なる。
    トレーニングデータと本番データの特徴量の分布が一致しない。
    分析によると、ユーザーの位置と行動に関する100個の数値特徴のうち、位置情報ソースに由来する47個の特徴が生の特徴として使用されていることがわかりました。偏りと分散の問題を改善するために提案された実験は、線形的に相関のない10個の特徴を設計することです。
    初期データ発見では、群衆感情モデルに使用される学習データにおいて、対象状態の密度が広範囲に渡っていることが示された。
    すべてのペナルティ検出モデルにおいて、SGD（Stochastic Gradient Descent）を用いた推論フェーズが遅すぎることがわかる。
    音声サンプルでは、キャッチフレーズの長さが地域によって25%～47%変化していることがわかる
    グローバルペナルティ検出モデルの性能は、トレーニングセットと検証セットを比較すると、分散は小さいがバイアスが大きいことがわかります。機能変更を実施する前に、すべてのトレーニングケースと検証ケースを使用して、バイアスと分散を確認する必要があります。
    広告レスポンスモデルは、各イベントの開始時にトレーニングし、スポーツイベント中に適用する必要があります。
    市場セグメンテーションモデルは、類似の広告レスポンス履歴に対して最適化する必要があります。
    サンプリングは、同じ特徴を共有するローカルとグローバルのセグメンテーションモデル間で相互的かつ集団的な排他性を保証しなければならない。
    ローカル・マーケット・セグメンテーション・モデルは、ユーザーの広告反応傾向を判断する前に適用される。
    広告反応モデルは、特徴の非線形境界をサポートする必要がある。
    広告傾向モデルは、カットスレッショルドを0.45とし、重み付きカッパが0.1±5%から逸脱した場合に再トレーニングを行う。
    広告傾向モデルは、下図に示すようなコスト係数を使用する。
    広告傾向モデルには、以下の図に示すようなコスト係数案が用いられています。
    現在のコスト係数と提案されたコスト係数のシナリオのパフォーマンスカーブを以下の図に示します。

### Q. 12
    あなたは、群集心理モデルの評価戦略を定義する必要があります。
    どの3つのアクションを順番に実行する必要がありますか？回答するには、アクションのリストから適切なアクションを回答エリアに移動し、それらを正しい順序で並べます。
1. 
    スーパーサイジングされたモデルの再トレーニングのための新機能の追加
    モデルのエラー率とセントロイドの距離の相関の変化を評価する
    セントロイドの最短距離を用いて再トレーニングのためのラベル付けされたケースをフィルタリングする
2. 
    モデルのエラー率とセントロイドの距離の相関の変化を評価する
    セントロイドの最短距離を用いて再トレーニングのためのラベル付けされたケースをフィルタリングする
    スーパーサイジングされたモデルの再トレーニングのための新機能の追加
3. 
    セントロイドの最短距離を用いて再トレーニングのためのラベル付けされたケースをフィルタリングする
    スーパーサイジングされたモデルの再トレーニングのための新機能の追加
    モデルのエラー率とセントロイドの距離の相関の変化を評価する
<details><div>
    答え：1
シナリオ
    ローカルな群衆感情モデルの実験では、ローカルなペナルティ検出データを組み合わせる必要があります。
    群集感情モデルは、歓声や既知のキャッチフレーズなどの既知の音を識別する必要があります。個々の群衆感情モデルは、類似した音を検出する。
注：モデルエラー率とセントロイド距離の相関の変化を評価する。
    機械学習において、最近接セントロイド分類器または最近接プロトタイプ分類器は、平均（セントロイド）が観察に最も近い学習サンプルのクラスのラベルを観察に割り当てる分類モデルである。
</div></details>

### Q. 13
    群衆感情ローカルモデルのための特徴量エンジニアリング戦略を実装する必要があります。
    どうすればいいのでしょうか？
1. 分散分析(ANOVA)の適用
2. パーソン相関係数の適用
3. スピアマン相関係数の適用
4. 線形判別分析を適用する
<details><div>
    答え：4
    線形判別分析法は、連続変数にのみ作用し、カテゴリー変数や順序変数には作用しない。
    線形判別分析は、変数の平均を比較することによって動作するという点で、分散分析（ANOVA）に似ています。
シナリオ
    データサイエンティストは、機械学習パイプラインの自動特徴エンジニアリングとモデル構築を使用して、ローカル環境でノートブックを構築する必要があります。
    ローカル群衆感情モデルの実験では、ローカルペナルティ検出データを組み合わせる必要があります。
    ローカルモデルのための共有された特徴はすべて連続変数である。
不正解です。
    B：ピアソン相関係数（ピアソンのR検定と呼ばれることもある）は、2つの変数間の線形関係を測定する統計値である。係数の値を調べることで、2つの変数の関係の強さや、正の相関があるか負の相関があるかなどを推測することができます。
    C：スピアマンの相関係数は、ノンパラメトリックかつ正規分布によらないデータで使用するために設計されています。スピアマンの係数は、2つの変数間の統計的依存性のノンパラメトリック尺度であり、ギリシャ文字のrhoで表記されることもある。スピアマン係数は、2つの変数が単調に関係している度合いを表します。また、順序変数に使用できるため、スピアマン順位相関とも呼ばれる。
</div></details>

### 事例紹介. 14
#### 概要
    あなたは、米国の良質な個人・商業用不動産を専門に扱う企業、ファブリカム・レジデンス社のデータサイエンティストです。Fabrikam Residencesはヨーロッパへの進出を検討しており、ヨーロッパの主要都市における個人住宅の価格を調査するよう依頼されました。
    あなたは、Azure Machine Learning Studioを使用して、不動産の中央値を測定します。線形回帰とベイズ線形回帰モジュールを使用して、不動産価格を予測する回帰モデルを作成します。
#### データセット
    ロンドンとパリの2つの都市の不動産詳細を含むCSV形式の2つのデータセットがあります。両方のファイルを別々のデータセットとしてAzure Machine Learning Studioに追加し、実験の出発点とします。両方のデータセットには、次の列が含まれています。
    最初の調査では、MedianValueカラムを除けば、両データセットの構造は同じであることがわかります。小さい方のParisデータセットにはテキスト形式のMedianValueが含まれ、大きい方のLondonデータセットには数値形式のMedianValueが含まれます。
#### データに関する問題
##### 欠落値
    両データセットのAccessibilityToHighwayカラムには欠損値があります。欠損値を埋める前に、データ中の他の変数を用いて条件付きでモデル化するため、欠損データを新しいデータに置き換える必要があります。
    各データセットのカラムには欠損値やヌル値が含まれている。また、データセットには多くの外れ値が含まれている。年齢列は外れ値の割合が高い。Age 列に外れ値がある行を削除する必要があります。MedianValue と AvgRoomsInHouse 列は、両方とも数値形式のデータを保持しています。2つの列の関係をより詳細に分析するために、特徴選択アルゴリズムを選択する必要があります。
##### モデルの適合性
    モデルはオーバーフィットの兆候を示す。オーバーフィッティングを低減する、より洗練された回帰モデルを作成する必要がある。
#### 実験要件
    パフォーマンスを評価するために、線形回帰モジュールとベイズ線形回帰モジュールを交差検証する実験をセットアップする必要があります。それぞれの場合において、データセットの予測変数はMedianValueという名前の列です。パリのデータセットの MedianValue カラムのデータ型が、ロンドンのデータセットの構造と一致することを確認する必要があります。
    結果を予測するためのデータ列の優先順位を決めなければなりません。あなたは、関係を測定するためにノンパラメトリック統計学を使用しなければなりません。
    MedianValue列とAvgRoomsInHouse列の間の関係を分析するために、特徴選択アルゴリズムを使用する必要があります。 
#### モデル学習
##### 並べ換え特徴の重要度
    学習済みモデルとテストデータセットがある場合、特徴変数の Permutation Feature Importance スコアを計算する必要があります。モデルの絶対的な適合度を決定する必要があります。
##### ハイパーパラメータ
    学習フェーズを高速化するために、モデル学習プロセスにおいてハイパーパラメータを設定する必要があります。また、この設定により、各評価区間で最も性能の低い実行をキャンセルし、成功する可能性の高いモデルに労力とリソースを振り向ける必要があります。
    モデルがハイパーパラメータのチューニングで計算資源を効率的に使用できないことを懸念している。また、モデルが全体のチューニング時間を増加させないかもしれないという懸念もあります。したがって、有望なジョブを終了させることなく節約できるような、モデルの早期停止基準を実装する必要があります。
##### クロスバリデーション
    クロスバリデーションのために、3つの等しいパーティションを作成する必要があります。また、テストデータセットとトレーニングデータセットの行が、各都市の主要河川の近くにあるプロパティによって均等に分割されるように、クロスバリデーション処理を構成する必要があります。このタスクは、データがサンプリングプロセスを通過する前に完了する必要があります。
##### 線形回帰モジュール
    線形回帰モジュールをトレーニングする場合、モデルで使用する最適な機能を決定する必要があります。フィーチャー重要性プロセスが完了する前と後のパフォーマンスを測定するために提供されている標準的なメトリックを選択することができます。複数の学習モデルにおける特徴の分布は、一貫していなければなりません。
#### データの可視化
    テスト結果をファブリカム・レジデンス・チームに提供する必要があります。あなたは、結果を提示するのに役立つデータの可視化を作成します。
    モデルの診断テスト評価を行うために、ROC（Receiver Operating Characteristic）曲線を作成する必要があります。2クラス決定森モジュールと2クラス決定ジャングルモジュールを互いに比較するために、Azure Machine Learning StudioでROC曲線を生成するための適切な方法を選択する必要があります。

### Q. 15
    モデルのトレーニング要件に従って、Permutation Feature Importance モジュールを設定する必要があります。
    どのプロパティを選択する必要がありますか？回答するには、回答欄の適切な選択肢を選択してください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：1
    ボックス1：正確さ
        シナリオ モデル学習プロセスにおいてハイパーパラメータを設定し、ハイパーパラメータを使用することで学習フェーズを高速化したいとします。さらに、この構成では、各評価インターバルで最もパフォーマンスの低い実行をキャンセルし、それによって成功する可能性が高いモデルに労力とリソースを振り向ける必要があります。
    ボックス2：R2乗
</div></details>

### Q. 16
    実験要件とデータセットに基づいて、Feature Based Feature Selection モジュールを設定する必要があります。
    モジュールのプロパティはどのように設定すればよいでしょうか。回答するには、回答エリアのダイアログボックスで適切なオプションを選択してください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：1
Box 1: 相互情報量。
    相互情報量スコアは、多次元のデータセットにおいて、結合分布と対象変数の相互情報量を最大化するため、特徴選択において特に有用である。
ボックス2: MedianValue
    MedianValueは特徴列、 、データセットの予測値である。
シナリオ MedianValue と AvgRoomsinHouse 列は、両方とも数値形式のデータを保持する。あなたは、2つの列の間の関係をより詳細に分析するために、特徴選択アルゴリズムを選択する必要がある。
</div></details>

### Q. 17
    特徴抽出の方法を選択する必要があります。
    どの方法を使うべきでしょうか？
1. mutual information
2. moods median test
3. kendall correlation
4. permutation feature importance
<details><div>
    答え：1
    統計学において、Kendall順位相関係数、一般的にKendallのタウ係数（ギリシャ文字のτから）と呼ばれ、2つの測定量間の順序の関連を測定するために使用される統計量である。
    これは、Azure Machine Learning の特徴選択でサポートされている方法である。
注：SpearmanのものもKendallのものも、より一般的な相関係数の特殊なケースとして定式化することができ、このシナリオではどちらも適切である。
    シナリオ MedianValue列とAvgRoomsInHouse列は、両方とも数値形式のデータを保持しています。あなたは、2つの列の間の関係をより詳細に分析するために、特徴選択アルゴリズムを選択する必要があります。
</div></details>

### Q. 18
    モデルのトレーニング要件に合わせて、Permutation Feature Importance モジュールを設定する必要があります。
    どうすればよいでしょうか？回答するには、回答エリアにあるダイアログボックスで適切なオプションを選択してください。
    注：正しい選択にはそれぞれ1点の価値があります。
1. random seed 500
2. random seed 0
3. regression mean absolute error
4. regression r squared
<details><div>
    答え：3
    ボックス1：500
        Random seedには、ランダム化のための種として使用する値を入力します。0（デフォルト）を指定すると、システムクロックに基づいた数値が生成されます。
        seedの値は任意であるが、同じ実験の実行間で再現性を持たせたい場合は、値を指定した方がよい。
        ここでは、調査結果を再現する必要がある。
    ボックス2：平均絶対誤差
        シナリオ 学習済みモデルとテストデータセットがあるとき、特徴変数のPermutation Feature Importance スコアを計算する必要があります。あなたは、モデルの精度を調査し、発見を再現するために、正しいメトリックを選択するために Permutation Feature Importance モジュールをセットアップする必要があります。
    回帰。次のいずれかを選択します。Precision, Recall, Mean Absolute Error , Root Mean Squared Error, Relative Absolute Error, Relative Squared Error, Coefficient of Determination（決定係数）。
</div></details>

### Q. 19
    特徴抽出の方法を選択する必要があります。
    どの方法を使うべきでしょうか？
1. mutual information
2. pearsons correalation
3. spearman correlattion
4. fisher liner discriminant analysis
<details><div>
    答え：1,3
スピアマンの順位相関係数は、2つの変数間の関係が単調な関数を用いてどの程度記述できるかを評価します。
注：スピアマンの相関係数もケンドールの相関係数も、より一般的な相関係数の特殊なケースとして定式化でき、このシナリオではどちらも適切である。
シナリオ MedianValue列とAvgRoomsInHouse列は、両方とも数値形式のデータを保持しています。あなたは、2つの列の間の関係をより詳細に分析するために、特徴選択アルゴリズムを選択する必要がある。
不正解です。
    B: 2つの変数間のスピアマン相関は、それら2つの変数のランク値間のピアソン相関に等しい。ピアソン相関が線形関係を評価するのに対し、スピアマン相関は単調関係（線形かどうかにかかわらず）を評価する。
</div></details>

### Q. 20
    あなたは、ロジスティック回帰アルゴリズムを用いて分類モデルを学習します。
    あなたは、各特徴の重要度を、全体的な相対重要度として、また特定の予測セットに対する局所重要度の尺度として計算することによって、モデルの予測値を説明することができる必要があります。
    必要な全体的および局所的な特徴の重要性値を取得するために使用できる説明器を作成する必要があります。
    解決策 MimicExplainerを作成する。
    解決策は目標を達成しているか？
1. はい
2. いいえ
<details><div>
    答え：2
    代わりにPermutation Feature Importance Explainer (PFI)を使用します。
注1：Mimic Explainerは、ブラックボックスモデルを模倣するためにグローバルサロゲートモデルをトレーニングするという考えに基づいています。グローバルサロゲートモデルは、あらゆるブラックボックスモデルの予測を可能な限り正確に近似するように訓練された、本質的に解釈可能なモデルである。データサイエンティストはサロゲートモデルを解釈して、ブラックボックスモデルに関する結論を導き出すことができる。
注2：並べ換え特徴量重要度説明器（PFI）。Permutation Feature Importanceは、分類モデルや回帰モデルを説明するために使われる手法である。その仕組みは、データセット全体に対して一度に1つの特徴をランダムにシャッフルし、関心のある性能指標がどれだけ変化するかを計算するものです。変化が大きければ大きいほど、その特徴が重要であることを意味する。PFIは、基礎となるモデルの全体的な挙動を説明することができますが、個々の予測は説明できません。
</div></details>

### Q. 21
    あなたは、ロジスティック回帰アルゴリズムを用いて分類モデルを学習します。
    あなたは、各特徴の重要度を、全体的な相対重要度として、また特定の予測セットに対する局所重要度の尺度として計算することによって、モデルの予測値を説明することができる必要があります。
    必要な全体的および局所的な特徴の重要性値を取得するために使用できる説明器を作成する必要があります。
    解決策 TabularExplainerを作成します。
    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：2
    代わりに、Permutation Feature Importance Explainer (PFI)を使用します。
注1:
注2：Permutation Feature Importance Explainer (PFI)。Permutation Feature Importanceは、分類と回帰モデルを説明するために使用されるテクニックです。その仕組みはというと、データセット全体に対して、ランダムに1つずつデータをシャッフルし、興味のある性能指標がどれだけ変化するかを計算するものである。変化が大きければ大きいほど、その特徴が重要であることを意味する。PFIは、基礎となるモデルの全体的な挙動を説明することができますが、個々の予測は説明できません。
</div></details>

### Q. 22
    あなたは、ロジスティック回帰アルゴリズムを用いて分類モデルを学習します。
    あなたは、各特徴の重要度を、全体的な相対重要度として、また特定の予測セットに対する局所重要度の尺度として計算することによって、モデルの予測値を説明することができる必要があります。
    必要な全体的および局所的な特徴の重要性値を取得するために使用できる説明器を作成する必要があります。
    解決策 PFIExplainerを作成する。
    この解決策は目標を達成していますか？
1. はい
2. いいえ
<details><div>
    答え：1
    Permutation Feature Importance Explainer (PFI)。Permutation Feature Importanceは、分類モデルや回帰モデルを説明するために使われる手法です。その仕組みは、データセット全体に対して、一度に1つの特徴をランダムにシャッフルし、興味のある性能指標がどれだけ変化するかを計算するものです。変化が大きければ大きいほど、その特徴が重要であることを意味する。PFIは、基礎となるモデルの全体的な挙動を説明することができますが、個々の予測は説明できません。
</div></details>

### Q. 23
    Azure Machine Learningデザイナーを使用して、トレーニングパイプラインを作成します。モデルを訓練するデータを含むCSVファイルをアップロードします。
    デザイナーを使用して、次のタスクを実行するための手順を含むパイプラインを作成する必要があります。
    pandas filterメソッドを使用して、トレーニングフィーチャーを選択します。
    naive_bayes.GaussianNBアルゴリズムに基づいたモデルを学習する。
    SELECT [Scored Labels] FROM t1 というクエリで、Scored Labels 列のみを返す。
    どのモジュールを使うべきですか？答えは、適切なモジュールを適切な場所にドラッグしてください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：1
Box 1: 2クラス ニューラルネットワーク
    Two-Class Neural Networkは、ニューラルネットワークアルゴリズムを使用してバイナリ分類器を作成します。
    naive_bayes.GaussianNBアルゴリズムに基づいたモデルを学習します。
Box 2: Pythonスクリプトの実行
    pandas filterメソッドを用いて学習特徴を選択します。
Box 3: DataSetのカラムを選択する
    SELECT [Scored Labels] FROM t1 というクエリで、Scored Labels カラムのみを返します。
</div></details>

### Q. 24
    あなたは、測定値からペンギンの種類を予測する機械学習モデルを作成している。あなたは、3種のペンギンの測定値をカンマ区切りで格納したファイルを持っています。
    モデルは、各クラスについて平均化された動作特性曲線下面積のパフォーマンス指標について最適化する必要があります。
    あなたは、Azure Machine Learning studioのAutomated Machine Learningユーザーインターフェイスを使用して実験を実行し、最高のパフォーマンスを発揮するモデルを見つける必要があります。
    あなたはどの5つのアクションを順番に実行する必要がありますか？
1. 
    ペンギンデータのカンマ区切りファイルをアップロードして、新しいデータセットを作成し、選択する。
    分類タスクの種類を選択する
    主要な構成設定をaccuracyに設定する
    実験名、ターゲットカラム、コンピュータターゲットを選択し、自動機械学習の実行を設定する。
    自動機械学習実験を実行し、結果を確認する。
2. 
    分類タスクの種類を選択する
    主要な構成設定をaccuracyに設定する
    実験名、ターゲットカラム、コンピュータターゲットを選択し、自動機械学習の実行を設定する。
    自動機械学習実験を実行し、結果を確認する。
    ペンギンデータのカンマ区切りファイルをアップロードして、新しいデータセットを作成し、選択する。
    分類タスクの種類を選択する
3. 
    主要な構成設定をaccuracyに設定する
    実験名、ターゲットカラム、コンピュータターゲットを選択し、自動機械学習の実行を設定する。
    自動機械学習実験を実行し、結果を確認する。
    ペンギンデータのカンマ区切りファイルをアップロードして、新しいデータセットを作成し、選択する。
<details><div>
    答え：1
Step 1:ペンギンデータのコマンド区切りファイルをアップロードし、新しいデータセットを作成・選択します。
ステップ2: Classificationタスクの種類を選択します。
ステップ3: プライマリー・メトリクスの構成設定を「精度」に設定します。
        選択可能なメトリクスは、選択したタスクの種類によって決まります。
        分類シナリオのプライマリー・メトリクス。
        accuracy、average_precision_score_weighted、norm_macro_recall、precision_score_weightedなどの閾値後のメトリクスは、非常に小さいデータセット、非常に大きなクラスの偏り（クラスの不均衡）、またはメトリックスの期待値が0.0または1.0に非常に近い場合、あまり最適化しない場合があります。このような場合、AUC_weightedは主要なメトリックとしてより良い選択となる可能性があります。
ステップ4：実験名、ターゲットカラム、計算対象を選択し、自動機械学習の実行を設定する。
ステップ5：自動化された機械学習実験を実行し、結果を確認する。
</div></details>

### Q. 25
    Azure Machine Learning Python SDKを使用して、モデルを学習するためのパイプラインを定義します。
    モデルの学習に使用されるデータは、データストア内のフォルダから読み取られます。
    あなたは、フォルダ内のデータが変更されるたびに、パイプラインが自動的に実行されるようにする必要があります。
    あなたは何をする必要がありますか？
1. パイプラインの regenerate_outputs プロパティを true に設定します。
2. schedulerecuttanceオブジェクトを作成し、頻度をautoに設定します。
3. デフォルト値でパイプラインパラメータを作成し、トレーニングデータの保存場所を参照する。
4. パイプラインのスケジュールを作成する datastoereプロパティにデータストアを、path_on_datastoreプロパティにトレーニングデータのあるフォルダを指定します。
<details><div>
    答え：4

</div></details>

### Q. 26
    Azure Machine Learningの実験として、Pythonスクリプトを実行する予定です。
    スクリプトは、フォルダーの階層からファイルを読み込む必要があります。ファイルは、データセット引数としてスクリプトに渡されます。
    データセット引数には、適切なモードを指定する必要があります。
    あなたはどの2つのモードを使用することができますか？各正解は完全な解答を提示します。
    注：各正解は 1 ポイントに相当します。
1. to_pandas_dataframe
2. as_download
3. as_upload
4. as_mount
<details><div>
    答え：2
</div></details>

### Q. 27
    Azure Machine Learningでトレーニング実験を実行するPythonスクリプトを作成します。スクリプトは、Azure Machine Learning SDK for Python を使用します。
    スクリプトによって生成されたログと出力の名前を取得するステートメントを追加する必要があります。
    あなたは、文のためにSDKからPythonクラスオブジェクトを参照する必要があります。
    あなたはどのクラスオブジェクトを使用する必要がありますか？
1. run
2. scriptrunconfig
3. workspace
4. experiment
<details><div>
    答え：1
    ランは、実験の単一の試行を表す。ランは、トライアルの非同期実行を監視し、トライアルのメトリクスを記録し、出力を保存し、トライアルの結果を分析し、トライアルの生成物にアクセスするために使用される。
    runクラスのget_all_logsメソッドは、実行のすべてのログをディレクトリにダウンロードします。
不正解です。
    A：ランは、実験の単一の試行を表します。ランは、トライアルの非同期実行を監視し、トライアルのメトリクスを記録し、出力を保存し、結果を分析し、トライアルによって生成されたアーティファクトにアクセスするために使用されます。
    B: ScriptRunConfigは、スクリプト、計算ターゲット、環境、および任意の分散ジョブ固有の設定を含む、Azure MLでランを提出するために必要な設定情報を一緒にパッケージ化します。
</div></details>

### Q. 28
    Azure Machine Learningで実験としてスクリプトを実行します。
    実験実行を参照するrunという名前のRunオブジェクトがあります。実験の実行中に生成されたログファイルを確認する必要があります。
    あなたは、レビューのためにログファイルをローカルフォルダにダウンロードする必要があります。
    この目標を達成するために、あなたはどの 2 つのコードセグメントを実行することができますか？各正解は完全な解決策を提示します。
    注：各正解は1ポイントに相当します。
1. run.get_details
2. run.get_file_names
3. run.get_metrics
4. run.download_diles
5. run.get_all_logs
<details><div>
    答え：1,5
    run クラス get_all_logs メソッドは、ランのすべてのログをディレクトリにダウンロードします。
    run クラス get_details は、ランの定義、ステータス情報、現在のログファイル、およびその他の詳細を取得します。
不正解です。
    B: ランの get_file_names は、ランと関連して保存されているファイルをリストアップします。
</div></details>

### Q. 29
    あなたは次のようなコードを持っています。このコードは、あるスクリプトを実行するための実験を準備するものである。

    実験はローカルコンピュータでデフォルトの環境を使って実行されなければならない。
    実験を開始し、スクリプトを実行するためのコードを追加する必要がある。
    どのコードセグメントを使うべきだろうか？
1. run=scriot_experiment.start_logging
2. run=run()
3. ws.get_run()
4. run=script_experiment.submit()
<details><div>
    答え：4
    実験クラスの submit メソッドは、実験を投稿し、アクティブに作成された run を返します。
        構文: submit(config, tags=None, **kwargs)
</div></details>

### Q. 30
    パイプラインのステップを定義するには、次のコードを使用します。
        from azureml.core import Workspace, Experiment, Run
        from azureml.pipeline.core import Pipeline
        from azureml.pipeline.steps import PythonScriptStep

        ws = Workspace.from_config()
        . . .

        step1 = PythonScriptStep(name="step1", ...)
        step2 = PythonScriptsStep(name="step2", ...)
        pipeline_steps = [step1, step2] です。

    ステップを実行するためのコードを追加する必要があります。
    この目標を達成するために、どの2つのコードセグメントを使用することができますか？各正解は完全な解決策を提示します。
    注：各正解の選択は1点の価値があります。
1. experiment=
2. run=
3. pipeline=pipeline()
4. pipeline=pipeline()
<details><div>
    答え：3,4
    ステップを定義したら、そのステップの一部または全部を使ってパイプラインを構築します。

* パイプラインを構築する 例
    pipeline1 = Pipeline(workspace=ws, steps=[compare_models])
* パイプラインを実行する
    pipeline_run1 = Experiment(ws, 'Compare_Models_Exp').submit(pipeline1)
</div></details>

### Q. 31
    Azure Databricks ワークスペースと、リンクされた Azure Machine Learning ワークスペースを作成します。
    Azure Machine Learning ワークスペースに、次の Python コードセグメントがあります。
        import mlflow
        import mlflow.azureml
        import azureml.mlflow
        import azureml.core

        from azureml.core import Workspace

            subscription_id = 'subscription_id'
            resourse_group = 'resource_group_name'
            workspace_name = 'workspace_name'
            ws = Workspace.get(name=workspace_name,
                subscription_id=subscription_id,
                resource_group=resource_group)

            experimentName = "/Users/{user_name}/{experiment_folder}/{experiment_name}"
            mlflow.set_experiment(experimentName)
            uri = ws.get_mlflow_tracking_uri()
            mlflow.set_tracking_uri(uri)

    指示 以下の各ステートメントについて、ステートメントが真である場合はYesを選択します。そうでない場合は、「いいえ」を選択してください。
    注：各正解の選択は1点の価値があります。
1. [yes]リソースグループと azure machine learning workspace が作成される。
2. [no] リソースグループと azure machine learning workspace が作成される。
3. [yes]azure datavricks experimentは、azure machine learning workspaceでのみ追跡される。
4. [no] azure datavricks experimentは、azure machine learning workspaceでのみ追跡される。
5. [yes]エポックロス指標を追跡するように設定されています。
6. [no] エポックロス指標を追跡するように設定されています。
<details><div>
    答え：2,3,5
ボックス1：なし
    Workspace.getメソッドは、設定ファイルを使用せずに既存のワークスペースを読み込みます。
    ws = Workspace.get(name="myworkspace",
                サブスクリプションID='<azure-subscription-id>',
                resource_group='myresourcegroup')
ボックス 2: はい
    MLflow Tracking with Azure Machine Learningでは、ローカルでの実行からAzure Machine Learningワークスペースにログされたメトリクスとアーティファクトを保存することができます。
    get_mlflow_tracking_uri()メソッドは、ワークスペース、wsに一意のトラッキングURIアドレスを割り当て、set_tracking_uri()は、そのアドレスにMLflowトラッキングURIを指し示します。
ボックス3: はい
注：Deep Learningにおいて、エポックとは、ニューラルネットワークにおいて、全データセットを1回ずつ前進・後退させることを意味します。
</div></details>

### Q. 32
    Azure Machine Learningワークスペースにモデルを作成し、登録する。
    Azure Machine Learning SDK を使用して、ParallelRunStep を使用して、モデルを使用して入力データをスコアリングするバッチ推論パイプラインを実装する必要があります。パイプラインステップの ParallelRunConfig compute_target 設定に値を指定する必要があります。
    あなたは、計算ターゲットを作成する必要があります。
    どのクラスを使用する必要がありますか？
1. batchcompute
2. adlacoimpute
3. AMLcompute
4. akscompute
<details><div>
    答え：3
    ParallelRunStepに使用する計算ターゲットです。このパラメータは、計算ターゲットオブジェクト、またはワークスペース内の計算ターゲットの文字列名として指定することができます。
    compute_targetは、AmlComputeまたは文字列のターゲットです。
注：Azure Machine Learning Compute（AmlCompute）は、単一ノードまたは複数ノードのコンピュートを簡単に作成できるマネージドコンピュートインフラストラクチャです。コンピュートは、他のユーザーと共有できるリソースとして、ワークスペース・リージョン内に作成されます。
</div></details>

### Q. 33
    あなたは以前、training-datasetというCSVファイルのフォルダに基づいた表形式のデータセットを使って学習させたモデルを展開しました。
    月ごとのCSVファイルを含むフォルダに、モデルによって生成された特徴量と予測されるラベルが蓄積されました。1つはpredictions-datasetという名前で、予測ラベルを含む学習データと完全に一致するスキーマを持ちます。もう1つはfeatures-datasetという名前で、すべての特徴カラムとファイル名に基づくタイムスタンプ列（日、月、年を含む）を持つスキーマを持ちます。
    モデルのトレーニング以降に変化した特徴データの傾向を特定するために、データドリフトモニタを作成する必要があります。これを実現するには、データドリフトモニターに必要なデータセットを定義する必要があります。
    データドリフトモニターを構成するために、どのデータセットを使うべきですか？回答するには、適切なデータセットを正しいデータドリフトモニタのオプションにドラッグします。
    注：正しい選択にはそれぞれ1点ずつ価値があります。
1. baseline datase: training-dataset
2. baseline datase: features-dataset
3. baseline datase: training-dataset
4. baseline datase: predictions-dataset
<details><div>
    答え：1,3
    Box 1: トレーニングデータセット
        ベースラインデータセット - 通常はモデルのトレーニングデータセットである。
    ボックス2: 予測データセット(predictions-dataset)
        ターゲットデータセット - 通常はモデルの入力データ - を、ベースラインデータセットと経時的に比較する。この比較は、ターゲットデータセットにタイムスタンプのカラムが指定されている必要があることを意味します。
        モニターはベースラインデータセットとターゲットデータセットを比較する。
</div></details>

### Q. 34
    Azure Machine Learningの実験として、Pythonスクリプトを実行する予定です。
    このスクリプトには、次のコードが含まれています。

    スクリプトの入力として、ファイルデータセットを指定する必要があります。データセットは、複数の大きな画像ファイルから構成されており、そのソースから直接ストリーミングする必要があります。
    実験用の ScriptRunConfig オブジェクトを定義し、引数として ds データセットを渡すコードを記述する必要があります。
    どのコードセグメントを使うべきだろうか？
1. arguments=[input-data, ds.to_pandas?dataframe()]
2. arguments=[input-data, ds.as_mount]
3. arguments=[data-data, ds]
4. arguments=[input-data, ds.as_download]
<details><div>
    答え：1
    まだデータセットとして登録されていない構造化データがある場合、TabularDatasetを作成し、ローカルまたはリモート実験のトレーニングスクリプトで直接使用します。
    TabularDatasetをpandasのDataFrameにロードするには、以下のようにします。
        df = dataset.to_pandas_dataframe()
注：TabularDatasetは、提供されたファイルまたはファイルのリストをパースすることによって作成された表形式のデータを表します。
</div></details>

### Q. 35
    Azure Databricksのワークスペースを新規に作成します。
    あなたは、以下の画像に示すように、コンピュートクラスタ上で混合負荷で長時間実行するタスクのための新しいクラスタを構成する。
    グラフィックに表示された情報に基づいて、各ステートメントを完了する回答の選択肢を選択します。
    注：各正解の選択は、1ポイントに値する。
1. [yes]各ユーザーのコードは別プロセスとして実行されます。
2. [no] 各ユーザーのコードは別プロセスとして実行されます。
3. [yes]作業者の数はジョブの全期間にわたって固定される
4. [no] 作業者の数はジョブの全期間にわたって固定される
<details><div>
    答え：2,4
ボックス1：いいえ
    Scalaでは、ユーザーコードを別のプロセスで実行することはできません。
ボックス2: いいえ
    オートスケールが有効です。最小2ワーカー、最大8ワーカー。    
</div></details>

### Q. 36
    モデルのトレーニングに使用するPythonコードを含むJupyter Notebookを持っています。
    あなたは、本番展開のためのPythonスクリプトを作成する必要があります。このソリューションは、コードのメンテナンスを最小限に抑える必要があります。
    あなたはどの2つのアクションを実行する必要がありますか？各正解は、ソリューションの一部を提示します。
    注：各正解の選択は1点の価値があります。
1. jupyter notebookのコードを関数にリファクタリングする。
2. 各関数を個別の Python ファイルに保存する。
3. Python スクリプトにメイン関数を定義する。
4. Pythonスクリプトからすべてのコメントと関数を削除します。
<details><div>
    答え：1,3

</div></details>

### Q. 37
    train.py という Python スクリプトを作成し、scripts というフォルダーに保存します。このスクリプトは、scikit-learn フレームワークを使用して、機械学習モデルをトレーニングします。
    このスクリプトは、ローカルのワークステーションで Azure Machine Learning 実験として実行する必要があります。
    train.py スクリプトを実行する実験を開始する Python コードを記述する必要があります。
    あなたはどのようにコードセグメントを完了する必要がありますか？答えるには、回答領域で適切なオプションを選択します。
    オプション:A

    オプション:B
1. A
2. B
<details><div>
    答え：2
ボックス1：source_directory
    source_directory。実行に必要なコードファイルを含むローカルディレクトリ。
ボックス2: スクリプト
    スクリプト。実行するスクリプトの source_directory からの相対的なファイルパス。
ボックス3: 環境
</div></details>

### Q. 38
    機械学習モデルを学習し、登録します。このモデルを使用して、複数のデータファイルから予測を生成するバッチ推論パイプラインを作成します。
    バッチ推論パイプラインは、毎晩実行するようにスケジュールできるサービスとして公開する必要があります。
    あなたは、推論サービスのために適切な計算ターゲットを選択する必要があります。
    どのコンピュート・ターゲットを使うべきですか？
1. azure machine learning compute instance
2. azure machine learning compute cluster
3. azure kubernetes service-based infernece cluster
4. azure container instance compute target
<details><div>
    答え：2
    Batch推論にはAzure Machine Learning compute clustersを使用。サーバーレスコンピュート上でバッチスコアリングを実行。通常のVMと低優先度のVMをサポート。リアルタイム推論をサポートしない。
</div></details>

### Q. 39
    ローカルワークステーション上でAzure Machine Learning SDKを使用して、モデルをトレーニングし、登録します。Python 3.6 と Visual Studio Code がワークステーションにインストールされています。
    Azure Kubernetes Service (AKS) ベースの Web サービスとしてモデルを本番環境にデプロイしようとすると、スコアリング スクリプトでエラーが発生し、デプロイに失敗します。
    サービスを実稼働環境にデプロイする前に、ローカルのワークステーションでサービスをデバッグする必要があります。
    どの4つのアクションを順番に実行する必要がありますか？回答するには、アクションのリストから適切なアクションを回答領域に移動し、それらを正しい順序で並べます。
1. 
    docker をワークステーションにインストールします。
    サービス用に akswebservice のデプロイメント構成を作成し、モデルをそこにデプロイします。
    localwebserviceを作成し、そこにモデルをデプロイします。
    スコアリングスクリプトをデバッグし、必要に応じて修正します。
2. 
    サービス用に akswebservice のデプロイメント構成を作成し、モデルをそこにデプロイします。
    localwebserviceを作成し、そこにモデルをデプロイします。
    スコアリングスクリプトをデバッグし、必要に応じて修正します。
    docker をワークステーションにインストールします。
<details><div>
    答え：1
ステップ1：ワークステーションにDockerをインストールする
    前提条件として、ローカルシステムでDockerが動作していることが必要です。
    計算ノードにdockerfileをビルドまたはダウンロードします。
ステップ2: AksWebserviceのデプロイメント構成を作成し、モデルをデプロイする
    Azure Kubernetes Service にモデルをデプロイするために、必要なコンピュートリソースを記述したデプロイメント設定を作成します。

* dev/test 用に設定されたクラスタにデプロイする場合は、このモデルを処理するのに十分な # コアとメモリを持つクラスタが作成されていることを確認してください。
* このデプロイメント構成を処理するのに十分なコアとメモリで作成されていることを確認してください。メモリは
* 依存関係や AML コンポーネントなどにもメモリが使われることに注意してください。
    deployment_config = AksWebservice.deploy_configuration(cpu_cores = 1, memory_gb = 1)
    service = Model.deploy(ws, "myservice", [model], inference_config, deployment_config, aks_target)
    service.wait_for_deployment(show_output = True)
    print(service.state)を実行します．
    print(service.get_logs())

ステップ3: サービス用の LocalWebservice デプロイメント設定を作成し、モデルをそこにデプロイします。
    ローカルにデプロイするために、コードを修正して LocalWebservice.deploy_configuration() を使い、デプロイメント設定を作成します。そして、Model.deploy()を使ってサービスをデプロイします。
ステップ 4: デバッグを行い、必要に応じてスコアリングスクリプトを修正します。修正するたびに、サービスの reload() メソッドを使用します。
    ローカルテストの間、ロギングを追加したり、発見した問題を解決するために score.py ファイルを更新する必要があるかもしれません。score.pyファイルへの変更をリロードするには、reload()を使用します。例えば、次のコードはサービス用のスクリプトをリロードし、データを送信します。
不正解です。
    AciWebserviceです。デプロイ可能なWebサービスの種類は、ローカルにモデルをデプロイするLocalWebserviceと、Azure Container Instances（ACI）およびAzure Kubernetes Service（AKS）にそれぞれモデルをデプロイするAciWebserviceとAksWebserviceがあります。
</div></details>

### Q. 40
    Azure Machine Learningワークスペースと、Azure DevOps組織を新規に作成します。ワークスペースにモデルを登録し、そのモデルをターゲット環境にデプロイします。
    ワークスペースに登録されたモデルのすべての新しいバージョンは、ターゲット環境に自動的にデプロイされる必要があります。
    あなたは、モデルをデプロイするためにAzure Pipelinesを構成する必要があります。
    あなたはどの4つのアクションを順番に実行する必要がありますか？
1. 
    create an azure devops project
    create a release pipeline
    install the machine extension for azure pipelines
    create a servidce connection
2. 
    create a release pipeline
    install the machine extension for azure pipelines
    create a servidce connection
    create an azure devops project
3. 
    create a release pipeline
    create a servidce connection
    create an azure devops project
    install the machine extension for azure pipelines
<details><div>
    答え：1
</div></details>

### Q. 41
    Azure Machine Learningデザイナーを使用して、トレーニングパイプラインを作成し、実行します。
    パイプラインは、大量のファイルから予測値を推論するために毎晩実行する必要があります。ファイルが格納されるフォルダは、データセットとして定義されます。
    パイプラインは、毎晩の推論実行に使用できるRESTサービスとして公開する必要があります。
    どうすればいいのでしょうか？
1. create a batch ingerence pipeline
2. set the compute target for the pipeline to an inference cluster
3. create a real-time inference pipeline
4. clone the pipeline
<details><div>
    答え：1
    Azure Machine Learning Batch Inferenceは、時間的制約のない大規模な推論ジョブを対象としています。Batch Inferenceは、コスト効率の良い推論コンピュートスケーリングと、非同期アプリケーションの比類ないスループットを提供します。大規模データに対する高スループットの推論、fire-and-forgetに最適化されています。
    pipeline_runによるバッチ推論ジョブの投入や、RESTコールによる公開パイプラインの投入が可能です。
</div></details>

### Q. 42
    機密データを含むデータセットがある。あなたはそのデータセットを使ってモデルを学習する。
    個人のデータを安全かつ非公開にするために、差分プライバシーパラメータを使用する必要があります。
    あなたは、集計結果に対するユーザデータの影響を軽減する必要があります。
    どうすればよいでしょうか？
1. イプシロンパラメータの値を小さくして、データに加わるノイズの量を減らす。
2. イプシロンパラメータの値を大きくすることで、プライバシを減らし、精度を上げる。
3. イプシロンパラメータの値を小さくすると、プライバシーが増加し、精度が低下します。
4. イプシロンパラメータの値を 1 に設定すると、プライバシーの最大値を確保できます。
<details><div>
    答え：3
    差分プライバシーは、ユーザーが不特定多数のレポートを作成し、最終的に機密データを明らかにすることができる可能性から保護しようとするものである。イプシロンと呼ばれる値は、レポートがどの程度ノイズであるか、またはプライベートであるかを測定します。イプシロンはノイズやプライバシーと逆相関の関係にあります。イプシロンが低いほど、データはよりノイズが多い（そしてプライベートである）ことを意味します。
</div></details>

### Q. 43
    あなたは、あるデータセットに対してフィーチャーエンジニアリングを行っています。
    CityName という名前のフィーチャーを追加して、列の値に London というテキストを入力する必要があります。
    あなたは、データセットに新しいフィーチャーを追加する必要があります。
    どのAzure Machine Learning Studioモジュールを使用する必要がありますか？
1. edit metadata
2. filter based feature selection
3. execute python script
4. latent dirichlet allocation
<details><div>
    答え：3
    典型的なメタデータの変更には、カラムをフィーチャーとしてマークすることが含まれるかもしれません。
</div></details>

### Q. 44
    10,000のデータ点と150の特徴からなる正規化された数値特徴セットを含む、多クラス分類タスクのためのデータセットがあります。
    データ点の75パーセントをトレーニングに、25パーセントをテストに使用します。Pythonの機械学習ライブラリscikit-learnを使用しています。Xは特徴セット、Yはクラスラベルを表します。
    以下のPythonデータフレームを作成します。
    あなたは、主成分分析（PCA）法を適用して、トレーニングセットとテストセットの両方で、特徴セットの次元を10の特徴に減らす必要があります。
    あなたは、どのようにコードセグメントを完了する必要がありますか？回答するには、回答欄で適切な選択肢を選択してください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：1
ボックス1：PCA(n_components = 10)
    トレーニングセットとテストセットの両方で、特徴セットの次元を10特徴に減らす必要がある。
例
    from sklearn.decomposition import PCA
    pca = PCA(n_components=2) ;2次元
    principalComponents = pca.fit_transform(x) ;2次元
ボックス2: pca
    fit_transform(X[,y])は、Xを用いてモデルをフィットさせ、Xに対して次元削減を適用します。
ボックス3: transform(x_test)
    transform(X) は X に次元削減を適用する。    
</div></details>

### Q. 45
    以下の数値特徴を含む特徴セットを持っている。X, Y, Z の3つの特徴量を持つ。
    X、Y、Zの特徴のポアソン相関係数（r値）は、次の図のようになる。

    図に示された情報に基づいて、各質問の答えとなる選択肢を選択しなさい。
    注：各正解の選択肢は 1 ポイントに相当します。
1. what is the r-value for the correlation of y to z? 1
2. what is the r-value for the correlation of y to z? 0.859122
3. which type of relationschip exists between z and y the feature set? a positice linear relationship
4. which type of relationschip exists between z and y the feature set? no linear relationship
<details><div>
    答え：2,3

</div></details>

### Q. 46
    scikit-learn Python ライブラリを用いて、x.1 x2 x3 の特徴量のスケーリングを行っているところである。
    元のデータとスケーリングされたデータは、次の画像に示されている。
    図に示された情報に基づいて、各質問に対応する回答の選択肢を選択します。
    注：各正解の選択肢は1ポイントに相当します。
1. 質問：which scaler is used in graph a?, 答え：min max scale
2. 質問：which scaler is used in graph a?, 答え：standard scaler
3. 質問：which scaler is used in graph b?, 答え：min max scale
4. 質問：which scaler is used in graph b?, 答え：nomalizer
4. 質問：which scaler is used in graph c?, 答え：nomalizer
4. 質問：which scaler is used in graph c?, 答え：min max scale
<details><div>
    答え：2,3,5
ボックス1：StandardScaler
    StandardScalerは、データが各フィーチャー内で正規分布していると仮定し、分布が0を中心とし、標準偏差が1になるようにスケーリングする。
例
    すべてのフィーチャーが互いに同じスケールになりました。
ボックス2：最小・最大スケーラー
    分布の歪度は維持され、3つの分布が重なり合うように同じスケールになったことに注意してください。
ボックス3: ノーマライザ
</div></details>

### Q. 47
    Azure Machine Learning Studioを使用して、2つのデータセットが互いに有意に異なるかどうかを判断しています。
    一方のデータセットにおける推定値は、他方のデータセットにおける参照値以上または以下である可能性があります。あなたは、相関の関数として一定のタイプIエラーを持つ分布を生成する必要があります。
    あなたは、分布を作成する必要があります。
    あなたはどのタイプの分布を作成する必要がありますか？
1. unpaired t-test with a two-tail option
2. unpaired t-test with a one-tail option
3. paired t-test with a one-tail option
4. paired t-test with a two-tail option
<details><div>
    答え：4
    片側検定または両側検定を選択します。デフォルトは両側検定です。これは最も一般的なタイプの検定で、期待される分布がゼロを中心に対称である。
例 相関の関数としての対になっていないおよび対になっている2標本のt検定のタイプIエラー。シミュレーションされた乱数は、分散が1の二変量正規分布に由来する。
</div></details>

### Q. 48
    Azure Machine Learning Studioで重回帰モデルを作成しています。
    いくつかの独立変数には高い相関があります。
    あなたは、すべてのデータに対して効果的な特徴エンジニアリングを実施するための適切な方法を選択する必要があります。
    あなたはどの3つのアクションを順番に実行する必要がありますか？回答するには、アクションのリストから適切なアクションを回答エリアに移動し、正しい順序でそれらを配置します。
1. 
    build a counting transform
    test the hypothesis sing t-test
    use the filter based feature selection module
2. 
    use the filter based feature selection module
    build a counting transform
    test the hypothesis sing t-test
3. 
    test the hypothesis sing t-test
    use the filter based feature selection module
    build a counting transform
<details><div>
    答え：2
ステップ1: Filter Based Feature Selection モジュールの使用
    Filter Based Feature Selectionは、データセットの中で最も予測力の高い特徴を特定します。
    このモジュールは、予測力によってランク付けされた最良の特徴列を含むデータセットを出力する。また、特徴の名前と選択されたメトリックからのスコアも出力される。
ステップ2：計数変換の構築
    計数変換は、計数表を特徴量に変える変換を作成し、複数のデータセットに変換を適用できるようにするものです。
ステップ3: t-Testを用いた仮説の検証    
</div></details>

### Q. 49
    ローカルの機械学習パイプラインのパフォーマンスの問題を解決する必要があります。どうすればいいのでしょうか？
1. increase graphic propcessing units(gpu)
2. increase the learning rate
3. increase the learning iterations
4. increase central proccessing units(cpu)
<details><div>
    答え：1
</div></details>

## 4
### Q. 1
    以下のバージョンのモデルを登録します。

    Azure ML Python SDKを使用して、トレーニング実験を実行します。runという変数を使って、実験の実行を参照します。
    run が送信され、完了した後、次のコードを実行します。
    以下の各ステートメントについて、そのステートメントが真である場合は、Yes を選択します。そうでない場合は、「いいえ」を選択します。
    注：正しい選択には 1 点の価値があります。
    1. 
2. 
3. 
4. 
<details><div>
    答え：2,3

</div></details>

### Q. 2
    あなたは、銀行会社がクレジットカード詐欺の可能性があるインスタンスを識別するための分類モデルを作成しています。あなたは、自動化された機械学習を使用してAzure Machine Learningでモデルを作成することを計画しています。
    使用しているトレーニングデータセットは、非常にアンバランスです。
    あなたは、分類モデルを評価する必要があります。
    どの主要なメトリックを使用する必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    AUC_weightedは、分類指標である。
注：AUCは、Receiver Operating Characteristic Curveの下での面積である。重み付けは、各クラスの真のインスタンスの数によって重み付けされた、各クラスのスコアの算術平均です。
不正解です。
    A：normalized_mean_absolute_errorは回帰指標であり、分類指標ではありません。
    C: アンバランスな分類問題へのアプローチを比較するとき、リコール、精度、AUROCなどの精度を超えたメトリックを使用することを検討してください。パラメータ選択またはモデル選択中に最適化する指標を切り替えることで、少数クラスを検出する望ましい性能を提供するのに十分である可能性があります。
    D: normalized_root_mean_squared_error は回帰指標であり、分類指標ではありません。
</div></details>

### 事例紹介. 3
#### 概要
    あなたは、プロスポーツイベントのデータサイエンスを提供する会社のデータサイエンティストです。モデルは、グローバルおよびローカルな市場データを使用して、以下のビジネス目標を達成します。
    スポーツイベントにおけるモバイルデバイスユーザーの感情を、観衆の反応から得た音声に基づいて理解する。
    広告に対するユーザーの反応傾向を把握する。
    モバイル端末に配信される広告のスタイルをカスタマイズする。
    ペナルティイベントの検知に動画を活用
#### 現在の環境
    ペナルティ検知に使用されるメディアは、コンシューマーデバイスから提供されます。メディアには、スポーツイベント中に撮影され、ソーシャルメディア上で共有される画像や動画が含まれることがあります。画像や動画のサイズや形式はさまざまです。
    モデル構築のために利用できるデータは、7年分のスポーツイベントメディアから構成されています。スポーツイベントのメディアには、録画されたビデオやラジオの解説、スポーツイベント中に撮影された関連するソーシャルメディアフィードのログが含まれます。
    群集心理には、イベント参加者から提出されたモノラルとステレオの両方の形式の音声記録が含まれます。
#### ペナルティの検出と感情
    データサイエンティストは、ペナルティイベントの検出のために複数の機械学習モデルを使用して、インテリジェントなソリューションを構築する必要があります。
    データサイエンティストは、機械学習パイプラインの自動特徴エンジニアリングとモデル構築を使って、ローカル環境でノートブックを構築する必要があります。
    ノートブックは、動的ワーカー割り当てのSparkインスタンスを使用して、再トレーニングのためにデプロイされなければなりません。
    ノートブックは、新しいSparkインスタンス上で同じコードで実行し、データのソースのみを再コード化する必要があります。
    グローバルなペナルティ検出モデルは、学習時に動的な実行時グラフ計算を使用して学習する必要があります。
    ローカルペナルティ検出モデルは、BrainScriptを用いて記述すること。
    ローカルな群衆感情モデルの実験では、ローカルなペナルティ検出データを組み合わせる必要があります。
    群集感情モデルは、歓声やキャッチフレーズなどの既知の音を識別しなければならない。個々の群衆感情モデルは、類似した音を検出する。
    ローカルモデルで共有する素性は全て連続変数である。
    共有される特徴量は倍精度を使用しなければならない。後続のレイヤーは、集計された実行平均と標準偏差のメトリクスが利用可能でなければならない。
#### 広告
    最初の数週間の運用で、以下のことが観察されました。
        広告のレスポンス率が低下した。
        広告のスタイルによって、レスポンスの低下が異なる。
        トレーニングデータと本番データの特徴量の分布が一致しない。
    分析によると、ユーザーの位置と行動に関する100個の数値特徴のうち、位置情報ソースに由来する47個の特徴が生の特徴として使用されていることがわかりました。偏りと分散の問題を改善するために提案された実験は、線形的に相関のない10個の特徴を設計することです。
    初期データ発見では、群衆感情モデルに使用される学習データにおいて、対象状態の密度が広範囲に渡っていることが示された。
    すべてのペナルティ検出モデルにおいて、SGD（Stochastic Gradient Descent）を用いた推論フェーズが遅すぎることがわかる。
    音声サンプルでは、キャッチフレーズの長さが地域によって25%～47%変化していることがわかる
    グローバルペナルティ検出モデルの性能は、トレーニングセットと検証セットを比較すると、分散は小さいがバイアスが大きいことがわかります。機能変更を実施する前に、すべてのトレーニングケースと検証ケースを使用して、バイアスと分散を確認する必要があります。
    広告レスポンスモデルは、各イベントの開始時にトレーニングし、スポーツイベント中に適用する必要があります。
    市場セグメンテーションモデルは、類似した広告レスポンス履歴に対して最適化されなければなりません。
    サンプリングは、同じ特徴を共有するローカルとグローバルのセグメンテーションモデル間で相互的かつ集団的な排他性を保証しなければならない。
    ローカル・マーケット・セグメンテーション・モデルは、ユーザーの広告反応傾向を判断する前に適用される。
    広告反応モデルは、特徴の非線形境界をサポートする必要がある。
    広告傾向モデルは、カットスレッショルドを0.45とし、重み付きカッパが0.1±5%から逸脱した場合に再トレーニングを行う。
    広告傾向モデルは、下図に示すようなコスト係数を使用する。

    広告傾向モデルには、以下の図に示すコスト係数案が使用されています。
    現在のコスト係数と提案されたコスト係数のシナリオのパフォーマンスカーブを以下の図に示します。

### Q. 4
    広告レスポンスのモデリング戦略を定義する必要があります。
    どの3つのアクションを順番に実行する必要がありますか？回答するには、アクションのリストから適切なアクションを回答エリアに移動し、正しい順序で並べます。
1. 
2. 
3. 
4. 
<details><div>
    答え：2
ステップ1: K-Meansクラスタリングモデルの実装
ステップ2: クラスターをデシジョンジャングルモデルの特徴として使用する。
    Decision jungleはノンパラメトリックモデルであり、非線形の意思決定境界を表現することができる。
ステップ3：Score Matchboxレコメンダーモデルで生スコアを特徴量として使用する
    推薦システムを作る目的は、システムの「ユーザー」に対して1つ以上の「アイテム」を推薦することである。アイテムとは、映画、レストラン、本、歌などである。ユーザーは、個人、グループ、またはアイテムの嗜好を持つ他のエンティティである。
シナリオ
    広告レスポンスが低下した。
    広告レスポンス・モデルは、各イベントの開始時にトレーニングされ、スポーツ・イベント中に適用される必要があります。
    市場セグメンテーション・モデルは、類似した広告レスポンス履歴に対して最適化されなければなりません。
    広告レスポンス・モデルは、特徴の非線形境界をサポートしなければならない。
</div></details>

### Q. 5
    あなたは、群集心理モデルの評価戦略を定義する必要があります。
    どの3つのアクションを順番に実行する必要がありますか？回答するには、アクションのリストから適切なアクションを回答エリアに移動し、正しい順序でそれらを配置します。
1. 
2. 
3. 
4. 
<details><div>
    答え：1
ステップ1：クロスエントロピー関数の活性化を定義する
    ニューラルネットワークを使って分類や予測を行う場合、ニューラルネットワークの品質を評価するには、通常、分類誤差よりもクロスエントロピー誤差を、平均二乗誤差よりもクロスエントロピー誤差を用いる方がやや良いとされています。
ステップ2：各ターゲット状態に対してコスト関数を追加する。
ステップ3：距離誤差の評価指標を評価する。
</div></details>

### Q. 6
    あなたは、広告に反応するユーザーの傾向を判断するために、モデル開発戦略を実行する必要があります。
    あなたはどの手法を使うべきでしょうか？
1. 
2. 
3. 
4. 
<details><div>
    答え：1
    データ分割は、データセットの行を2つの異なるセットに分割します。
    Azure Machine Learning Studio の Split Data モジュールの Relative Expression Split オプションは、数式を使用してデータセットをトレーニングデータセットとテストデータセットに分割する必要がある場合に便利です。
    相対式分割。このオプションは、数値カラムに条件を適用したい場合に使用します。数値には、日付/時刻フィールド、年齢や金額を含む列、あるいはパーセンテージが含まれることがあります。たとえば、アイテムの価格によってデータセットを分割したり、年齢層によって人々をグループ化したり、カレンダーの日付によってデータを分離したりすることができます。
シナリオ
    広告に反応するユーザーの傾向を決定する前に、ローカル市場のセグメンテーションモデルを適用する。
    学習データと本番データの特徴量の分布が一致していない
</div></details>

### Q. 7
    パフォーマンス曲線に示されているように、広告レスポンスモデルに新しいコスト要因のシナリオを実装する必要があります。
    どの手法を使うべきでしょうか？
1. 
2. 
3. 
4. 
<details><div>
    答え：1
シナリオ
    現行シナリオと提案するコストファクターシナリオのパフォーマンスカーブを下図に示す。
</div></details>

### 事例紹介. 8
#### 概要
    あなたは、アメリカの良質な個人・商業用不動産を専門に扱う企業、Fabrikam Residencesのデータサイエンティストです。Fabrikam Residencesはヨーロッパへの進出を検討しており、ヨーロッパの主要都市における個人住宅の価格を調査するよう依頼されました。
    あなたは、Azure Machine Learning Studioを使用して、不動産の中央値を測定します。線形回帰とベイズ線形回帰モジュールを使用して、不動産価格を予測する回帰モデルを作成します。
#### データセット
    ロンドンとパリの2つの都市の不動産詳細を含むCSV形式の2つのデータセットがあります。両方のファイルを別々のデータセットとしてAzure Machine Learning Studioに追加し、実験の出発点とします。両方のデータセットには、次の列が含まれています。
    最初の調査では、MedianValueカラムを除けば、両データセットの構造は同じであることがわかります。小さい方のParisデータセットにはテキスト形式のMedianValueが含まれ、大きい方のLondonデータセットには数値形式のMedianValueが含まれます。
#### データに関する問題
##### 欠落値
    両データセットのAccessibilityToHighwayカラムには欠損値があります。欠損値を埋める前に、データ中の他の変数を用いて条件付きでモデル化するため、欠損データを新しいデータに置き換える必要があります。
    各データセットのカラムには欠損値やヌル値が含まれている。また、データセットには多くの外れ値が含まれている。年齢列は外れ値の割合が高い。Age 列に外れ値がある行を削除する必要があります。MedianValue と AvgRoomsInHouse 列は、両方とも数値形式のデータを保持しています。2つの列の関係をより詳細に分析するために、特徴選択アルゴリズムを選択する必要があります。
##### モデルの適合性
    モデルはオーバーフィットの兆候を示す。オーバーフィッティングを低減する、より洗練された回帰モデルを作成する必要がある。
#### 実験要件
    パフォーマンスを評価するために、線形回帰モジュールとベイズ線形回帰モジュールを交差検証する実験をセットアップする必要があります。それぞれの場合において、データセットの予測変数はMedianValueという名前の列です。パリのデータセットの MedianValue カラムのデータ型が、ロンドンのデータセットの構造と一致することを確認する必要があります。
    結果を予測するためのデータ列の優先順位を決めなければなりません。あなたは、関係を測定するためにノンパラメトリック統計学を使用しなければなりません。
    MedianValue列とAvgRoomsInHouse列の間の関係を分析するために、特徴選択アルゴリズムを使用する必要があります。
#### モデル学習
##### 並べ換え特徴の重要度
    学習済みモデルとテストデータセットがある場合、特徴変数の Permutation Feature Importance スコアを計算する必要があります。モデルの絶対的な適合度を決定する必要があります。
##### ハイパーパラメータ
    学習フェーズを高速化するために、モデル学習プロセスにおいてハイパーパラメータを設定する必要があります。また、この設定により、各評価区間で最も性能の低い実行をキャンセルし、成功する可能性の高いモデルに労力とリソースを振り向ける必要があります。
    モデルがハイパーパラメータのチューニングで計算資源を効率的に使用できないことを懸念している。また、モデルによって全体のチューニング時間が増加することを懸念しています。したがって、有望なジョブを終了させることなく節約できるような、モデルの早期停止基準を実装する必要があります。
#### テスト
    Azure Machine Learning StudioのPartition and Sampleモジュールを使用して、サンプリングに基づくデータセットの複数のパーティションを作成する必要があります。
#### クロスバリデーション
    クロスバリデーションのために、3つの等しいパーティションを作成する必要があります。また、テストデータセットとトレーニングデータセットの行が、各都市の主要河川に近い物件によって均等に分割されるように、クロスバリデーション処理を構成する必要があります。このタスクは、データがサンプリングプロセスを通過する前に完了する必要があります。
#### 線形回帰モジュール
    線形回帰モジュールをトレーニングする場合、モデルで使用する最適な機能を決定する必要があります。フィーチャー重要性プロセスが完了する前と後のパフォーマンスを測定するために提供されている標準的なメトリックを選択することができます。複数の学習モデルにおける特徴の分布は、一貫していなければなりません。
#### データの可視化
    テスト結果をファブリカム・レジデンス・チームに提供する必要があります。あなたは、結果を提示するのに役立つデータの可視化を作成します。
    モデルの診断テスト評価を行うために、ROC（Receiver Operating Characteristic）曲線を作成する必要があります。2クラス決定林モジュールと2クラス決定ジャングルモジュールを互いに比較するために、Azure Machine Learning StudioでROC曲線を作成するための適切な方法を選択する必要があります。

### Q. 9
    あなたは、モデルトレーニングのための早期停止基準ポリシーを実装する必要があります。
    その解決策を開発するために、どの 3 つのコードセグメントを使うべきですか？回答するには、コードセグメントのリストから適切なコードセグメントを回答エリアに移動し、正しい順番で並べます。
    注意: 回答の選択肢の順番は1つ以上でも正解です。あなたが選択した正しい順序のいずれかが評価されます。
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    有望なジョブを終了させることなく節約できるような、モデルに対する早期停止基準を実装する必要があります。
    切り捨て選択では、評価間隔ごとに、パフォーマンスが最も低いランを所定の割合でキャンセルします。ランは、主要評価基準でのパフォーマンスに基づいて比較され、最も低いX％が終了されます。
例
    from azureml.train.hyperdrive import TruncationSelectionPolicy
    early_termination_policy = TruncationSelectionPolicy(evaluation_interval=1, truncation_percentage=20, delay_evaluation=5)
不正解です。
    Banditは、スラックファクター/スラック量と評価間隔に基づく終了ポリシーです。このポリシーは、最も性能の良い学習実行に対して、主要な測定基準が指定されたスラックファクター/スラック量の範囲内にない実行を早期に終了させるものである。
</div></details>

### Q. 10
    あなたはモデルの適合の問題を修正する必要があります。
    どの 3 つのアクションを順番に実行する必要がありますか? 回答するには、アクションのリストから適切なアクションを回答エリアに移動し、正しい順番に並べます。
1. 
2. 
3. 
4. 
<details><div>
    答え：1
ステップ1：データの補強
シナリオ 
    各データセットの列には、欠損値やヌル値が含まれている。また、データセットには多くの外れ値が含まれている。
ステップ2: ベイズ線形回帰モジュールを追加する。
シナリオ 
    線形回帰モジュールとベイズ線形回帰モジュールを使って、不動産価格を予測する回帰モデルを作成します。
ステップ3: 正則化の重みを設定します。
    正則化は通常、オーバーフィッティングを避けるために使用されます。例えば、L2正則化の重みで、L2正則化の重みとして使用する値を入力します。オーバーフィッティングを避けるために、0以外の値を使用することをお勧めします。
シナリオ
    モデルの適合。モデルはオーバーフィットの兆候を示しています。オーバーフィッティングを低減する、より洗練された回帰モデルを作成する必要があります。
不正解。
    多クラス決定ジャングルモジュール。
    意思決定ジャングルは、意思決定フォレストの最近の拡張である。決定ジャングルは、決定有向非循環グラフ（DAG）のアンサンブルで構成されています。
L-BFGS。
    L-BFGSは、"limited memory Broyden-Fletcher-Goldfarb-Shanno "の略である。これは、wwo-Class Logistic Regressionモジュールにあり、2つの（そして2つだけの）結果を予測するために使用できるロジスティック回帰モデルを作成するために使用されるものである。
</div></details>

### Q. 11
    あなたは、モデルトレーニングの要件に記載されているように、早期停止基準を実装する必要があります。
    その解決策を開発するために、どの 3 つのコードセグメントを使うべきですか？回答するには、コードセグメントのリストから適切なコードセグメントを回答エリアに移動し、正しい順番で並べます。
    注：回答の選択肢の順序は1つ以上が正しいです。あなたが選択した正しい順序のいずれかが単位となります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1
ステップ1： azureml.train.hyperdriveから
ステップ2: TruncationCelectionPolicyのインポート
    Truncation Selectionは、各評価インターバルにおいて、最も低いパフォーマンスのランを所定の割合でキャンセルします。実行は、主要なメトリックのパフォーマンスに基づいて比較され、最も低いX％が終了されます。
シナリオ
     学習フェーズを高速化するために、モデル学習プロセスでハイパーパラメータを設定する必要があります。さらに、この構成では、各評価インターバルで最も低いパフォーマンスのランをキャンセルし、それによって成功する可能性が高いモデルに労力とリソースを向ける必要があります。
ステップ3：early_terminiation_policy = TruncationSelectionPolicy...    
不正解です。
中央値です。
    中央値停止は、実行によって報告された主要なメトリックの実行平均に基づく早期終了ポリシーです。このポリシーは、すべてのトレーニング実行の実行平均を計算し、実行平均の中央値よりもパフォーマンスが悪い実行を終了させます。
Slack
    Banditは、スラックファクター/スラック量と評価間隔に基づく終了ポリシーです。このポリシーは、最も性能の良いトレーニング実行に対して、主要評価指標が指定されたスラックファクター/スラック量の範囲内にない実行を早期に終了させます。
</div></details>

### Q. 12
    Azure Machine Learningのリモート計算機でトレーニング実験を実行しています。
    この実験は、mlflowとazureml-contrib-runパッケージを含むconda環境を使用するように構成されています。
    実験で生成されたメトリクスを追跡するためのロギングパッケージとして、MLflowを使用する必要があります。
    あなたは、実験用のスクリプトを完成させる必要があります。
    どのようにコードを完成させるべきですか？回答するには、回答欄で適切な選択肢を選択してください。
    注：正しい選択にはそれぞれ1点ずつ価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,5,7
ボックス1：mlflowのインポート
    mlflowクラスとWorkspaceクラスをインポートして、MLflowのトラッキングURIにアクセスし、ワークスペースを設定します。
ボックス2：mlflow.start_run()
    set_experiment()でMLflowの実験名を設定し、start_run()でトレーニングを開始します。
ボックス3：mlflow.log_metric(' ...')
    log_metric() で、MLflow のロギング API を起動し、メトリクスのロギングを開始します。
ボックス4：mlflow.end_run()
    実行を終了します。
    run.endRun()を実行します。
</div></details>

### Q. 13
    CPUベースのコンピュートクラスタとAzure Kubernetes Services（AKS）推論クラスタを含むAzure Machine Learningワークスペースを持っています。分類モデルを作成するために使用する予定のデータを含む表形式のデータセットを作成します。
    あなたは、Azure Machine Learningデザイナーを使用して、クライアントアプリケーションが新しいデータを送信し、応答として即時の予測を得ることによって分類モデルを消費することができるWebサービスを作成する必要があります。
    どの3つのアクションを順番に実行する必要がありますか？回答するには、アクションのリストから適切なアクションを回答領域に移動し、それらを正しい順序で並べます。
1. 
2. 
3. 
4. 
<details><div>
    答え：2
ステップ1：コンピュートインスタンスの作成と起動
    Azure Machine Learning designerを使用してモデルをトレーニングし、デプロイするには、トレーニングプロセスを実行し、モデルをテストし、デプロイされたサービスでモデルをホストするための計算機が必要です。
    作成できるコンピュートリソースは4種類あります。
    コンピュートインスタンス。コンピュートインスタンス：データサイエンティストがデータやモデルを扱うための開発用ワークステーション。
    コンピュートクラスタ。実験コードをオンデマンドで処理するためのスケーラブルな仮想マシンのクラスタ。
    論クラスタ: 推論クラスタ。学習したモデルを使用する予測サービスのためのデプロイメントターゲットです。
    アタッチド・コンピュート: 既存のAzureコンピュート・リソースへのリンク。仮想マシンやAzure Databricksクラスタなど、既存のAzureコンピュートリソースへのリンク。
ステップ2：トレーニングパイプラインを作成し、実行する。
    データ変換を使用してデータを準備したら、それを使用して機械学習モデルをトレーニングできます。学習パイプラインの作成と実行
ステップ3：リアルタイム推論パイプラインを作成・実行する
    モデルを学習させるパイプラインを作成・実行した後は、新しいデータに対して同じデータ変換を行い、学習済みのモデルを使ってその特徴量からラベル値を推論（言い換えれば予測）するパイプラインが必要になる。このパイプラインは、アプリケーションで利用できるように公開できる予測サービスの基礎となります。
</div></details>

### Q. 14
    Azure Machine Learningワークスペースを作成し、開発環境をセットアップします。Tensorflowフレームワークを使用し、トレーニングスクリプトを送信するためにエスティメーターを使用して、ディープニューラルネットワーク（DNN）をトレーニングすることを計画しています。
    トレーニング実行のための計算速度を最適化する必要があります。
    あなたは、使用する適切な推定量と適切なトレーニング計算ターゲット構成を選択する必要があります。
    どのような値を使用すればよいのでしょうか？回答するには、回答欄で適切な選択肢を選択してください。
    注：正しい選択肢を1つ選ぶごとに1ポイント加算されます．
1. 
2. 
3. 
4. 
<details><div>
    答え：1,2,3
ボックス1：Tensorflow
    TensorFlowは、TensorFlow実験における学習用の推定器を表しています。
ボックス2：12vCPU、112GBメモリ...、2GPU,...。
    ディープニューラルネットワークにGPUを使用する。
</div></details>

### Q. 15
    ノートブックで Azure Machine Learning SDK を使用し、experiment フォルダ内のスクリプト ファイルを使用して実験を実行します。
    実験は失敗します。
    あなたは失敗した実験をトラブルシュートする必要があります。
    この目標を達成するための2つの可能な方法は何ですか？各正解は完全な解決策を提示します。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,4
get_details_with_logs() を使用して、実行の詳細と実行によって作成されたログを取得します。
Azure Machine Learningの実行を監視し、Azure Machine Learningスタジオでそのログを表示することができます。
不正解です。
    A：run.get_metrics()を使用して、学習済みモデルのメトリックを表示することができます。
    E：get_output()は、ステップの出力をPipelineDataとして取得する。
</div></details>

### Q. 16
    あなたは、ホテル予約サイトの不正取引を予測するための機械学習モデルを開発し、訓練します。
    このサイトへのトラフィックはかなり変化する。このサイトは月曜日と金曜日にアクセスが集中し、それ以外の日はアクセスが大幅に減少する。また、休日もトラフィックが多い日です。
    このモデルは、Azure Machine Learning リアルタイム Web サービスエンドポイントとして、需要に応じて動的にスケールアップおよびスケールダウンできるコンピューティング上に展開する必要があります。
    どのデプロイメントコンピュートオプションを使用する必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：4
    Azure Machine Learningのコンピュートクラスタは、単一ノードまたは複数ノードのコンピートを簡単に作成できるマネージドコンピュートインフラストラクチャです。コンピュートは、ワークスペース内の他のユーザーと共有可能なリソースとして、ワークスペース・リージョン内に作成されます。ジョブが投入されるとコンピュートは自動的にスケールアップし、Azure Virtual Networkに配置することができます。
</div></details>

### Q. 17
    Azure Machine Learning を使用して、モデルをリアルタイムの Web サービスとしてデプロイします。
    あなたは、サービスの開始時にモデルがロードされ、それが受信されると新しいデータをスコアリングするために使用されることを保証するサービスのためのエントリスクリプトを作成する必要があります。
    スクリプトにはどの関数を含めるべきですか？回答するには、適切な関数を正しいアクションにドラッグしてください。各関数は、1回だけ使うことも、2回以上使うことも、全く使わないこともできます。ペイン間でスプリットバーをドラッグしたり、コンテンツを表示するためにスクロールする必要がある場合があります。
    注：正しい選択には1点ずつ価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
ボックス1：init()
    エントリスクリプトには、init()とrun(data)という2つの必須関数があるだけです。これらの関数は、起動時にサービスを初期化し、クライアントから渡されたリクエストデータを使ってモデルを実行するために使われます。スクリプトの残りの部分は、モデルのロードと実行を処理します。
ボックス2: run()
</div></details>

### Q. 18
    あなたは、ホテル予約サイト会社に勤務するデータサイエンティストです。あなたは、Azure Machine Learningサービスを使用して、不正なトランザクションを識別するモデルを訓練します。
    Azure Machine Learning SDK の Model.deploy メソッドを使用して、Azure Machine Learning リアルタイム Web サービスとしてモデルをデプロイする必要があります。デプロイされた Web サービスは、トランザクション データの入力に基づいて、不正行為のリアルタイムの予測を返す必要があります。
    モデルをデプロイするために使用する InferenceConfig クラスの entry_script パラメータとして指定されるスクリプトを作成する必要があります。
    エントリスクリプトは何を行うべきですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：3
    エントリースクリプトは、デプロイされたウェブサービスに送信されたデータを受け取り、それをモデルに渡します。そして、モデルから返されたレスポンスを受け取り、それをクライアントに返します。スクリプトはモデルに固有のものです。スクリプトは、モデルが期待するデータと返すデータを理解する必要があります。
    エントリースクリプトで達成しなければならないことは、以下の2つです。
        モデルのロード (init()と呼ばれる関数を使用)
        入力データに対するモデルの実行 (run() と呼ばれる関数を使用)
</div></details>

### Q. 19
    ある都市の住宅販売データを含むデータセットがある。このデータセットには、以下のカラムが含まれています。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
</div></details>

### Q. 20
    あなたは、あるワイナリーでデータサイエンティストとして採用されました。前任のデータサイエンティストは、Azure Machine Learningを使用していました。
    あなたは、モデルをレビューし、各モデルがどのように意思決定を行うかを説明する必要があります。
    あなたはどの説明モジュールを使用する必要がありますか？回答するには、解答欄で適切な選択肢を選択してください。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,5
メタ説明器は、与えられたモデルやデータセットに基づき、適切な直接説明器を自動的に選択し、最適な説明情報を生成します。メタ説明器は、弊社が統合・開発した全てのライブラリ（SHAP、LIME、Mimicなど）を活用します。以下は、SDKで利用可能なメタ説明器です。
表形式説明器。表形式のデータセットで使用されます。
テキスト説明ツール。テキストデータセットで使用します。
画像説明ツール。画像データセットで使用されます。
ボックス1：表形式
ボックス2：テキスト
ボックス3: イメージ
不正解。
    階層的注意ネットワーク（HAN: Hierarchical Attention Network）
    HAN は、2016 年に Yang らによって提案された。既存の文書分類のアプローチと異なるHANの主な特徴は、（1）テキストデータの階層性を利用していること、（2）注意のメカニズムを文書分類に適応していることである。
</div></details>

### Q. 21
    Azure Machine Learningで機械学習モデルをトレーニングしています。あなたはハイパーパラメータをチューニングするためにHyperdriveを使用しています。
    以前のモデルトレーニングおよびチューニングの実行では、多くのモデルが同様のパフォーマンスを示しました。
    あなたは、次の要件を満たす早期終了ポリシーを選択する必要があります。
    現在の実行を評価する際に、以前のすべての実行のパフォーマンスを考慮する。
    現在の実行を、これまでで最も性能の良い実行と比較することを避ける。
    どの2つの早期終了ポリシーを使うべきですか？各正解は、解答の一部を示しています。
    注：各正解は1ポイントに相当します。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3
中央値停止ポリシーは、すべての実行の実行平均を計算し、実行平均の中央値よりもパフォーマンスが悪い実行はキャンセルされます。
ポリシーが指定されていない場合、ハイパーパラメータチューニングサービスはすべてのトレーニング実行を完了まで実行させます。
不正解です。
    B: BanditPolicyは、スラック基準、および評価のための頻度と遅延間隔に基づいて早期終了ポリシーを定義します。
        バンディットポリシーは、次の構成パラメータを取ります。
        slack_factor。最も性能の良いトレーニング実行に対して許容されるスラックの量。このファクターは，スラックを比率で指定する．
    D：Truncation選択ポリシーは、主要なメトリックのパフォーマンスが最も低いランの与えられたパーセンテージを定期的にキャンセルする。このポリシーは、トレーニング時間によるモデル性能の向上を考慮することで、ランのランキングの公平性を図ります。比較的若いランをランク付けする場合、ポリシーは古いランの対応する（そして以前の）性能を比較のために使用します。そのため、他のランよりも実行時間が短いからと言って、性能が低いと判断してランを終了させることはありません。
</div></details>

### Q. 22
    Azue Machine LearningのHyperdrive機能を使って、モデルのトレーニング時に最適なハイパーパラメータ値を決定する予定です。
    Hyperdriveを使用して、以下のハイパーパラメータ値の組み合わせを試す必要があります。早期終了ポリシーは適用しないでください。
        learning_rate: 0.001 から 0.1 の間の任意の値
        batch_size: 16、32、または64
    ハイパードライブ実験のサンプリング方法を設定する必要があります。
    あなたはどの2つのサンプリング方法を使用することができますか？各正解は完全な解答である。
    注：各正解は 1 ポイントに相当します。
1. 
2. 
3. 
4. 
<details><div>
    答え：3,4
    C: ベイズサンプリングは、ベイズ最適化アルゴリズムに基づき、次にサンプリングするハイパーパラメータ値をインテリジェントに選択します。新しいサンプルが報告された主要な指標を改善するように、前のサンプルがどのように実行されたかに基づいてサンプルを選択する。
    ベイズサンプリングは、早期終了ポリシーをサポートしていません。
    D：ランダムサンプリングでは、ハイパーパラメータ値は定義された探索空間からランダムに選択されます。ランダムサンプリングでは、探索空間が離散的および連続的なハイパーパラメータの両方を含むことができます。
不正解です。
    B：グリッドサンプリングは、ハイパーパラメータ空間が離散値の間の選択として定義でき、定義された探索空間のすべての値を網羅的に探索するのに十分な予算がある場合に使用することができます。さらに、性能の悪い実行を自動的に早期終了させることができるので、リソースの無駄を省くことができる。
</div></details>

### Q. 23
    Azure Machine Learning SDKを使用して、分類モデルを訓練し、その精度メトリックを計算する訓練実験を実行します。
    モデルは、新しいデータが利用可能になるにつれて、毎月再トレーニングされます。
    バッチ推論パイプラインで使用するために、モデルを登録する必要があります。
    モデルを登録し、その後の再トレーニング実験によって作成されたモデルの精度が、現在登録されているモデルよりも高い場合にのみ、そのモデルが登録されるようにする必要があります。
    この目標を達成するために可能な2つの方法は何ですか？各正解は完全な解答を提示する。
    注：各正解は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：3,5
    E: タグを使用すると、モデルの学習に使用した機械学習ライブラリの名前やバージョンなど、有用な情報を追跡することができます。タグは英数字でなければならないことに注意してください。
</div></details>

### Q. 24
    あなたは、AutoMLConfig クラスを使用して、最大 10 回のモデル学習反復を伴う自動機械学習タスクを定義する実験を実行します。このタスクは、accuracy という名前のメトリックに基づいて、最も性能の良いモデルを見つけようとするものである。
    あなたは、次のコードで実験を提出します。
    あなたは、自動化された機械学習タスクによって生成された最良のモデルを返す Python コードを作成する必要があります。
    あなたはどのコードセグメントを使うべきですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：4
    get_outputメソッドは、最適なランと適合したモデルを返します。
</div></details>

### Q. 25
    あなたは、決定木アルゴリズムを使用して分類モデルを学習します。
    以下のPythonコードを実行し，推定器を作成する．変数 feature_names は全ての特徴量のリスト、class_names は全てのクラス名のリストです。
        from interpret.ext.blackbox import TabularExplainer
    あなたは、すべての特徴量の重要性を決定することによって、すべてのクラスに対してモデルによってなされた予測を説明する必要があります。
    以下の各記述について、その記述が真であれば、Yes を選択します。そうでなければ、No を選択します。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,6
ボックス1：はい
    TabularExplainer は、下にある 3 つの SHAP 説明器 (TreeExplainer, DeepExplainer, または KernelExplainer) のいずれかを呼び出します。
ボックス2: はい
    説明と可視化をより有益にするために、分類を行う場合は特徴名と出力クラス名を渡すことを選択できます。
ボックス3: いいえ
    TabularExplainerは、ユースケースに最も適したものを自動的に選択するが、その下にある3つの説明器（TreeExplainer、DeepExplainer、KernelExplainer）のそれぞれを直接呼び出すことも可能である。
</div></details>

### Q. 26
    あなたは、学生の教育期間、学位タイプ、および芸術形式という変数に応じて、学生の芸術作品の価格を予測するモデルを作成しています。
    あなたは、線形回帰モデルを作成することから始めます。
    あなたは、この線形回帰モデルを評価する必要があります。
    解決策 次の測定基準を使用します。平均絶対誤差，ルート平均絶対誤差，相対絶対誤差，相対2乗誤差，および決定係数．
    解答は目標を達成しているか？
1. はい
2. いいえ
<details><div>
    答え：1
    回帰モデルを評価するために、以下のメトリックが報告されます。モデルを比較する場合、評価のために選択したメトリックによってランク付けされます。
    平均絶対誤差(MAE)は、予測が実際の結果にどれだけ近いかを測定します。したがって、スコアが低いほど優れています。
    二乗平均誤差(RMSE)は、モデルの誤差を要約した1つの値を作成します。差を二乗することで、この指標は過大予測と過小予測の差を無視します。
    相対絶対誤差（RAE）は、予測値と実測値の相対的な絶対差で、差の平均値を算術平均で割るため相対的な値です。
    相対二乗誤差（RSE）は、同様に予測値の二乗誤差の合計を実測値の二乗誤差の合計で割って正規化したものです。
    平均ゼロ一誤差（MZOE）は、予測が正しかったか否かを示します。言い換えれば ZeroOneLoss(x,y) = x!=y のとき 1、それ以外のとき 0。
    決定係数（しばしばR2と呼ばれる）は，0と1の間の値としてモデルの予測力を表す．0はモデルがランダムである（何も説明しない）ことを意味し，1は完全な適合があることを意味する．しかし、R2値の解釈には注意が必要で、低い値は完全に正常であり、高い値は疑わしい場合があるからです。
    AUC。
</div></details>

### Q. 27
    あなたは、学生の教育期間、学位タイプ、および芸術形式という変数に応じて、学生の芸術作品の価格を予測するモデルを作成しています。
    あなたは、線形回帰モデルを作成することから始めます。
    あなたは、この線形回帰モデルを評価する必要があります。
    解決策 次の測定基準を使用する。精度、プレシジョン、リコール、F1スコア、およびAUC。
    解答は目標を達成しているか？
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    これらは分類モデルを評価するための指標であり、代わりに使用します。平均絶対誤差、ルート平均絶対誤差、相対絶対誤差、相対2乗誤差、決定係数。
</div></details>

### Q. 28
    あなたは、学生の教育期間、学位タイプ、および芸術形式という変数に応じて、学生の芸術作品の価格を予測するモデルを作成しています。
    あなたは、線形回帰モデルを作成することから始めます。
    あなたは、この線形回帰モデルを評価する必要があります。
    解決策 次の測定基準を使用します。相対2乗誤差，決定係数，精度，プレシジョン，リコール，F1スコア，およびAUC．
    解答は目標を達成しているか？
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    相対二乗誤差、決定係数などは線形回帰モデルを評価するのに適した指標ですが、その他は分類モデル用の指標です。
</div></details>

### Q. 29
    あなたは、線形回帰モデルを作成するデータサイエンティストです。
    あなたは、データが回帰線にどれだけ近いかを判断する必要があります。
    あなたはどの指標をレビューすべきですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    決定係数（R2）は、モデルの予測力を0と1の間の値で表し、0はモデルがランダムであること（何も説明しない）、1は完全に適合していることを意味する。しかし、R2 値の解釈には注意が必要です。低い値は完全に正常であり、高い値は疑わしい可能性があるからです。
不正解。
    A: 平均二乗誤差（RMSE）は、モデルの誤差を要約した1つの値を作成します。差を二乗することで、この指標は過剰予測と過小予測の差を無視する。
    C: Recallは、モデルによって返されたすべての正しい結果の割合である。
    D: Precision（精度）は、すべての肯定的な結果に対する真の結果の割合である。
    E: 平均絶対誤差(MAE)は、予測が実際の結果にどれだけ近いかを測定するもので、したがって、スコアが低ければ低いほどよい。
</div></details>

### Q. 30
    あなたは、2クラスロジスティック回帰モデルを使用して、バイナリ分類を作成しています。
    あなたは、アンバランスのためにモデル結果を評価する必要があります。
    あなたはどの評価指標を使うべきですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：2
真陽性率と偽陽性率の関係は，ROC（Receiver Operating Characteristic）曲線とそれに対応するAUC（Area Under the Curve）値で見ることができます．この曲線が左上隅に近いほど，分類器の性能が優れていることを意味します（つまり，偽陽性率を最小にしながら真陽性率を最大にすることです）．プロットの対角線に近い曲線は，ランダムな推測に近い予測をする傾向のある分類器から生じます．
</div></details>

### Q. 31
    Azure Machine Learning Studioで線形回帰モデルを開発しています。あなたは、異なるアルゴリズムを比較するために実験を実行します。
    次の画像は、結果のデータセット出力を表示します。

    画像に表示された情報に基づいて、各質問の答えとなる選択肢を選択します。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
ボックス1：ブーストされた決定木回帰
    平均絶対誤差(MAE)は，予測が実際の結果にどれだけ近いかを測定する．
ボックス2:
    オンライン勾配降下法。アルゴリズムに最適なパラメータを探させたい場合は、Create trainer modeオプションをParameter Rangeに設定します。そうすれば、アルゴリズムが試すべき複数の値を指定することができます。
</div></details>

### Q. 32
    あなたは決定木アルゴリズムを使用しています。あなたは、木の深さが10でうまく汎化するモデルを訓練しました。
    あなたは、木の深さを変化させたモデルのバイアスと分散のプロパティを選択する必要があります。
    各樹木の深さに対して、どの特性を選択する必要がありますか？回答するには、回答欄で適切なオプションを選択してください。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3
決定木では、木の深さが分散を決定する。複雑な決定木（例えば深い）は、バイアスが低く、分散が高い。
注：統計学や機械学習において、バイアスと分散のトレードオフとは、予測モデルの集合の特性であり、それによってパラメータ推定のバイアスが低いモデルは、サンプル間のパラメータ推定値の分散が高くなり、その逆もまた然りである。バイアスを増加させると、分散が減少します。分散を増加させるとバイアスが減少する。
</div></details>

### Q. 33
    トレーニング誤差と検証誤差の値が大きく異なるモデルがあります。
    新しいモデルを作成し、クロスバリデーションを実行する必要があります。
    Azure Machine Learning Studioを使用して、新しいモデルのパラメータセットを特定する必要があります。
    各ステップでどのモジュールを使用する必要がありますか？答えるために、適切なモジュールを正しいステップにドラッグします。各モジュールは、1回または複数回使用することができ、全く使用しないこともできます。ペイン間の分割バーをドラッグしたり、コンテンツを表示するためにスクロールする必要がある場合があります。
    注：正しい選択には1点ずつ価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,6,7

1. 2クラス・ブースト決定木
2. パーティションとサンプル
3. モデルのハイパーパラメータを調整する
4. モデルのハイパーパラメータを調整する
</div></details>

### Q. 34
    C-Support Vector分類を使用して、アンバランスな学習データセットでマルチクラス分類を行う場合です。C-Support Vectorによる分類をPythonのコードで表すと以下のようになります。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
ボックス1：入力データのクラス頻度に反比例した重みの自動調整
    balanced" モードでは，y の値を用いて，n_samples / (n_classes * np.bincount(y)) として入力データのクラス頻度に反比例するように重みを自動的に調整します．
ボックス 2: ペナルティパラメータ
    パラメータ C : float, オプション (default=1.0)
    誤差項のペナルティ・パラメータ C。
</div></details>

### Q. 35
    あなたは、英語のテキストコンテンツをフランス語のテキストコンテンツに翻訳するための機械学習モデルを構築している。
    あなたは、テキストコンテンツの順序を学習するために、機械学習モデルを構築し、訓練する必要があります。
    どのタイプのニューラルネットワークを使うべきですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：3
    英語テキストのコーパスをフランス語に翻訳するために、リカレントニューラルネットワーク（RNN）を構築する必要がある。
注：RNNは、入力としてテキストのシーケンスを取るか、出力としてテキストのシーケンスを返すか、またはその両方を行うように設計されている。ネットワークの隠れ層は、各時間ステップの出力とセルの状態が次の時間ステップの入力となるループを持つため、再帰と呼ばれる。このリカレントは一種の記憶として機能する。これにより、文脈情報がネットワークに流れ、前の時間ステップの関連出力を現在の時間ステップのネットワーク操作に適用することができる。
</div></details>

### Q. 36
    あなたは、2値分類モデルを作成します。
    あなたはモデルの性能を評価する必要があります．
    あなたはどの2つの測定基準を使うことができますか？各正解は完全な解決策を提示します。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
バイナリ分類モデルで利用可能な評価指標は以下の通りである。Accuracy, Precision, Recall, F1 Score, and AUC.
注意: 非常に自然な疑問は，「モデルに該当する個体のうち，いくつが正しく分類されたのか（TP）」である．
この質問は，正しく分類された陽性の割合であるモデルの精度を見ることによって答えることができる．    
</div></details>

### Q. 37
    Azure Machine Learning Studio の Two-Class Neural Network モジュールを使用して、バイナリ分類モデルを構築します。Tune Model Hyperparameters モジュールを使用して、モデルの精度をチューニングします。
    Tune Model Hyperparameters モジュールを構成する必要があります。
    どの2つの値を使用する必要がありますか？各正解は、解答の一部を示しています。
    注意: 各正解は 1 ポイントに相当します。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,4
Tune Model Hyperparametersモジュールは、Hidden Layer Specification（オプションE、間違って正しいとしてマークされている）の異なる値を試すことができません。Two-Class Neural Networkを追加していますが、この場合の正解は。B. 学習速度 D. 学習反復の回数
</div></details>

### Q. 38
    次のように定義された6つのデータポイントを含むPython NumPy配列を評価しています。
        データ = [10, 20, 30, 40, 50, 60].
    Python Scikit-learn機械学習ライブラリのk-foldアルゴリズムの移植を使用して、次の出力を生成する必要があります。
        train: [10 40 50 60], test: [20 30]
        train: [20 30 40 60], test: [10 50]
        train: [10 20 30 50]、test: [40 60]
    あなたは、出力を生成するためにクロスバリデーションを実装する必要があります。
    どのようにコードセグメントを完成させるべきでしょうか？回答するには、回答領域のダイアログボックスで適切なコードセグメントを選択してください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：1
</div></details>

### Q. 39
    Azure Machine Learning Studioを使用して、バイナリ分類モデルを作成します。
    モデルのパラメータスイープを実行し、ハイパーパラメータを調整する必要があります。パラメータスイープは、以下の要件を満たす必要があります。
    ハイパーパラメータの可能なすべての組み合わせを反復する。
    掃引の実行に必要なコンピューティングリソースを最小化すること。
    モデルのパラメータ掃引を実行する必要があります。
    どのパラメータ掃引モードを使用すべきですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：4
    ランダムグリッドでの最大実行回数。このオプションはパラメータ値のランダムサンプリングに対する反復回数を制御しますが、値は指定した範囲からランダムに生成されるのではなく、パラメータ値のすべての可能な組み合わせの行列が作成され、その行列に対してランダムサンプリングが行われます。この方法は、より効率的で、地域的なオーバーサンプリングやアンダーサンプリングが発生しにくい方法です。
    統合されたパラメータスイープをサポートするモデルをトレーニングする場合、使用するシード値の範囲を設定し、ランダムシードを反復することも可能です。これはオプションですが、種子の選択によって生じるバイアスを回避するのに有効です。
不正解です。
    B: クラスタリングモデルを構築する場合、Sweep Clusteringを使用して、最適なクラスタ数とその他のパラメータを自動的に決定します。
    C: グリッド全体。このオプションを選択すると、モジュールはシステムによって事前に定義されたグリッド上でループし、さまざまな組み合わせを試し、最適な学習者を特定します。このオプションは、最適なパラメータ設定が何かわからず、可能な限りの値の組み合わせを試したい場合に有効です。
</div></details>

### Q. 40
    あなたは、2値分類を実行するためにリカレントニューラルネットワークを構築しています。
    あなたは、各トレーニングエポックのトレーニングロス、検証ロス、トレーニング精度、検証精度を確認します。
    あなたは、モデルの性能を分析する必要があります。
    あなたは、分類モデルが過剰適合しているかどうかを識別する必要があります。
    次のうち正しいものはどれですか？
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    オーバーフィットモデルとは、学習セットでの性能が良好で、かつ向上し続けるのに対し、検証セットでの性能がある時点まで向上し、その後低下し始めるモデルのことである。   
</div></details>

### Q. 41
    あなたは、統計分布の非対称性を分析しています。
    次の画像には、2つのデータセットの確率分布を示す2つの密度曲線が含まれています。

    図に示された情報に基づいて、各質問に対する答えの選択肢を選びなさい。
    注：正しい選択にはそれぞれ1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,4
ボックス1：正偏差値
    正のスキュー値は、分布が右側に偏っていることを意味する。
ボックス2: 負のスキュー
    負の歪度の値は、分布が左に偏っていることを意味します。    
</div></details>

### Q. 42
    あなたは、K-means アルゴリズムを使用してクラスタリングを実行しています。
    あなたは可能な終了条件を定義する必要がある。
    あなたはどの3つの条件を使うことができますか？各正解は完全な解決策を提示します。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,4
AD: セントロイドが安定したとき，あるいは指定された回数の反復が終了したときに，このアルゴリズムは終了する．
C: セントロイドがクラスタのメンバーをどれだけよく表しているかの指標は，残差平方和（RSS）であり，すべてのベクトルにわたって合計されたセントロイドからの各ベクトルの2乗距離である．RSSは目的関数であり，我々の目標はそれを最小化することである．
</div></details>

### Q. 43
    あなたは、画像分類のための深い畳み込みニューラルネットワーク（CNN）を構築しているデータサイエンティストです。
    あなたが構築したCNNモデルには、オーバーフィッティングの兆候が見られます。
    あなたは、オーバーフィットを減らし、モデルを最適な適合に収束させる必要がある。
    あなたはどの2つのアクションを実行する必要がありますか？各正解は完全な解決策を提示する。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
    ドロップアウトの際には、実際に学習データを減らすのではなく、ニューロンを落とすことで記憶量を減らし、オーバーフィットしないようにします。
</div></details>

### Q. 44
    Azure Machine Learning Studioで時系列データセットを使用しています。
    あなたは、Split Dataモジュールを使用して、データセットをトレーニングサブセットとテストサブセットに分割する必要があります。
    どの分割モードを使用する必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：4
    行を分割する。データを2つに分けるだけの場合は、このオプションを使用します。それぞれの分割に入れるデータの割合を指定できますが、デフォルトでは50対50の割合で分割されます。
不正解です。
    B: 正規表現による分割。1つの列の値をテストしてデータセットを分割したい場合にこのオプションを選択します。
    C: 相対表現分割。このオプションは、数値の列に条件を適用したいときに使用します。   
</div></details>

### Q. 45
    Azure Machine Learning Studioで分類タスクを実行しているところです。
    提供されたデータセットに基づいて、バランスの取れたテストサンプルとトレーニングサンプルを準備する必要があります。
    あなたは、0.75:0.25の比率でデータを分割する必要があります。
    各パラメータにどの値を使用する必要がありますか？回答するには、解答欄で適切な選択肢を選択してください。
    注：各正解の選択は1ポイントに相当します。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,5,8
ボックス1：行の分割
    データを2つに分けるだけなら、Split Rowsオプションを使用します。それぞれの分割に入れるデータの割合を指定できますが、デフォルトでは50対50で分割されます。
    また、各グループの行の選択をランダムにし、層化サンプリングを使用することもできます。層化サンプリングでは、2つの結果データセットに等しく値を割り振りたいデータの列を1つ選択する必要があります。
ボックス2：0.75
    数値をパーセントで指定した場合、または「%」文字を含む文字列を使用した場合、その値はパーセントとして解釈されます。パーセンテージの値はすべて範囲（0、100）内でなければならず、値0と100は含まれない。
ボックス 3: はい
    分割がバランスよく行われるようにするため。
ボックス4: いいえ
    層別分割のオプションを使用すると、層列を選択することによって、出力データセットをさらにサブグループごとに分割することができます。
</div></details>

### Q. 46
    あなたは、あるアルゴリズムのハイパーパラメータをチューニングしています。次の表は、異なるハイパーパラメータ、学習誤差、検証誤差を持つデータセットを示しています。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,4
ボックス1: 4
    訓練誤差と検証誤差が小さく、かつ最もよく一致するものを選ぶ。
    分散（検証誤差と訓練誤差の差）を最小にする。
ボックス2: 5
    分散（検証誤差と訓練誤差の差）を最小にする。    
</div></details>

### Q. 47
    あなたは、学生の教育期間、学位タイプ、および芸術形式という変数に応じて、学生の芸術作品の価格を予測するモデルを作成しています。
    あなたは、線形回帰モデルを作成することから始めます。
    あなたは、この線形回帰モデルを評価する必要があります。
    解決策 次の測定基準を使用します。平均絶対誤差，ルート平均絶対誤差，相対絶対誤差，精度，プレシジョン，リコール，F1スコア，およびAUC．
    解答は目標を達成しているか？
1. はい
2. いいえ
<details><div>
    答え：2
    Accuracy、Precision、Recall、F1 score、AUCは、分類モデルを評価するための指標です。
注）平均絶対誤差、根本平均絶対誤差、相対絶対誤差は、線形回帰モデルでOKです。
</div></details>

### Q. 48
    2,000行を含むデータセットがあります。Azure Learning Studioを使用して、機械学習の分類モデルを構築しています。Partition and Sample モジュールを実験に追加する。
    モジュールを構成する必要があります。次の要件を満たす必要があります。
    データをサブセットに分割する
    ラウンドロビン方式で行をフォルドに割り当てる。
    データセット内の行の再利用を許可する。
    モジュールはどのように構成すればよいのでしょうか。回答するには、回答エリアのダイアログボックスで適切なオプションを選択してください。
    OPTION:A

    OPTION:B
1. A
2. B
<details><div>
    答え：1
データをパーティションに分割する」オプションは、データセットをサブセットに分割する場合に使用します。このオプションは、クロスバリデーションのためのフォルド数をカスタムで作成したい場合や、行をいくつかのグループに分割したい場合にも便利です。
Studio (classic)でPartition and Sampleモジュールを実験に追加し、データセットに接続します。
PartitionまたはSampleモードでは、Assign to Foldsを選択します。
パーティショニングで置換を使用する。サンプリングした行を行のプールに戻し、再利用する可能性がある場合は、このオプションを選択します。その結果、同じ行が複数のフォールドに割り当てられる可能性があります。
置換を使用しない場合（デフォルトのオプション）、サンプリングされた行は再利用可能な行のプールに戻されません。その結果、各行が割り当てられるフォールドは 1 つになります。
ランダムな分割。行をフォルドにランダムに割り当てる場合は、このオプションを選択します。
このオプションを選択しない場合、行はラウンドロビン方式でフォルドに割り当てられます。
</div></details>

### Q. 49
    あなたは，提供された訓練集合を用いて2値分類モデルを構築しています．
    訓練セットは2つのクラス間でアンバランスになっています．
    あなたはデータのアンバランスを解決する必要があります．
    この目標を達成するために可能な3つの方法は何ですか？各正解は完全な解決策を提示する。
    注：各正解は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,2,4
A: ペナルティ付きモデルを試す
    同じアルゴリズムを使用しても、問題に対して異なる視点を与えることができます。
    ペナルティ付き分類は、学習中に少数派のクラスで分類ミスをした場合、モデルに追加コストを課します。これらのペナルティは、モデルが少数派クラスにもっと注意を払うようにバイアスをかけることができます。
B: 予測モデルを構築するために使用するデータセットを変更し、よりバランスの取れたデータにすることができます。
    この変更は、データセットのサンプリングと呼ばれ、クラスを均等にするために使用できる2つの主な方法があります。
    大量のデータ（数万から数十万インスタンス以上）がある場合、アンダーサンプリングのテストを検討します。
    データ数が少ない場合（数万レコード以下）には、オーバーサンプリングのテストを検討する。
D: 合成サンプルを作ってみる
    合成サンプルを生成する簡単な方法は、少数派のクラスのインスタンスから属性をランダムにサンプリングすることである。
</div></details>

### Q. 50
    あなたは、過去のデータに基づいて天候を予測するモデルを作成します。
    データストアからデータをロードする処理スクリプトを実行し、処理されたデータを機械学習モデルの学習スクリプトに渡すパイプラインを作成する必要があります。
    解決策 次のコードを実行します。

    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：1
process_stepとtrain_stepの2つのステップが存在します。
注
    パイプラインで使用されるデータは、あるステップの出力として PipelineData オブジェクトを提供し、1つ以上の後続のステップの入力とすることで、あるステップで生成され、別のステップで消費されることができる。
    PipelineDataオブジェクトは、パイプラインを構築する際に、ステップの依存関係を記述するためにも使用されます。あるステップが他のステップの出力を入力として必要とすることを指定するには、両方のステップのコンストラクタでPipelineDataオブジェクトを使用します。
例えば、パイプラインの列車ステップは、パイプラインのプロセスステップの process_step_output の出力に依存します。
</div></details>

### Q. 51
    あなたは、過去のデータに基づいて天候を予測するモデルを作成します。
    データストアからデータをロードする処理スクリプトを実行し、処理されたデータを機械学習モデルの学習スクリプトに渡すパイプラインを作成する必要があります。
    解決策 次のコードを実行します。

    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：2
</div></details>

### Q. 52
    あなたは、過去のデータに基づいて天候を予測するモデルを作成します。
    データストアからデータをロードする処理スクリプトを実行し、処理されたデータを機械学習モデルの学習スクリプトに渡すパイプラインを作成する必要があります。
    解決策 次のコードを実行します。

    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：2
</div></details>

### Q. 53
    scripts という名前のローカルフォルダに train.py という Python スクリプトがあります。このスクリプトは scikit-learn を使って回帰モデルを学習します。このスクリプトには、scripts フォルダにあるトレーニング データ ファイルを読み込むコードが含まれています。
    このスクリプトは、aml-compute という名前のコンピュートクラスタ上で Azure ML の実験として実行する必要があります。
    モデルの学習に必要なパッケージが環境に含まれていることを確認するために、実行を設定する必要があります。ターゲットの計算クラスターを参照するaml-computeという名前の変数をインスタンス化しました。
    解決策 次のコードを実行します。

    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：1
    scikit-learn estimator は scikit-learn のトレーニングジョブをコンピュートターゲット上で起動する簡単な方法を提供します。これは SKLearn クラスを通して実装されており，シングルノード CPU での学習をサポートするために使用できます．
</div></details>

### Q. 54
    scripts という名前のローカルフォルダに train.py という Python スクリプトがあります。このスクリプトは scikit-learn を使って回帰モデルを学習します。このスクリプトには、scripts フォルダにあるトレーニング データ ファイルを読み込むコードが含まれています。
    このスクリプトは、aml-compute という名前のコンピュートクラスタ上で Azure ML の実験として実行する必要があります。
    モデルの学習に必要なパッケージが環境に含まれていることを確認するために、実行を設定する必要があります。ターゲットの計算クラスターを参照するaml-computeという名前の変数をインスタンス化しました。
    解決策 次のコードを実行します。

    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：2
</div></details>

### Q. 55
    scripts という名前のローカルフォルダに train.py という Python スクリプトがあります。このスクリプトは scikit-learn を使って回帰モデルを学習します。このスクリプトには、scripts フォルダにあるトレーニング データ ファイルを読み込むコードが含まれています。
    このスクリプトは、aml-compute という名前のコンピュートクラスタ上で Azure ML の実験として実行する必要があります。
    モデルの学習に必要なパッケージが環境に含まれていることを確認するために、実行を設定する必要があります。ターゲットの計算クラスターを参照するaml-computeという名前の変数をインスタンス化しました。
    解決策 次のコードを実行します。

    解決策は目標を達成しましたか？
1. はい
2. いいえ
<details><div>
    答え：2
</div></details>

### Q. 56
    Azure Machine Learningを使用して、機械学習モデルを作成します。
    さまざまな計算コンテキストを使用して、モデルをトレーニングし、スコアリングすることを計画しています。また、Azure Machine Learningスタジオで新しいコンピュートリソースを作成する予定です。
    あなたは、適切なコンピュートタイプを選択する必要があります。
    どのコンピュートタイプを選択する必要がありますか？回答するには、適切なコンピュートタイプを正しい要件にドラッグします。各コンピュートタイプは、1回、2回以上、または全く使用しないことができます。ペイン間でスプリットバーをドラッグしたり、コンテンツを表示するためにスクロールする必要がある場合があります。
    注：正しい選択には1点ずつ価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,5,7
Box 1: アタッチドコンピュータ
Box 2：推論クラスタ
Box 3: トレーニングクラスタ
Box 4：アタッチド・コンピュータ
</div></details>

### Q. 57
    Azure Machine Learning designerを使用して実験を構築しています。
    データセットをトレーニングセットとテストセットに分割します。アルゴリズムとして、2クラス・ブースト決定木を選択します。
    あなたは、モデルの曲線下面積（AUC）を決定する必要があります。
    どの 3 つのモジュールを順番に使用する必要がありますか？回答するには、モジュールのリストから適切なモジュールを回答領域に移動し、正しい順序でそれらを配置します。
1. 
2. 
3. 
4. 
<details><div>
    答え：1
ステップ1：モデルの学習
    2クラス・ブースト決定木
    まず、ブーストされた決定木のモデルをセットアップします。

1. モジュールパレットから2クラス・ブースト決定木モジュールを探し、キャンバス上にドラッグします。
2. Train Modelモジュールを見つけてキャンバス上にドラッグし、2-Class Boosted Decision Treeモジュールの出力をTrain Modelモジュールの左入力ポートに接続します。
    Two-Class Boosted Decision Treeモジュールは汎用モデルを初期化し、Train Modelは学習データを使ってモデルを学習させます。
3. 3. 左側のExecute R Scriptモジュールの左出力をTrain Modelモジュールの右入力ポートに接続します（このチュートリアルでは、Split Dataモジュールの左側から来るデータをトレーニングに使用します）。
この部分の実験は、次のようになります。

ステップ2：モデルのスコアリング
    モデルのスコアリングと評価
    Split Dataモジュールで分離されたテストデータを使って、学習したモデルをスコアリングします。そして、2つのモデルの結果を比較して、どちらがより良い結果を生成したかを確認します。
    Score Model モジュールを追加します。

1. Score Model モジュールを探し、キャンバス上にドラッグします。
2. 2. Two-Class Boosted Decision Tree モジュールに接続された Train Model モジュールを、Score Model モジュールの左入力ポートに接続します。
3. 3. 右側の R スクリプト実行モジュール（テストデータ）を、Score Model モジュールの右側の入力ポートに接続します。

ステップ3：モデルの評価
    2つのスコアリング結果を評価し、比較するために、Evaluate Model モジュールを使用します。

1. 1. Evaluate Model モジュールを探し、キャンバス上にドラッグします。
2. 2. ブースト決定木モデルに関連するスコアモデルモジュールの出力ポートを、評価モデルモジュールの左の入力ポートに接続します。
3. 3. 右の入力ポートにもう一つのScore Model モジュールを接続します。
</div></details>

### Q. 58
    ラベル付けされた画像のセットを使用するマルチクラス画像分類ディープラーニングモデルを作成します。モデルを訓練するためにPyTorch 1.3フレームワークを使用するtrain.pyというスクリプトファイルを作成します。
    このスクリプトは、estimatorを使用して実行する必要があります。このスクリプトでは、estimator 用の環境に追加の Python ライブラリをインストールする必要がないようにする必要があります。モデル学習に必要な時間を最小限にする必要がある．
    スクリプトの実行に使用するestimatorを定義する必要があります。
    どのタイプのエスティメーターを使用する必要がありますか。
1. 
2. 
3. 
4. 
<details><div>
    答え：2
PyTorch、TensorFlow、Chainerのタスクに対して、Azure Machine LearningはそれぞれのPyTorch、TensorFlow、Chainerのエスティメータを提供し、これらのフレームワークを簡単に使用できるようにします。
</div></details>

### Q. 59
    あなたは、自動車価格を予測するモデルを訓練するために、デザイナーでパイプラインを作成します。
    データには非線形関係があるため、パイプラインはトレーニングデータの価格の自然対数（Ln）を計算し、この価格の自然対数値を予測するモデルをトレーニングし、次にスコアリングされたラベルの指数を計算して予測価格を得ます。
    学習パイプラインは図の通りです。(トレーニングパイプラインタブをクリックしてください。）
    学習パイプライン

    トレーニングパイプラインから、図のようなリアルタイム推論パイプラインを作成します。(Real-time pipelineタブをクリック)
    リアルタイムパイプライン

    Webサービスが予測される自動車価格としてスコア付きラベルの指数を返すように、またクライアントアプリケーションが入力値に価格値を含める必要がないように、推論パイプラインを変更する必要があります。
    推論パイプラインに加えなければならない3つの修正はどれですか？各正解は解答の一部を提示します。
    注：各正解の選択には1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,5
</div></details>

### Q. 60
    モデルをトレーニングし、Azure Machine Learningワークスペースに登録します。モデルをリアルタイムの Web サービスとしてデプロイする準備が整いました。
    Azure Kubernetes Service (AKS) 推論クラスターにモデルをデプロイしますが、モデルのデプロイに関連付けられたエントリスクリプトをサービスが実行するときにエラーが発生するため、デプロイが失敗します。
    コードを更新するたびにサービスの再デプロイメントを必要とせずに、コードを繰り返し修正してサービスを再ロードすることによって、エラーをデバッグする必要があります。
    どうすればよいでしょうか。
1. 
2. 
3. 
4. 
<details><div>
    答え：2
    Azure Container Instances (ACI) と Azure Kubernetes Service (AKS) で、Azure Machine Learning を使って、よくある Docker デプロイエラーを回避・解決する方法です。
    モデルデプロイのための推奨かつ最新のアプローチは、入力パラメータとしてEnvironmentオブジェクトを使用してModel.deploy() APIを経由することです。この場合、私たちのサービスは、デプロイ段階であなたのためのベースDockerイメージを作成し、必要なモデルをすべて1回の呼び出しでマウントします。基本的なデプロイタスクは以下の通りです。

1. 1. モデルをワークスペースのモデルレジストリに登録する。
2. 2. 推論構成を定義する。
    a. a. 環境 yaml ファイルで指定した依存関係に基づいて Environment オブジェクトを作成するか、弊社が調達した環境の 1 つを使用します。
    b. b. 環境とスコアリングスクリプトに基づいて推論設定 (InferenceConfig オブジェクト) を作成します。
3. 3. Azure Container Instance (ACI) サービスまたは Azure Kubernetes Service (AKS) にモデルをデプロイする。
</div></details>

### Q. 61
    既存のモデルを再トレーニングする場合。
    モデルの現在のバージョンをレジストリに残したまま、新しいバージョンのモデルを登録する必要があります。
    どうすればよいでしょうか。
1. 
2. 
3. 
4. 
<details><div>
    答え：2
モデルのバージョン。登録されたモデルのバージョン。モデルレジストリに新しいモデルが追加されると、バージョン1として追加される。同じモデル名で登録されたモデルごとに、バージョン番号がインクリメントされます。
</div></details>

### Q. 62
    Azure Machine Learning designerを使用して、回帰モデルのトレーニングパイプラインを作成します。
    あなたは、入力データ値のデータセットに対して非同期に予測を生成するエンドポイントとして展開するためにパイプラインを準備する必要があります。
    あなたは何をする必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：3
まず、トレーニングパイプラインをリアルタイムの推論パイプラインに変換する必要があります。このプロセスでは、トレーニングモジュールを削除し、リクエストを処理するためにWebサービスの入出力を追加します。
不正解です。
    A: Enter Data Manually モジュールを使用して、値を入力して小さなデータセットを作成します。    
</div></details>

### Q. 63
    近くの気象観測所からデータを収集します。以下のデータを含むweather_dfという名前のpandas dataframeを持っています。
    データは12時間ごとに収集されている：正午と真夜中。

    あなたは、自動機械学習を使用して、今後7日間の気温を予測する時系列モデルを作成することを計画しています。トレーニングの最初のラウンドでは、最大50種類のモデルをトレーニングしたいと思います。
    Azure Machine Learning SDKを使用して、これらのモデルをトレーニングするために自動化された機械学習実験を実行する必要があります。
    自動化された機械学習の実行を構成する必要があります。
    どのようにAutoMLConfigの定義を完了する必要がありますか？回答するには、回答領域で適切なオプションを選択します。
    注：正しい各選択肢は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：1,3,6,7,9
ボックス1：フォーキャスティング
    タスク 実行するタスクの種類。解決すべきMLの自動化問題の種類に応じて、「分類」、「回帰」、「予測」のいずれかの値をとることができる。
ボックス2：温度
    実験内で使用される学習データ。学習用特徴量とラベル列（オプションでサンプル重み列）の両方を含む必要があります。
ボックス3: observation_time
    time_column_name: 時間カラムの名前。このパラメータは、時系列を構築し、その頻度を推論するために使用される入力データ中の日時列を指定するために、予測時に必要とされる。この設定は非推奨となっている。代わりにforecasting_parametersを使用してください。
ボックス4：7
    "今後7日間の気温を予測する"
    max_horizon: 時系列周波数の単位で、希望する最大予測地平線を指定する。デフォルトは1である。
    単位は、フォアキャスターが予測すべき学習データの時間間隔（例：月単位、週単位）に基づく。タスクのタイプが予測である場合、このパラメータは必須です。
ボックス5：50
    "トレーニングの最初のラウンドでは、最大50種類のモデルをトレーニングしたい。"
    イテレーション（Iterations）。自動化されたML実験中にテストする異なるアルゴリズムとパラメータの組み合わせの合計数。
</div></details>

### Q. 64
    Azure Machine Learningデザイナーを使用して、機械学習モデルを作成します。Azure Kubernetes Service（AKS）推論コンピュートクラスタ上でリアルタイムサービスとしてモデルを公開します。デプロイされたエンドポイント構成に変更を加えません。
    あなたは、アプリケーション開発者がエンドポイントを消費するために必要な情報を提供する必要があります。
    あなたはアプリケーション開発者にどの2つの値を提供する必要がありますか？各正解は、ソリューションの一部を提示します。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：3,5
    Azure Machine Learning モデルを Web サービスとしてデプロイすると、REST API のエンドポイントが作成されます。このエンドポイントにデータを送信し、モデルによって返される予測を受け取ることができます。
    モデルをローカル環境、Azure Container Instances、Azure Kubernetes Service、またはFPGA（Field-Programmable Gate Arrays）にデプロイすると、Webサービスが作成される。Azure Machine Learning SDKを使用して、Webサービスにアクセスするために使用されるURIを取得します。認証が有効な場合、SDKを使用して認証キーまたはトークンを取得することもできます。
</div></details>

### Q. 65
    Azure Machine Learning Python SDK を使用して、複数のステップで構成されるパイプラインを定義します。
    パイプラインを実行すると、いくつかのステップが実行されないことが確認されます。以前の実行からキャッシュされた出力が代わりに使用されます。
    パラメータとソースディレクトリの内容が前回の実行から変更されていない場合でも、パイプラインのすべてのステップが実行されることを確認する必要があります。
    この目標を達成するために可能な2つの方法は何ですか？各正解は完全な解決策を提示します。
    注：各正解の選択は1点の価値があります。
1. 
2. 
3. 
4. 
<details><div>
    答え：2,3
B: regenerate_outputs を True に設定すると、新しいサブミットでは常にすべてのステップの出力が生成され、このランのどのステップでもデータの再利用が禁止されます。ただし、この実行が完了すると、以降の実行でこの実行の結果を再利用することができます。
C: パイプラインのステップ、入出力データ、ステップの再利用を扱う際には、以下の点に留意してください。
    ステップで使用されるデータがデータストアにあり、allow_reuse が True の場合、データ変更の変更は検出されません。もしデータがスナップショットの一部として（ステップのsource_directoryの下に）アップロードされている場合、これは推奨されませんが、ハッシュが変更され、再実行のトリガーとなります。
</div></details>

### Q. 66
    デザイナーを使用して、分類モデルの学習パイプラインを作成します。パイプラインは、モデルのトレーニングに必要なフィーチャーとラベルを含むデータセットを使用します。
    トレーニングパイプラインからリアルタイムの推論パイプラインを作成します。生成されたWebサービス入力のスキーマは、データセットに基づいており、モデルが予測するラベル列が含まれていることを確認します。このサービスを使用するクライアントアプリケーションは、この値を送信する必要がないようにする必要があります。
    あなたは、この要件を満たすために推論パイプラインを変更する必要があります。
    あなたは何をする必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：1
    デフォルトでは、Web Service Input は、それと同じダウンストリームポートに接続するモジュール出力データと同じデータスキーマを期待します。Select Columns in Dataset モジュールを使って、推論パイプラインのターゲット変数カラムを削除することができます。ターゲット変数カラムを削除した Select Columns in Dataset の出力が、Web Service Intput モジュールの出力と同じポートに接続されていることを確認します。
</div></details>

### Q. 67
    Azure Machine Learningデザイナーを使用して、トレーニングパイプラインを作成し、実行します。その後、リアルタイムの推論パイプラインを作成します。
    あなたは、Webサービスとしてリアルタイム推論パイプラインをデプロイする必要があります。
    リアルタイム推論パイプラインをデプロイする前に、何をする必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：4
インファレンシングクラスターを作成する必要があります。
    リアルタイムエンドポイントのデプロイ
    AKS サービスのプロビジョニングが完了したら、リアルタイム・インフィレンシング・パイプラインに戻ってデプロイを完了させます。
    キャンバスの上にある[デプロイ]を選択します。
    Deploy new real-time endpoint］を選択します。
    作成した AKS クラスターを選択します。
    デプロイ]を選択します。
</div></details>

### Q. 68
    Azure Machine Learningワークスペースに複数の機械学習モデルが登録されています。
    選択したモデルの公平性を評価するために、Fairlearnダッシュボードを使用する必要があります。
    あなたはどの3つのアクションを順番に実行する必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：1
ステップ1：評価するモデルの特徴を選択する。
ステップ2：バイナリ分類または回帰モデルを選択します。
    Azure Machine Learning にモデルを登録する。便宜上、登録したモデルのID（name:version形式の文字列）と予測器自身を対応付けた辞書に結果を格納します。
ステップ3：測定するメトリックを選択する
    フェアネスメトリクスを事前に計算する。
    Fairlearnのメトリクスパッケージを使用してダッシュボード辞書を作成する。
</div></details>

### Q. 69
    ある生物医学研究会社が，実験的な医療治療の臨床試験に患者を登録することを計画している．
    あなたは，この試験への患者の選択と登録を支援するために，バイナリ分類モデルを作成し訓練する．このモデルには、以下の特徴が含まれる。年齢，性別，民族性．
    このモデルは、異なる民族の人々に対して異なるパフォーマンス指標を返します。
    あなたは Fairlearn を使用して、Ethnicity 機能の各カテゴリの格差を緩和し、最小化する必要があります。
    どの技法と制約を使うべきですか？回答するには、解答欄で適切な選択肢を選択してください。
    注：各正解選択は 1 ポイントに相当します。
1. 
2. 
3. 
4. 
<details><div>
    答え：2，3
ボックス1：グリッドサーチ
    Fairlearnオープンソースパッケージは、後処理と削減の不公平感緩和アルゴリズムを提供します。ExponentiatedGradient、GridSearch、ThresholdOptimizer。
注：Fairlearnオープンソースパッケージは、後処理と削減の不公平感緩和アルゴリズムタイプを提供します。
削減。これらのアルゴリズムは、標準的なブラックボックス機械学習推定量（例えば、LightGBMモデル）を取り、一連の再重み付けトレーニングデータセットを使用して再トレーニングモデルのセットを生成します。
ポストプロセッシング。これらのアルゴリズムは、既存の分類器と感度の高い特徴を入力とします。
ボックス2：人口統計学的パリティ
    Fairlearnオープンソースパッケージは、以下のタイプのパリティ制約をサポートしています。Demographic parity、Equalized odds、Equal opportunity、Bounded group lossです。
</div></details>

### Q. 70
    バイナリ分類モデルを作成します。モデルは、Azure Machine Learningワークスペースに登録される。Azure Machine Learning Fairness SDK を使用して、モデルの公正さを評価する。
    ローカルマシン上でモデルのトレーニングスクリプトを開発する。
    あなたは、Azure Machine Learningスタジオにモデルフェアネスメトリクスをロードする必要があります。
    あなたは何をする必要がありますか？
1. 
2. 
3. 
4. 
<details><div>
    答え：3
    azureml.contrib.fairness パッケージをインポートして、アップロードを実行します。
</div></details>

### Q. 
1. 
2. 
3. 
4. 
<details><div>
    答え：
</div></details>
